# Les scripts de workflow sont lus OCTET PAR OCTET par l'outil Workflow, qui refuse tout caractere de controle
# (« script contains control characters that would be hidden in the approval dialog ») -- y compris le \r qu'une
# extraction Windows sous core.autocrlf=true ajoute a chaque ligne. Mesure le 2026-09-26 (agagi-40) : blob LF sain,
# extraction CRLF (275 \r), Refutateur inlancable sur cette machine malgre c0274ea9. Ces fichiers s'extraient en LF
# partout ; temoin : tests/sandbox/test_workflows_no_invisible_chars.py (lit le disque, pas le blob).
.claude/workflows/*.js text eol=lf
