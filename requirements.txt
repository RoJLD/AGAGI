numpy>=1.26.0
pandas>=2.1.0
matplotlib>=3.8.0
graphviz>=0.20.1
kuzu
scikit-learn>=0.4.0
pydantic>=2.4.0
fastapi>=0.110.0
uvicorn>=0.23.2
pytest>=7.4.0
langgraph>=0.0.26
langchain>=0.1.0
langchain-core>=0.1.0
PyYAML>=6.0
# check_agi_taxonomy (porte 6) valide demands.json par jsonschema, importe PARESSEUSEMENT : absent, la porte
# declare une VIOLATION (elle ne valide pas en silence) -- garde-methodologique rouge le 2026-09-25 pour ca.
jsonschema>=4.0
# tools/jobs (lease, doctor, run), tools/cost_guard, tools/pm (bulletin, snapshot) lisent la VIE d'un processus par
# psutil, importe PARESSEUSEMENT et rendu None sans lui -- degrade honnete, mais ~19 tests de la suite le mesurent
# (test_jobs, test_doctor_visibility, test_pm_snapshot, test_pm_tick, test_cost_guard) : rouges en CI le 2026-09-26.
psutil>=5.9
