---
id: REF-REGISTRE-ERREURS
type: REF
title: "Registre vivant des classes d'erreur — chaque erreur trouvée doit y atterrir avec sa garde"
status: active
---

## Pourquoi ce registre existe

Quand une revue trouve une erreur, elle est corrigée et racontée dans un record. **Rien ne garantit
qu'elle devienne une garde.** Conséquence mesurée sur l'arc WARM-005→009 : la même classe d'erreur est
revenue **trois fois** — généraliser depuis `agents[0]`, puis depuis trois cas saillants, puis d'une
population à une autre — dont une fois **dans le record qui la dénonçait**.

Le cliquet de `check_instrument_calibration.py` résout ce problème pour les **instruments**. Ce registre
est l'équivalent pour les **classes d'erreur**.

## Rituel (obligatoire)

Toute erreur trouvée en revue, ou tout run nul/contaminé, doit :
1. être **rattachée** à une classe existante, ou en créer une ;
2. recevoir un statut de garde : **`exécutable`** · **`documenté`** · **`non automatisable`** ;
3. si `exécutable` → la garde est écrite ET testée dans la même passe ;
4. si `non automatisable` → le dire explicitement (c'est ce qui justifie l'obligation de revue).

Une erreur qui repasse deux fois avec un statut `documenté` doit être **promue** en `exécutable` ou
reclassée `non automatisable`. Pas de troisième occurrence tolérée sans changement de statut.

---

## Registre

| # | Classe d'erreur | Occurrences (arc WARM-005→009) | Statut | Garde |
|---|---|---|---|---|
| **E1** | **Contrôle qui ne peut pas ÉCHOUER** (no-op analytique présenté comme contrôle négatif) | contrôle négatif tautologique : 6/8 tableaux bit-identiques, rapporté comme `wins 2/48` **(2026-09-01, méta-niveau)** — la classe s'applique au REGISTRE lui-même : le statut `exécutable` était une promesse que rien ne vérifiait. Audit : 8 classes sur 14 ne pointaient aucun test, donc rien ne disait OÙ était la preuve qu'elles savent refuser. **Garde armée** : `tools/check_guard_negative_cases.py` (baseline à ZÉRO) + `tests/sandbox/test_guard_negative_cases.py` (9 contre-exemples gelés, dont 3 issus de défauts RÉELS de l'instrument lui-même). **(3, 2026-09-07, P2.15 — LE SEUIL, PAS LE CONTRÔLE)** : symétrique exact de l'occ. 2 d'E2. Là où E2 déplaçait vers la BARRE l'impossibilité de RÉUSSIR, celle-ci y déplace l'impossibilité d'ÉCHOUER. La barre de vitalité `1/K+0.15 = 0.3167` est franchie par un substrat PROUVABLEMENT INCAPABLE de la tâche : forme close `σ(W[j,j])·tanh(W[key,j]+W[K+q,j])`, score SÉPARABLE, plafond mesuré **au moins 30/36, et très probablement 36/36** — cf. la rectification ci-contre en E19 occ. (4) : trois recherches indépendantes du même jour ont rendu 29/36, 34/36 puis 36/36, par ordre d'effort croissant. Quelle que soit la valeur exacte, elle est TRÈS au-dessus de la barre. La franchir n'établissait donc aucune capacité, seulement que le plancher était bas. **Le sous-cas qui compte le plus** : le plafond publié le 2026-09-02 valait 0.3889, avec DEUX contrôles appariés verts (table libre → 1.000 ; forme close sur cible séparable → 1.000). Ils innocentaient la FORME et l'OPTIMISEUR ; aucun ne pouvait dire si la RECHERCHE avait convergé — la seule question qui fixe un plafond. Reproduit à dessein : à 2 restarts × 60 pas les deux contrôles valent 1.000 et le plafond lit 0.278. **Un contrôle de CAPACITÉ ne calibre pas un contrôle de BUDGET** (E19 déplacé du pas d'apprentissage vers l'effort de recherche). **Gardes armées** : `assert_bar_separates_the_incapable` (désormais APPELÉE), `tools/check_bar_separation.py` (porte 9, AST), `saturation_control` dans `tools/plain_substrate_ceiling.py`. **(2026-09-09, TROIS D'UN COUP, et c'est le META-NIVEAU)** : le premier tir de `tools/check_gate_mutation.py` — qui casse chaque porte du hook d'UNE ligne et exige qu'un témoin rougisse — a trouvé **trois portes dont le verdict BLOQUANT n'avait AUCUN contre-exemple**. Porte 1 : neutraliser la détection d'**orphelin** (`if not has_gate and not has_edge:` -> `if False:`) laissait les onze tests du graphe VERTS — ils couvraient les clés d'arêtes, `gate_unlinked` et le mismatch gate/tests, c'est-à-dire tout SAUF le verdict sur lequel la porte bloque. Porte 2 : tout le fichier testait `collision_coverage` (la fonction qui décide si une collision est COUVERTE) et rien ne testait `scan_collisions` (celle qui la DÉTECTE) — le second maillon gardé, le premier nu. Porte 6 : quatorze tests confrontaient le JUGE `_defects` à des réponses connues, et les deux tests d'arbre réel attendaient que `scan` ne trouve RIEN (dette close) — donc casser son report leur donnait exactement ce qu'ils attendaient. ⚠️ **La forme est générale** : quand la dette d'un cliquet tombe à zéro, tous ses tests d'arbre réel attendent le VIDE, et sa couche de report devient structurellement intestable SUR LE RÉEL — il faut un défaut FABRIQUÉ pour la mesurer. Aucune relecture ne les avait vus en deux mois ; la mutation les dit en trois secondes. Les trois contre-exemples manquants ont été écrits dans la même passe. **(2026-09-14, un CONTRÔLE NUL MORT)** : `null_metronome_gap` dans `craft_or_starve_edr` — la borne nulle du binding (un métronome open-loop qui ne lit pas l'inventaire) — était évaluée à `T = 200`, où le métronome est **mort au dernier quart : 0 vivant sur 64**, pour E0 = 16, 32 et 64. Ses deux masques conditionnels étaient vides, `p1 = p0 = 0.0` par défaut, et son « gap ≈ 0 » valait `0 − 0`. Le test `test_null_metronome_gap_is_low` gelait ce zéro : le contrôle passait PARCE QU'il ne pouvait rien produire. Trouvé en rendant `None` les probabilités indéfinies (P2.52) : la garde a levé, le test a rougi, et la mesure a dit pourquoi. Mesurable à T ≤ 40 où il vaut 0 pour de vrai (p1 = p0 = 1). Aucun record ne s'en réclame. Contre-exemple gelé : `test_null_metronome_REFUSES_a_DEAD_cohort` (la config d'origine, exactement), apparié au test d'origine réécrit à un horizon où le contrôle survit. **(occ. 2026-09-24, la clause `closes_when` AUTO-RÉFÉRENTIELLE — et elle s'est PROPAGÉE par copie)** : une clause qui grep sa PROPRE ligne « **P4.x — ✅ CLOSE** » dans le backlog ne peut structurellement pas échouer autrement qu'en restant fausse, ni signaler qu'une condition de FOND est remplie — elle recopie la décision de l'auteur. Écrite par moi sur P4.11 le 2026-09-16, puis COPIÉE sur P4.17 par une autre session : deux cas sur les 60 clauses du backlog (balayage complet, demandé par la session qui porte le harnais). ⚠️ SPÉCIFICITÉ, à ne pas confondre : la clause de P4.15 vise `P4.9`, une AUTRE entrée — dépendance légitime, et elle a FONCTIONNÉ (P4.15 s'est fermée quand P4.9 est passée CLOSE). C'est l'auto-visée qu'il faut interdire, pas la famille. | `exécutable` | `assert_ablation_changes_something` · contre-exemple GELÉ : `test_tautological_control_is_rejected` · P2.15 : `test_the_ratchet_FIRES_on_a_chance_plus_margin_bar` · `test_the_aggregate_reports_INVALID_on_an_UNCONVERGED_search` · **méta-niveau : `tools/check_gate_mutation.py` (porte 15)**, dont le contre-exemple gelé est la PAIRE `test_le_harnais_dit_TUEE_sur_une_mutation_qui_CASSE_la_porte` / `test_le_harnais_dit_SURVECUE_sur_une_mutation_INERTE` — sans le second, un harnais qui répondrait « TUEE » à tout passerait le premier et certifierait des gardes creuses **Occ. 2026-09-24 (clause auto-référentielle)** : P4.11 ré-ancrée sur la SORTIE (`grep_present=docs/EDR/TD-STEP-PILOT-R0_….md::verdict: TD0_INERTE`) ; P4.17 signalée à son propriétaire. Durcissement de la porte 4 inscrit en P4.20 avec son contre-exemple et sa mutation : refuser une clause dont le motif vise l'entrée qui la porte, en épargnant la dépendance vers une AUTRE entrée (cas P4.15 gelé). |
| **E2** | **Bras qui ne peut pas RÉUSSIR** (l'issue cherchée est structurellement inatteignable) | WARM-009 : bras « production » dans un monde n'engendrant **aucun** `Fruit`. **(2, 2026-09-02, EDR-DELAYED-COORD — LE SEUIL, PAS LE BRAS)** : la même impossibilité déplacée du BRAS vers la BARRE. Au bruit par défaut du module (`flip_p=0.3`), le bras le PLUS FACILE de la sonde (PRESENT sans leurre, aucune rétention demandée) plafonne à **0.239**, soit 0.078 SOUS la barre de vitalité `1/K+0.15 = 0.3167` : toute cellule mesurée à ce réglage et comparée à cette barre est un instrument à **ISSUE UNIQUE**, il ne peut rendre qu'« échec ». À `flip_p=0` le MÊME bras rend **0.3375** et la barre redevient franchissable — une seule variable sépare les deux, et ce n'est ni la capacité ni le seuil, c'est le **RÉGIME**. **(3, 2026-09-07, EVO-011 — LA CHAÎNE EST COUPÉE DANS LA RÈGLE SCELLÉE ELLE-MÊME)** : `EVO-011.json` fait peser `mammoth_kills` 400 sur la chaîne obs[4]→throw→kill. Or un lancer ne peut pas tuer une proie : le bloc balistique fait `hit_entity["energy"] -= damage` SI la clé existe et pose `stunned` SINON, et une proie n'a que `{x, y, type, stunned, hp}` (`_spawn_prey_instance`) — `mammoth_kills` n'est incrémenté que par la MÊLÉE. Le bras ne pouvait pas réussir, et les trois bras auraient rendu le même chiffre même avec des lances. Trouvé en réparant le harnais, AVANT tout run. ⚠️ Une règle SCELLÉE n'est pas une règle VÉRIFIÉE : le sceau prouve qu'elle n'a pas bougé, pas que sa chaîne causale existe dans le monde. Troisième manifestation du jour de la même famille (avec **E19**, un pas validé en régime facile, et **P2.15**, une barre sous le plafond structurel du substrat qu'elle déclare nul) **(occ. 4, 2026-09-14, [[EDR-CALIB-LEARNER]] — LA DOSE COMME BRAS QUI NE PEUT PAS REUSSIR)** : les trois nuls « le crédit in-world n'apprend pas à froid » (S2-009 §crédit, S2-010, S2-011) mesurés sur des agents morts à 7-9 ticks, soit quelques dizaines de mises à jour de crédit, JAMAIS comptées ; à ~2250 mises à jour (cohorte immortelle) le MÊME apprenant apprend, 12/12 seeds au-dessus de la référence lr=0 appariée. Garde : `count_learning_events` publie la dose à côté de tout nul (`tests/sandbox/test_learning_events.py`). **(occ. 5, 2026-09-15, P2.60 — un banc de juillet dont le POSITIF était impossible PAR DÉFAUT)** : `compare_factorial` (tag keep/edr-177-178) partait à `seeds=(0, 1, 2, 3)`, sous le plancher n ≥ 5 de `compute_ab_verdict` — aucune cellule ne pouvait rendre un positif sans que l'appelant corrige le défaut ; trouvé au portage par le cas de calibration `seeds-default:5` (tests/sandbox/test_edr177_178_calibration.py), corrigé à `(0, 1, 2, 3, 4)`. **(occ. 6, 2026-09-24, P4.11 — LE POINT DE FONCTIONNEMENT, PAS LE BRAS, et la faute est dans une CLÔTURE)** : la clôture de P4.11 (`7fa6b2d8`) publiait « invariance au pas **RÉFUTÉE** » en lisant `aide09 = 0/12` à lr 2,0. Or les bras de R1 à ce pas sont `lr0_reference@2`, `td0@2`, `tdlam09@2` — **aucun `td0_d0@2`**, donc aucun contrôle de CHEMIN — et `td0@2` lui-même n'y rend que **1/12** : à ce point de fonctionnement une seule issue était atteignable, « pas d'aide », que la trace transporte du crédit ou non. Un nul lu dans un régime quasi mort ne réfute pas un positif lu dans un régime vivant (symétrique d'**E19**, qui interdit de calibrer le régime DUR sur le FACILE). Deux aggravants : (a) le bras manquant était inscrit **dans le même commit** en P4.19 (a) — la lacune a été vue, consignée, puis ignorée au moment de conclure ; (b) la coupe de la ligne lr 2,0 de R2 venait de la CONTENTION et non de la structure, donc le point était MESURABLE et ne l'avait pas été. Amendé le jour même (`17cff006`) : statut ramené à **NON ÉTABLIE** dans l'en-tête de P4.11 et dans la jambe 1 du critère de révision d'ADR-005 ; aucun verdict ne bouge, seule la portée. ⚠️ **Ce que l'occurrence ajoute à la classe** : `assert_positive_control` garde un BRAS, une fois, au lancement ; rien ne garde une PROSE qui agrège plusieurs points de fonctionnement en une phrase — et c'est la phrase qui est lue. Garde NON écrite dans cette passe (elle demande une DÉCLARATION point par point, pas une devinette) : dette nommée **P4.21**, avec sa forme. | `exécutable` | `assert_positive_control` · contre-exemple GELÉ : `test_positive_control_catches_incapable_arm` · **contre-exemple GELÉ (occ. 3, 2026-09-07)** : `test_a_hit_prey_is_STUNNED_not_killed_by_a_throw` (`tests/sandbox/test_throw_counters_legacy.py`) |
| **E3** | **Métrique dégénérée** (plancher/plafond) lue comme « pas d'effet » | 24 génomes tous à 6.0-7.2 ticks ; 32/48 déjà à `move_acc = 1.000` ; **WARM-002 (occurrence ANTÉRIEURE, trouvée le 2026-07-21)** : ratio ≈ 1.00 lu sur un bras intact à 5.0-7.2 ticks, soit **SOUS** le plancher no-perception (9.0) → verdict `PAYSAGE PLAT` sur le MONDE, réfuté par WARM-010. **(3, 2026-09-01, LANG-MEMORY — la garde CONTOURNÉE par un chemin de code voisin)** : `functional_aliasing` était calculé comme `leakage = \|median(control_intact) − median(control_ablated)\| ≤ tol → SURGICAL → 'pass'`, **sans jamais vérifier que le bras CONTROL est VIVANT** — donc en contournant `_degeneracy`, qui protégeait pourtant le bras principal du MÊME instrument. Les deux dégénérescences sont ATTESTÉES dans les données committées, pas hypothétiques : **plancher** (`train_control=False` n'entraîne jamais CONTROL → deux mesures de hasard, `leakage≈0`) et **plafond** (`results/lang_memory_diagnostic.json` : `control_ablated=[1.0,1.0,1.0]`, `leakage=0.0` **mécaniquement**). Un `pass` y était donc arithmétiquement forcé. Aggravant : ce `pass` **dispensait** du contrôle de demande à la porte du graphe (cf. **E19**/durcissement du même jour) — une arête aurait pu se graver sur deux gardes dont AUCUNE ne pouvait échouer. **(4, 2026-09-01, `ablation_verdict` — l'instrument le plus cité du graphe, revue adversariale AVANT une 3ᵉ arête)** : la borne `decoy` était UNILATÉRALE (`ratio <= decoy_ceiling`, decoy_ceiling=1.3) — un ratio **0.596** mesuré sur le bras de contrôle Lewis (médiane 0.592 -> 0.994, l'ablation MULTIPLIE le contrôle par **1.68**) tombait dans cette borne et rendait `X_DECOY` (« ablation inerte »), donc `specificity_control='pass'`. `_degeneracy` ne l'attrapait pas : elle ne teste que « intact au PLANCHER » et « les DEUX bras au PLAFOND », or ici l'intact est VIVANT (0.592, au-dessus du plancher déclaré 1/6) et seul l'ABLÉ frôle le plafond déclaré (0.994 < 1.0 STRICT) — aucune des deux conditions ne se déclenche. Un effet massif de SIGNE INVERSE n'est pas une inertie. **(5, 2026-09-01, `assert_verdict_invariant_to_optimizer` — la garde ÉCRITE POUR ATTRAPER LES NULS ARTEFACTUELS EST ELLE-MÊME EN DÉFAUT, cf. P2.21)** : livrée le 2026-09-01 pour lire une FERMETURE D'ÉCART entre le bras testé et le bras de RÉFÉRENCE sur un balayage de `lr`, sans jamais vérifier que ce bras de RÉFÉRENCE reste VIVANT aux points comparés — donc elle tire tout autant quand c'est la référence qui S'EFFONDRE (aucun artefact à dénoncer) que quand c'est le bras testé qui monte (le cas visé, un vrai artefact d'optimiseur). Constaté sur les données d'[[EDR-DELAYED-COORD]], où les DEUX bras sont au plancher : la fermeture d'écart mesurée n'a rien d'un artefact — les deux bras sont simplement morts. Même faute-mère que l'occurrence (4) : une lecture d'écart sans vérifier la VITALITÉ du terme de référence confond une fermeture RÉELLE (dégénérescence conjointe) avec l'artefact qu'elle est censée signaler — exactement la forme du défaut qu'elle corrigeait chez `functional_aliasing` (occ. 3) la veille. **Correctif écrit dans la MÊME passe, par une tâche PARALLÈLE (P2.21)** : exiger que le bras de RÉFÉRENCE reste vivant aux deux points de `lr` avant de lire une fermeture d'écart, sinon rendre un verdict distinct (`INCONCLUSIVE_REFERENCE_COLLAPSED`) plutôt qu'un refus silencieux — plus le contrôle POSITIF apparié (une vraie fermeture d'écart, référence vivante, doit continuer à tirer), sans lequel on remplacerait une garde trop laxiste par une garde trop stricte, le même défaut de l'autre côté | `exécutable` | `assert_not_degenerate` (bras principal) — ⚠️ **mais rien ne la RÉTRO-APPLIQUE aux records déjà gravés** (trou du cliquet, cf. E14). **Occ. 3** : `alias_guard_verdict` (`tools/language_memory_demand_probe.py`) fait passer le bras CONTROL par `ablation_verdict` aux mêmes bornes et émet `DEGENERATE_CONTROL` au lieu d'un `pass` silencieux ; 5 branches déclarées au cliquet, **contre-exemples GELÉS aux DEUX bornes** (plancher ≈1/K ; plafond 1.0/1.0 avec les valeurs réelles du JSON) **plus un contrôle POSITIF** (CONTROL vivant et chirurgical → `pass`), sans lequel une garde qui refuse tout serait aussi inutile qu'une garde qui accepte tout. ⚠️ Conséquence assumée et MESURÉE : **plus aucune config de LANG-MEMORY ne produit `pass`** — le blocage était déjà réel, il est maintenant EXPLICITE au lieu d'être masqué · contre-exemple GELÉ : `test_degenerate_metric_is_rejected_on_floor_and_ceiling`. **Occ. 4** : `ablation_verdict` borne désormais `decoy` des DEUX côtés (`1/decoy_ceiling <= ratio <= decoy_ceiling`) et une nouvelle branche `INCONCLUSIVE_INVERTED` (`ratio < 1/decoy_ceiling`) précède `X_DECOY` dans l'ordre de décision · contre-exemple GELÉ : `test_ablation_verdict_REFUSES_an_inverted_effect_as_decoy`, **plus un contrôle POSITIF** (`test_ablation_verdict_STILL_accepts_a_genuine_decoy` — un vrai decoy, ratio ≈ 1.0, doit RESTER `X_DECOY`) et une non-régression du sens principal (`test_ablation_verdict_still_collapses_a_real_demand`), sans lesquels une garde qui refuse tout serait aussi inutile qu'une garde qui accepte tout. **Occ. 5** : correctif de `assert_verdict_invariant_to_optimizer` (vitalité du bras de référence + verdict `INCONCLUSIVE_REFERENCE_COLLAPSED` + contrôle positif apparié) écrit dans la MÊME passe par la tâche P2.21 — voir cette entrée pour le détail du code et ses tests |
| **E4** | **Vérification vide** (« 0 échec » indiscernable d'un succès) **(2026-09-06, P2.28 — occurrence de forme NOUVELLE : la sémantique de FAMILLE appliquée à UNE porte et pas à l'autre ; chaque membre tombait dans un trou différent, la famille DELAYED-COORD-LR-N12 passait vert sans être confrontée à un record qui EXISTAIT et la CITAIT. Garde : `_inspection()` source unique des trois portes + contre-exemple gelé `test_family_reached_through_a_CITING_record_is_INSPECTED_and_can_FAIL`. ⚠️ La garde elle-même a été calibrée par le test du dépôt réel — son réfutateur étant mort : la clé `record:` collisionnait avec un `rule.record` en PROSE présent dans 24 règles scellées, et criait sur 13 familles incorrigibles ; corrigé par localisation, enveloppe stricte / sceau lenient.)** | `pytest -k` désélectionnant les 1034 tests, lu comme non-régression validée. **(2026-07-27, EVO-005)** : le cliquet de calibration rendait « 0 NOUVEAU non calibré » alors que `benchmark_cognitive` (qui produit LE chiffre du verdict) et `benchmark_discrimination` (EVO-003) lui étaient **invisibles** — aucun motif de nommage ne les attrape. Une garde ne peut pas signaler ce qu'elle ne sait pas NOMMER, et son « OK » est alors indiscernable d'une vraie couverture. C'est le **3ᵉ** angle mort du même outil (motif, puis répertoire, puis motif à nouveau) — compteur qui ne peut lire QUE zéro : `_throw_did` n'est écrit que dans la branche `use_torch_inworld and torch_throw_gate` (`world_1_stoneage.py:1313`) ; hors torch il n'est JAMAIS posé, donc « 0 lancer » était un artefact de compteur et non une mesure. Même forme qu'en EVO-009 (bras baseline non instrumenté). **Règle** : avant de lire un compteur comme une mesure, vérifier qu'il existe un chemin de code qui l'ÉCRIT dans le régime testé. **(4, 2026-08-04, EVO-024)** — forme SILENCIEUSE, la plus dangereuse : un runner dérivé par transformation de texte a gardé le pré-vol d'[[EDR-EVO-023]] (croissance on/off) tout en ANNONÇANT celui d'EVO-024 (flag on/off). Le dispositif tournait, affichait un titre juste, et ne vérifiait PAS ce qu'il prétendait. Les 4 transformations ratées précédentes avaient échoué BRUYAMMENT (SyntaxError, NameError) ; celle-ci non — le run a dû être tué. **Règle** : ne pas dériver un runner d'expérience par regex, l'écrire explicitement. **(5, 2026-09-01, audit des cliquets)** — un cliquet dont on ÉLARGIT la baseline rend un vert VIDE. `tools/record_link_baseline.json` est passé de 19 à 42 orphelins et de 5 à 8 collisions dans l'arbre de travail SANS commit ; deux rapports « OK, aucun nouveau » ont été produits contre cette baseline élargie, alors que face à celle de `HEAD` il y avait 23 orphelins et 3 collisions NOUVEAUX. **Mécanisme** : le hook ne lançait la garde que si un `docs/**/*.md` était indexé — stager la baseline SEULE ne déclenchait rien. **Garde armée** : la baseline est désormais elle-même un déclencheur du hook. **(6, 2026-09-02, revue adversariale C3 — le trou RÉSIDUEL de l'occ.5)** : la baseline stagée SEULE déclenchait bien la garde, mais `--only` recevait une liste VIDE (`grep -v record_link_baseline`) et le checker filtrait tout → le ratchet ne pouvait pas échouer sur ce commit précis. Garde corrigée dans la même passe : baseline stagée sans record → run NON scopé (`tools/hooks/pre-commit`, porte 1). **(2026-09-02 — angle mort SUIVANT, et il portait sur les GARDES elles-mêmes)** : l'heuristique de nommage ne connaissait aucun motif `assert_*`. Vérifié mécaniquement — `assert_bar_is_reachable`, livrée avec ses 5 cas de calibration ET sa déclaration dans `CALIBRATED`, était **absente du rapport du cliquet**, sa déclaration ignorée en SILENCE ; et `assert_verdict_invariant_to_optimizer` n'était détectée **que par accident**, son nom contenant « verdict ». Autrement dit **la famille de fonctions qui SONT les gardes du dépôt échappait au cliquet censé garantir leur calibration** : une future garde aurait pu être livrée sans le moindre cas, le compteur affichant « OK ». Décompte fait AVANT d'élargir : 11 `assert_*` dans le périmètre, 2 déclarées, 9 non — mais **les 9 avaient déjà leurs cas** `fires`+`spares`, vérifiés un par un : la lacune était de **DÉCLARATION**, pas de calibration → élargissement à **ZÉRO dette**, rien de gelé (`uncalibrated: []`, aucun `--update-baseline`). Portée choisie par la MESURE : `assert_aux_off_safe` (garde d'EDR-WARM-008) vit **hors** du module de pré-vol — la restreindre à ce module aurait rouvert le trou. Contre-exemple gelé vérifié **PAR MUTATION** (il échoue sous les 3 régressions : motif retiré, portée rétrécie, déclaration rejetée en silence). Cliquet 105 → 115 calibrés, 0 nouveau. **(7, 2026-09-15, P2.62 — la porte 2 du hook ne pouvait pas TIRER sur un commit qui n'élargissait QUE sa baseline)** : `tools/instrument_calibration_baseline.json` n'est pas un `.py`, et le regex de la porte 2 (`^(tools|src/seed_ai)/.*\.py$`) ne la voyait pas — l'occ. 5 (porte 1, 2026-09-01) rejouée sur une autre porte, jamais rétro-appliquée (même mécanisme qu'E14) ; invisible deux semaines parce que la baseline était VIDE, comptait le jour où elle gèle 3 dettes nommées (`compute_policy_gradient`, `learn` legacy, `learn_episode_bptt`). Seconde faille sur la même ligne : le hook scopait `src/seed_ai` alors que le checker balaye `src` ENTIER depuis le 9ᵉ élargissement (2026-09-09) — un apprenant non calibré dans `src/agents/` était committable sans que la porte tire. Correctif : regex de la porte 2 élargi à `^((tools|src)/.*\.py|tools/instrument_calibration_baseline\.json)$`, baseline stagée seule = run NON scopé (modèle porte 1). Garde restant `documenté` pour cette porte : aucun témoin exécutable ne lit le hook (limite déclarée de `check_gate_mutation` : les témoins ne voient pas une mutation du hook en mémoire) — candidat : un test de forme sur `tools/hooks/pre-commit` qui rétro-applique l'occ. 5 à TOUTES les portes (chaque porte à baseline doit lister sa baseline dans son regex). Trouvée EN PASSANT par l'agent P2.62, avec le 5ᵉ axe de faillibilité de l'heuristique de nommage : la PROFONDEUR (tous les motifs étaient ancrés `^def`, or les apprenants sont des MÉTHODES). **(8, 2026-09-15, P3.5 a — « 0 numéro double » rendu par un cliquet qui ne LISAIT pas le titre)** : `tools/check_backlog_freshness.py` exigeait `P\d+\.\d+` en tête d'entrée ; le titre composite `**P1.x / P2.45 — ✅ CLOSE par P2.54 …` (l.2326 du backlog) lui échappait ENTIÈREMENT — ni entrée (son bloc était avalé par l'entrée précédente, donc une clause `closes_when:` y aurait été jugée contre le statut d'une AUTRE entrée), ni numéro (P2.45 est en tête d'une SECONDE entrée, l.2981, et le compte de doublons rendait OK). Un titre que le cliquet ne lit pas ne peut pas être en double : entrée invisible → succès (forme (a) de CLAUDE.md commise par une garde, même mécanisme que l'occ. 2 : une garde ne signale pas ce qu'elle ne sait pas NOMMER). Élargir le motif a révélé un doublon RÉEL (`numero-double:P2.45`) que la baseline gelée ne portait pas : résolu dans le backlog (l.2326 renommée `P1.x`), jamais gelé par `--update-baseline`. Garde : `_NUM`/`_TETE` — une tête composite vote pour chacun de ses numéros, `P1.x` est un substitut (pas un numéro : deux entrées « à numéroter » ne sont pas deux versions d'une même tâche, les signaler FABRIQUERAIT un doublon), même tête pour `_ENTREE`, `_TASKNUM` et `compter_entrees` ; 4 contre-exemples gelés dans `tests/sandbox/test_backlog_freshness.py` (3 rouges contre HEAD + 1 no-op de spécificité), mutation déclarée à la porte 15 (porte 4 : 3/3 tuées). Livrée par un agent en worktree (P3.5). **(2026-09-22, le DOCTOR — l'outillage d'exploitation, pas une sonde)** : `python -m tools.jobs.doctor` a rendu DEUX affirmations négatives fausses sur le même écran, et leur conjonction se lit « le run est mort » — conclusion que j'ai tirée et écrite. (a) **« processus python du projet : 0 »** alors qu'un run tournait depuis SIX JOURS (pid 54476, `python -m tools.evo_runs.s2_blind_champion_bis --decomp`, 623 Mo, 5/7 seeds faits) : `project_processes` cherchait `PROJECT_MARKERS = ("AGAGI",)` dans la LIGNE DE COMMANDE, or la forme canonique que CLAUDE.md prescrit — `python -m tools.…` lancé DEPUIS la racine — ne porte pas le nom du projet ; le `cwd` du processus EST le dépôt, le code ne le lisait jamais. C'est mot pour mot la garde d'E4 (« quand un instrument réel passe à travers, élargir l'heuristique dans la MÊME passe, sinon le compteur ment dans le sens rassurant ») restée sans application ici. (b) **« bails : 0 vivant(s), 1 mort(s) »** pour un bail dont `is_lease.is_holder_alive()` rend `True` : la fonction de vérité EXISTE, le chemin `--kill` l'appelle et REFUSE de tuer (la protection destructive était intacte) — mais le RAPPORT ne l'appelait pas : « MORT » était l'étiquette de tête, « détenteur vivant » un détail entre crochets. Forme (b) de CLAUDE.md appliquée à un outil d'exploitation : *l'instrument SAIT et ne le dit pas*. Conséquence mesurée : j'ai annoncé au propriétaire du run qu'il était interrompu et proposé de le rejouer ou de le déclarer mort — le run a fini 40 minutes plus tard avec son verdict scellé (`NI_L_UN_NI_L_AUTRE`, n=7). Une session voisine a lu le même écran sans réaper, par prudence, pas parce que l'écran le disait. **(9, 2026-09-23, P2.56 / porte 18 — l'instrument de PORTÉE lui-même)** : `check_calibration_reach` jugeait une déclaration garde-seule « déclarative » dès qu'un test IMPORTAIT et APPELAIT le symbole — et six l'étaient par leur SEUL test de garde, `with pytest.raises(ValueError): run_arm(**degenere)` : un refus à la garde compté comme une atteinte du corps (les quatre `s2_*::run_arm`, `run_curriculum`, `run_world_era`). Le cliquet livré en 4b68b121 annonçait donc 6 muettes de moins qu'il n'y en avait ; trouvé en re-déclarant les 38 « déclaratives » d'après leurs témoins (le témoin n'affirmait RIEN sur le corps). Corrigé le jour même : un appel sous `pytest.raises` n'atteint pas ; les six résorbées par injection (fournée 10) ; garde gelée `test_a_call_under_pytest_raises_does_NOT_reach_the_body_but_a_real_call_beside_it_does`. La leçon est celle du 2026-09-01 (« un outil de vérification non calibré PRODUIT un verdict ») : la première mesure d'un cliquet neuf se confronte à un cas dont on connaît la réponse — ici, un test de garde ne devait résorber personne. **(occ. 2026-09-24, la clause ancrée sur l'artefact d'ENTRÉE — 6 cas)** : `path_present=docs/preregistrations/<règle>.json` est VRAI dès le SCELLEMENT, avant tout run. Une entrée peut donc passer « ✅ CLOSE » avec le cliquet de fraîcheur au VERT alors que le record et l'évidence n'existent pas : la clause vérifie que la question a été POSÉE, jamais qu'elle a reçu une réponse. Vu en acte sur **P4.16** — l'arbre annonçait CLOSE et `records_total=325` pendant que le record `S2-CREDIT-ABLATION-2` et `results/s2_credit_ablation_2.json` étaient NON SUIVIS par git. Les 6 : `LOCK-001-PROXY-R1`, `S2-REWARD-ABLATION`, `S2-CREDIT-ABLATION`, `S2-CREDIT-ABLATION-2`, `S2-BASSIN-FRAGILITY`, `S5-G4-PHASE-A`. Aggravant mesuré : `_CLOSE_MARQUEURS` (`tools/check_backlog_freshness.py`) contient « ✅ » SEUL, donc « ✅ SCELLÉE ET LANCÉE » bascule l'entrée du côté CLOSE de la logique à deux sens — c'est pourquoi P4.16 ne déclenchait aucune violation malgré son état réel. | `exécutable` **(3, 2026-08-03, EVO-011)** | `assert_selection_nonempty` · pour le cliquet : motif `benchmark_\w+` ajouté à `_INSTRUMENT_PATTERNS`. **Règle** : quand un instrument réel passe à travers, élargir l'heuristique dans la MÊME passe — sinon le compteur ment dans le sens rassurant · contre-exemple GELÉ : `test_empty_selection_is_rejected` **Occ. doctor (2026-09-22)** : `classify_leases` rend désormais TROIS états — `live`, `expired_alive` (TTL dépassé, détenteur VIVANT : machine en veille ou run très long, **ne se réape pas**), `orphan` (détenteur disparu ou PID réattribué) ; `dead` reste leur UNION pour les appelants existants. Le rapport titre « N vivant(s), N expiré(s) à détenteur VIVANT, N orphelin(s) » et étiquette `EXPIRÉ … PAS un orphelin : ne pas réaper` ; `--kill` ne boucle plus que sur `orphan` (+ garde de course si le détenteur revit entre la lecture et le kill). `project_processes` accepte un processus dont le `cwd` est SOUS la racine du dépôt, en plus du marqueur de ligne de commande (worktrees compris). Contre-exemples : `tests/sandbox/test_doctor_visibility.py` — 6 cas, dont DEUX vrais processus lancés à la forme canonique (un mock testerait le mock) avec contrôle de SPÉCIFICITÉ apparié (même interpréteur, même commande, `cwd` HORS du dépôt -> non détecté), un bail expiré à détenteur vivant qui doit sortir en `expired_alive` et JAMAIS en `orphan`, son contrôle apparié (détenteur réellement tué -> `orphan`), et le texte du RAPPORT (la ligne de tête ne doit pas contenir « mort »). ⚠️ En passant, `test_jobs.py::test_classify_leases_sur_un_repertoire_VIDE_ne_classe_RIEN` gelait la FORME du dict (`== {"live": [], "dead": []}`) et non l'invariant : il a rougi sur un correctif qui ne touchait pas sa propriété (E25), il est reformulé « tout état présent est vide, et les trois états existent ». **Occ. 2026-09-24 (clause sur l'artefact d'ENTRÉE)** : inscrite en P4.20 avec les trois durcissements de la porte 4 (refus d'une clause `path_present` visant `docs/preregistrations/`, baseline gelée sur les 6 existantes ; séparation de « ✅ CLOSE » et « ✅ SCELLÉE/LANCÉE » dans `_CLOSE_MARQUEURS`), chacun avec contre-exemple et mutation. |
| **E5** | **Aliasing** — l'acte de MESURER mute le système mesuré (portée élargie le 2026-07-28 : pas seulement la mémoire, mais tout ÉTAT GLOBAL partagé) | `forward` renvoie une VUE de `H` : clamper les logits épinglait le neurone 88. **(2)** EVO-008 : `measure_decision_saliency` appelait `np.random.seed(...)`, donc RÉÉCRIVAIT le RNG global. Inoffensif APRÈS un run ; destructeur intercalé ENTRE deux ères — la sonde détourne l'évolution qu'elle est censée OBSERVER. La lignée du seed 0 n'a pas été rejouée, une autre a été fabriquée à chaque ère, et la courbe rendue était PLATE là où le lecteur est avéré (4 reproductions) **(2026-09-08, une variable d'ENVIRONNEMENT posee A L'IMPORT, qui decide QUEL SUJET est mesure)** — `tools/evo_memory_inworld.py:51` fait `os.environ.setdefault("HOF_PATH", <chemin vers un HoF VIDE>)` **au niveau module**. L'intention est legitime et ecrite (EVO-003 veut une tabula rasa), mais l'effet est PROCESSUS-GLOBAL : tout script qui importe ce module -- meme pour un helper sans rapport comme `_cfg` ou `N_APEX` -- voit ensuite `load_champion_genome()` echouer. **Chaine complete a trois maillons, mesuree** : (1) l'import repointe `HOF_PATH` ; (2) `load_hall_of_fame` avalait TOUTE exception (`except: pass`) et rendait `(1, [])`, donc un fichier ABSENT, un fichier CORROMPU et un HoF reellement vide devenaient INDISCERNABLES ; (3) `tools/arm_nas` transformait ce vide en **`(0, 0)`** -- une taille de genome moyenne MESUREE a zero. Symptome observe : une sonde annoncait « HoF vide : evoluer d'abord » alors que `data/hall_of_fame.pkl` contenait ses 10 entrees, intactes depuis juillet -- un diagnostic qui envoie chercher la cause a l'oppose de sa cause. **Corrige aux trois maillons** : la redirection CRIE desormais (et signale le cas ou elle arrive TROP TARD, `persistence` deja importe, ou elle est sans effet et le bras croit partir de zero en partant du champion) ; `load_hall_of_fame` distingue ABSENT (silencieux, cas nominal) de ILLISIBLE (leve, avec le chemin ET `HOF_PATH` dans le message) ; `arm_nas` refuse de mesurer sur du vide. **Regle qui en sort** : une variable d'environnement lue A L'IMPORT par un module et ECRITE A L'IMPORT par un autre fait dependre le resultat de l'ORDRE DES IMPORTS -- ce que personne ne relit. Si un module doit imposer un tel reglage, il le CRIE. **(occ. 2026-09-24, `GIT_INDEX_FILE` — la fuite qui rend le commit PATH-SCOPÉ contournable)** : git pose `GIT_INDEX_FILE` pour les hooks pendant TOUT `git commit`, et la variable FUIT dans la chaîne de sous-processus (hook → `check_gate_mutation` → pytest → un test → `git`). Un test qui manipule un dépôt git jetable hérite donc de l'index DU COMMIT EN COURS. Signalé par la session PM sous la forme « vert hors hook, rouge pendant le commit » ; la SÉVÉRITÉ, mesurée ici le 2026-09-24, est plus grave : (a) `git add` dans un dépôt jetable laisse l'index de CE dépôt VIDE et écrit 112 octets dans l'index étranger ; (b) en dépôt JOUET, un `git add` lancé depuis un `pre-commit` **ATTERRIT DANS LE COMMIT** — commit path-scopé sur `voulu.txt`, le commit contient `intrus.txt` (`git cat-file -e HEAD:intrus.txt` réussit). La discipline « commit path-scopé obligatoire », sur laquelle repose tout le protocole d'arbre PARTAGÉ (E10 occ. 17 et la règle de CLAUDE.md), est donc contournable PAR LE HOOK : un test peut injecter un chemin dans le commit d'une autre session sans que personne l'ait stagé. ⚠️ La LECTURE subit la même fuite que l'écriture — `git ls-files` sous la variable lit l'index étranger : la première version du contre-exemple ci-dessous affirmait le contraire et a rougi. Population mesurée : 7 fichiers de `tests/` lancent git en sous-processus, 4 sans isoler l'environnement. **(occ. 2026-09-24 — MÊME CLASSE, AUTRE VARIABLE : `GIT_DIR` ; et cette fois le MÉCANISME EST REPRODUIT)** : l'arbre PARTAGÉ s'est retrouvé avec `core.bare = true` dans `.git/config` (écrit à 13:30:13, pendant le harnais de mutation d'un commit), d'où `git status` / `add` / `reset` / `stash` en « fatal: this operation must be run in a work tree » pour toute session du checkout principal. **Reproduit sur DEUX DÉPÔTS JOUETS** (jamais sur celui-ci, assertion de sécurité sur le scratchpad) : un `git init` lancé depuis un cwd quelconque en HÉRITANT de `GIT_DIR` — que git exporte à tout hook, exactement comme `GIT_INDEX_FILE` — réinitialise le dépôt POINTÉ et, faute de `GIT_WORK_TREE`, le déclare **bare** : `core.bare` passe de `false` à `true`, le `git status` de la victime rend le message EXACT observé, et le tmpdir du test ne reçoit **aucun `.git`** — le test croit opérer sur un dépôt jetable et opère sur le VRAI. ⚠️ **Empreinte CHERCHABLE** : l'accident émet `warning: re-init: ignored --initial-branch=…`. Une sortie de test qui contient « re-init: ignored » EST l'accident en train de se produire, et une garde peut s'y accrocher à coût nul — elle attrape même le cas où la purge d'environnement est contournée par un `env=` explicite. ⚠️ **Et l'identité y passe aussi** : le `git config user.email test@example.com` qui suit dans les fixtures écrit dans le dépôt pointé (mesuré : `vrai@proprietaire.fr` → `test@example.com`) — c'est le mécanisme des 33 commits signés « Test » de P2.86. **CE QUE LA MESURE PERMET D'EXCLURE, et c'est son principal apport** : les trois sites non protégés trouvés au balayage (`tests/sandbox/test_pm_roles_counts.py:95`, `tests/sandbox/test_pm_snapshot.py:22`, `tests/sandbox/test_staged_authorship.py:53` — tous `subprocess.run(..., cwd=repo)` sans `env=`) écrivent TOUS une identité de test juste après leur `init` ; or l'identité du dépôt est intacte (`roblastar@live.fr`, valeur unique en portée LOCALE, `git config --local --get-all` ; ⚠️ `--get-all` sans `--local` rend DEUX lignes de même valeur, la locale et la globale — la valeur est unique, pas le nombre de lignes, et ma première formulation confondait les deux) et aucun commit encadrant 13:30:13 n'est mal signé. **Ces trois-là ne sont donc PAS le chemin emprunté ce jour-là**, et le site réel — un `init` sans écriture d'identité derrière — reste **NON IDENTIFIÉ**. La CLASSE est établie, le CAS ne l'est pas, et on ne comble pas l'écart par l'hypothèse commode. **Portée mesurée, contre ma première affirmation** : les worktrees LIÉS ne sont PAS touchés (`rev-parse --is-bare-repository` y rend `false`, `git status` y fonctionne) — `core.bare` ne gouverne que le dépôt PRINCIPAL ; j'avais conseillé « plomberie seulement » à une session qui travaille en worktree, c'était faux et rectifié. La PLOMBERIE reste intacte partout, ce qui explique que les commits par index temporaire aient traversé toute la panne sans rien remarquer : le protocole adopté pour ne pas emporter le travail des voisins se trouve être aussi le seul qui survive à ce dégât. ⚠️ **Et l'état a changé sous moi pendant que je le rapportais** : réparé à 13:42:31 par un tiers, alors que mes mesures datent de 13:32-13:42 — publiées, elles étaient déjà périmées. Sur arbre partagé le disque est un FLUX : un diagnostic d'état s'horodate ou s'ancre sur un sha, sinon il périme avant d'être lu ; seule la reproduction sur dépôts jouets, indépendante de cet état, a survécu. Dette **P2.107**. ⚠️ **CAS IDENTIFIÉ le soir même, à la VRAISEMBLANCE FORTE — sept concordances indépendantes, une réserve dite.** Le témoin qui a tiré est `tests/sandbox/test_record_graph_completeness.py` de la branche `chantier/pm-portes`, dans sa PREMIÈRE version, modifiée sur disque à 13:28:04 et jamais committée. Chaîne datée par les transcriptions de session : à **13:29:39** un commit est lancé dans le worktree `pm-portes`, pathspec comprenant ce fichier et les modules des portes ; son hook lance le témoin de la porte 1 sous `GIT_DIR=.git/worktrees/pm-portes` exporté par git ; à **13:30:13** `core.bare = true` est écrit ; à **13:31:37** le commit s'achève sur un REFUS — une mutation survit — donc AVORTÉ, sans entrée de reflog, ce qui explique que personne ne l'ait vu. Concordances : (1) le fichier est modifié une minute avant ; (2) il est témoin de la porte 1, donc lancé par ce hook ; (3) l'écriture tombe au milieu du hook ; (4) sa version CORRIGÉE, committée à 13:45:47 dans `c7b655a8`, décrit elle-même le défaut — la « première version » ne retirait que `GIT_INDEX_FILE`, or `git commit` fixe aussi `GIT_DIR` ; (5) il passe l'identité par « -c user.email= », qui n'écrit rien : d'où l'identité du dépôt restée intacte, la contrainte qui avait disculpé les trois premiers suspects ; (6) reproduit sur dépôts jouets : un `git init` sous le gitdir d'un WORKTREE, sans `extensions.worktreeConfig`, écrit dans la config COMMUNE ; (7) cette variante laisse les worktrees INTACTS pendant que le checkout principal casse — exactement ce qu'une session avait mesuré dans l'après-midi sans pouvoir l'expliquer. **Réserve** : le texte exact de la première version n'a jamais été committé ; c'est son auteur qui la décrit, dans le correctif. **Deux corrections** : le candidat précédent — le test du tableau de pilotage — est EXCLU par la chronologie, son commit ayant abouti à 13:29:33 ; et le correctif du transport sur la branche PM, `f6bb56a0`, qualifiait le défaut de « latent, non tiré aujourd'hui » : il a tiré, dans un commit avorté de la même branche. ⚠️ Et l'empreinte « re-init: ignored » a vraisemblablement été ÉMISE ici, ce témoin passant un nom de branche initial — imprimée dans la sortie d'un hook que personne n'a lue. Une empreinte n'est une garde que si quelqu'un regarde le canal. | `exécutable` | `assert_no_aliasing` + cas de calibration · pour l'état global : l'instrument SAUVEGARDE et RESTAURE `np.random.get_state()` (2 tests gelés — aucune trace laissée, ET valeur mesurée inchangée). ⚠️ **Ce qui a révélé le défaut n'est ni une relecture ni une intuition, mais un cas à RÉPONSE CONNUE placé dans le dispositif** (le seed 0 DEVAIT sortir lecteur). Règle : toute sonde intercalée dans une boucle stochastique doit être neutre pour le RNG, et tout traçage doit embarquer un témoin dont la réponse est connue · contre-exemple GELÉ : `test_aliasing_is_detected_on_a_real_view` ; et la LIMITE de la garde est elle aussi gelée par `test_alias_structural_guard_is_blind_to_functional_leak` — elle voit l'aliasing STRUCTUREL, pas une fuite fonctionnelle **Occ. 2026-09-24 (fuite `GIT_INDEX_FILE`)** : `tests/sandbox/test_jobs.py::_env_sans_git()` (retire `GIT_INDEX_FILE`, `GIT_DIR`, `GIT_WORK_TREE`, `GIT_OBJECT_DIRECTORY`, `GIT_ALTERNATE_OBJECT_DIRECTORIES`, `GIT_PREFIX`, `GIT_COMMON_DIR`) passé à TOUS les appels git du fichier — dont le `git worktree add` du contre-exemple de racine commune, qui écrivait sans elle dans l'index du commit. Contre-exemples APPARIÉS : `test_un_git_de_test_sous_GIT_INDEX_FILE_ecrit_dans_l_index_ETRANGER` (le défaut, gelé : index du dépôt VIDE, index étranger écrit) et `test_l_environnement_NETTOYE_renvoie_le_git_de_test_dans_son_propre_index` (la garde : le fichier atterrit dans son propre index, l'index étranger n'est JAMAIS créé) — le second rougit si `_env_sans_git` cesse de retirer la variable ou si un appel oublie `env=`. Vérifié : la suite entière passe 19/19 AVEC `GIT_INDEX_FILE` hérité, et l'index simulé garde ses 2015 entrées (= le compte de HEAD, non pollué). ⚠️ Helper LOCAL en attendant la fusion de `_env_isole()` (branche PM, `7d6c04c9`) : **un seul** helper partagé à terme. RESTE, chez le PM (P2.84) : le cliquet « aucun NOUVEL appel git en sous-processus dans `tests/` sans environnement nettoyé », baseline gelée sur les 3 fichiers restants (`test_pm_snapshot.py`, `test_preregistration_guard.py`, `test_staged_authorship.py`). |
| **E6** | **Prédicteur — ou POLITIQUE — mesuré hors du RÉGIME où il opère** (portée élargie le 2026-07-27 : pas seulement les prédicteurs d'une corrélation, mais toute politique validée dans une distribution d'état qui n'est pas celle du monde) | taux de grab mesuré sur trajectoire oracle vs comportement in-world (ρ 0.33 vs 0.53). **(2)** [[EDR-S2-011]] : un bassin BC atteint `acc 1.00` sur `_step(obs, H=0)` mais la cohorte warm-startée survit 8 — « le bassin ne transfère pas au forward RÉCURRENT ». Constaté, **jamais expliqué**. **(3)** EVO-005 : le MÉCANISME, mesuré — un lecteur câblé est parfait à H=0 et tombe à la CHANCE in-world, parce que `H` accumule (δ = sigmoid(diag(W)) ≈ 0.5 sur une soupe fraîche) et que l'activation ayant `f(0) ≠ 0`, même les sorties JAMAIS câblées dérivent à **+7.45 ± 9.8** après 25 ticks, noyant une marge de signal de ±2.5 **(2026-09-01, EVO-026)** — la classe s'applique aussi aux **CONTRÔLES**, pas seulement aux prédicteurs. Le pré-vol censé vérifier « l'horizon long délivre-t-il 21× plus de tirages ? » appliquait 16 905 mutations CUMULATIVES à un seul génome — un régime que le run ne visite **jamais** (`apply_mutations` CLONE, donc une lignée n'accumule qu'~1 mutation par ère). Non représentatif ET pathologiquement lent (N filait vers ~3 200, `np.insert` en O(N²)). **Correctif** : le contrôle de manipulation ne passe plus par une SONDE mais par des compteurs **in situ** lus sur le run lui-même, qui conditionnent la lisibilité du verdict. **Règle** : un contrôle de manipulation doit s'exécuter dans le régime du dispositif — sinon il contrôle un proxy, pas l'expérience. | `exécutable` | `assert_predictor_measured_in_situ` · contre-exemple GELÉ : `test_synthetic_reader_needs_the_reflex_diagonal_state_drift_counterexample`. **Conséquence de design** : un lecteur RÉACTIF exige une CONJONCTION de deux mutations — câbler le canal ET dé-mémoriser la sortie. ⚠️ **PORTÉE CORRIGÉE (2026-08-04, mesure)** : vrai pour une décision par `argmax` (que la dérive arbitre entièrement), seulement PARTIEL pour un seuil de SIGNE — un lecteur `throw` SANS réflexe atteint 0.305 d'erreur contre 0.500 à la chance, et gagne +6.5 ans. La règle avait été surétendue depuis une seule sous-tâche |
| **E7** | **Mauvaise unité de réplication** (pseudo-réplication) | `sign_p = 1.5e-05` sur 16 agents partageant entraînement, optimiseur et mondes → n réel = 2 | `documenté` | `declare_design(replication_unit=…)` — la déclaration rend le conflit visible, elle ne le détecte pas |
| **E8** | **Inférence substituée à la mesure** pour économiser du calcul | WARM-008 : maillon final inféré (~7 h économisées) ; mesuré **NUL** par la revue en 28 min. **WARM-002** : seuil « ~99 % d'accuracy » importé de WARM-001 (`cf. WARM-001`) — autre grandeur, autre population ; mesuré FAUX (récompense dès p=0.25). Le contrôle qui manquait coûtait **6 secondes** **(2026-09-08, dans la FORMULATION d'une regle SCELLEE)** — `S2-BLIND-CHAMPION` decrit son intervention comme « les lignes d'entree de W mises a zero : **l'observation n'entre plus dans le calcul** ». La seconde moitie est une INFERENCE tiree de la premiere, et elle est FAUSSE : `MambaBatchModel.forward` fait `H[:, :max_I] = x` — l'observation est ecrite DIRECTEMENT dans l'etat cache, avant toute multiplication par `W` — -- et, **rectifie le jour meme apres MESURE**, ce n'est meme pas la bonne cause : `world_model` vaut `None` dans ce regime, le routeur et le NTM sont innocentes, et le mecanisme reel est un CHEVAUCHEMENT des blocs entree/sortie du genome (classe **E24**) qui rend 18 logits d'action IDENTIQUES a l'observation. J'avais donc infere DEUX fois : une premiere dans le sceau, une seconde en expliquant l'echec du sceau. Seule la sonde a reponse connue -- avec son no-op a 0,0 exact -- a tranche. **Ce n'est pas une mesure qui a rate, c'est une phrase qui n'a jamais ete mesuree** — et elle etait SCELLEE, donc incorrigible : reprise sous `-bis`. Detecte par un residu de **1,2 %** la ou un sujet reellement aveugle devait rendre 1,0000 exact ; sans ce chiffre attendu, l'ecart serait passe pour du bruit. **Regle qui en sort** : dans une pre-inscription, une clause qui DECRIT l'intervention doit etre verifiable par une assertion, au meme titre qu'un seuil — sinon c'est une hypothese deguisee en design. Cf. [[EDR-S2-BLIND-CHAMPION]]. **(4, 2026-09-09, dans la PRÉMISSE d'un record, et la garde armée ne pouvait pas la voir)** — `EDR-GRAB-COST` s'ouvrait sur : « le régime de famine dure porte `forage_payoff = 3.0` — ramasser un fruit RAPPORTE ». **Deux fautes dans une phrase, et aucune n'est une mesure ratée.** (a) La VALEUR : `run_condition` construit le monde avec `config=None`, donc le DÉFAUT — mesuré, `forage_payoff = 1.0`. (b) Le RÉFÉRENT, et c'est le plus grave car il est structurel : `forage_payoff` ne multiplie que `prey_reward` sur une **mise à mort de proie** (`world_1_stoneage.py:800`) ; il ne touche **jamais** `do_grab`. Même à 3.0 il n'aurait rien dit sur l'action ablatée. Le paradoxe qui donnait son titre au record (« coûte alors que ça NOURRIT ») était donc adossé à un paramètre hors sujet — et la mesure du mécanisme l'a **retourné** : sur **20 776 agent-ticks**, le grab nourrit **exactement 0 fois** par le chemin du moteur (+20) ET **0 fois** par le cache de famine ; le champion ne porte **aucun** aliment (4 549 `rock`, 1 166 `Spark`, 663 `stick`, 513 `Spear`… **zéro `Fruit`**). ⚠️ **Ce que l'occurrence apprend sur la GARDE, et qui est le vrai enseignement** : `declare_design(links={…: "inferred"})` couvre les liens CAUSAUX inférés — pas la description du RÉGIME. Un record peut donc affirmer une valeur de configuration qui n'a jamais eu cours, et **rien dans le dépôt ne le dit**. Balayage de la FORME (leçon d'E23 occ. 3 : chercher l'idiome, pas l'instance) : **56 affirmations de paramètre** dans la prose des records, sur 8 paramètres et 24 fichiers ; une seule fautive, mais aucune n'était vérifiable. Remède posé : les runners publient un bloc `regime` dans leur JSON de résultats (`results/p41_grab_mechanism.json`), ce qui rend la confrontation prose↔run mécanisable. **(occ. 5, 2026-09-14, même forme que l'occ. 4 — un TERME de récompense cité comme vivant)** : [[EDR-S2-CREDIT-RETENTION]] cite « Δénergie + curiosité + nouveauté » depuis la LIGNE de code, pas depuis une mesure ; le terme curiosité est MORT sous torch (E16 occ. 2). Le bloc `regime` ne suffit pas : il publie des PARAMÈTRES (les échelles 2,0 et 3,0 y sont), pas le fait que le facteur qu'ils multiplient soit non nul. Remède posé : `preflight_reward_seam` publie la DÉCOMPOSITION mesurée de la récompense passée au learner (part intrinsèque au tick 1) — une prémisse sur une récompense se mesure sur la récompense, pas sur sa formule. **(occ. 6, 2026-09-15, P2.60 première passe — une DIVERGENCE déclarée non fusionnable sans avoir lancé le merge-tree)** : un agent a gravé dans EDR-177/178 « les fichiers ont divergé, le diff est rapporté au backlog, pas appliqué » sans lancer le `git merge-tree --write-tree` que l'entrée de backlog prescrivait ; la mesure (arbre 39aa160, ZÉRO conflit, +129/−28 côté HEAD fusionnables) donnait le contraire. Attrapée par le vérificateur adversarial, corrigée en seconde passe (fusion 3-voies, gardes de HEAD conservées, note réécrite sur les chiffres). Règle d'opérateur, `documenté` : on ne conclut pas à une divergence non fusionnable sans avoir lancé la fusion à blanc. **(occ. 2026-09-24, P4.17 — LA PRÉMISSE DU MODÈLE DE COÛT, DANS UN SCEAU ; trouvée par la session propriétaire sur sa PROPRE règle et rapportée)** : la règle scellée de P4.17 justifie sa projection de coût par « cellules uniformes : mêmes épisodes, mêmes agents — la queue est la contention machine, pas le seed ». Mesuré après coup : **FAUX** — `td0_d0` tourne en **56 s** de médiane contre **160 s** pour les bras à trace, soit **2,8×**. Or l'unité est prise sur la PREMIÈRE cellule neuve, qui appartient à la famille LENTE, puis appliquée à une grille HÉTÉROGÈNE ; et cette projection a décidé une COUPE (la ligne lr 1,0). La prémisse qui autorise la projection était donc inférée, pas mesurée. ⚠️ **Axe NOUVEAU** par rapport à la règle déjà écrite dans CLAUDE.md (« une unité de coût mesurée SOUS CHARGE n'est pas une unité ») : ici la charge n'est pas en cause, c'est l'**HÉTÉROGÉNÉITÉ DES CELLULES** — il faut une unité PAR BRAS, pas une unité par grille. Et le sceau n'y peut rien : il garantit que la prémisse n'a pas bougé, jamais qu'elle a été MESURÉE — même mécanisme qu'**E2** occ. 3, où la chaîne causale était coupée DANS la règle scellée elle-même. Contexte chiffré, qui montre l'enjeu : la contention a coûté EXACTEMENT une ligne de grille (255 min contre 231 après la coupe, pour un budget de 240) — la fragilité était dans la MARGE entre projection et budget, pas dans la mesure du coût. Dettes inscrites par sa propriétaire en P4.17 : unité PAR BRAS dans `project_cost`, et vocabulaire FERMÉ pour `_regime.coupe` (contention / structure / budget — c'est cette confusion-là qui a produit ma sur-déclaration de P4.11, cf. **E2** occ. 6). **(occ. 2026-09-24 — L'ATTRIBUTION, puis le RÔLE INVENTÉ ; deux fois dans la même heure, par moi, et la seconde est gravée DANS CE FICHIER)** : (a) le commit d'une session voisine emportait un fichier sur lequel une troisième session avait du travail non fini. J'ai identifié l'auteur en **INFÉRANT depuis le CONTENU** de son pathspec (« ce lot-là, c'est le lot Rôles/Réfutateur, donc c'est telle session ») alors que la MESURE était sous mes yeux : le chemin passé au `-F` d'un `git commit` contient l'UUID de session de son auteur, et je l'avais LU — je m'étais contenté de le comparer au MIEN pour me disculper, sans m'en servir pour désigner. L'inférence était FAUSSE ; la session accusée a produit son propre UUID et ses trois derniers pathspecs pour la réfuter ; l'attribution avait déjà circulé à TROIS sessions avant d'être retirée. Une identité se MESURE, elle ne se déduit pas d'un faisceau thématique — et disculper par la mesure puis accuser par l'inférence est la forme la plus tentante de la classe, parce que la première moitié est irréprochable. (b) **Le rôle inventé.** J'ai écrit « demandé par le master » dans CE registre, « accord du master » dans trois entrées du backlog et dans ADR-005. Or `docs/roadmap/ROLES.md` (writer unique : la session PM) porte quatre rôles instanciés — **Intégrateur, PM, Réfutateur, Stratège** — et **aucun « master »** ; la flotte a tranché explicitement « PAS de master : bulletin par session, un writer par fichier ». J'ai donc gravé une **AUTORITÉ INEXISTANTE** comme source de l'accord d'une clôture, **cinq fois**, et l'unique occurrence du mot dans ce registre était la mienne. C'est la ligne creuse que la porte 21 existe pour refuser, commise dans le fichier qui sert de mémoire aux fautes, par la session qui le tient. Trouvé parce que la session que j'appelais ainsi a REFUSÉ le titre et cité le registre des rôles plutôt que de l'accepter. Les cinq attributions sont corrigées dans cette passe : elles nomment désormais le FAIT — une session a revu et posé des réserves, appliquées telles quelles — et non un rang. ⚠️ **Ce qui rend l'occurrence instructive** : l'agrément était RÉEL, la revue a vraiment eu lieu, seule sa QUALIFICATION était fabriquée. Un fait vrai décrit par une autorité fausse reste une affirmation fausse, et elle se propage mieux qu'une erreur de mesure — personne ne vérifie un rang, alors que tout le monde vérifierait un chiffre. | `exécutable` **(promu le 2026-09-02 — 2 occurrences en `documenté`, la 3ᵉ est interdite)** | `declare_design(links={…: "inferred"})` **LÈVE** `PreflightError` sauf `allow_inferred_reason=` écrite (publiée dans le design) · contre-exemple GELÉ : `test_inferred_link_without_reason_is_REFUSED` (la configuration exacte de WARM-008) **(5, 2026-09-15, dans la PRÉMISSE d'un bras de RÉFÉRENCE — attrapée par le compteur, pas par moi)** — P3.4 : le bras `lr=0` de l'apprenant legacy (`MambaBatchModel.compute_policy_gradient`) était décrit comme « le même code à pas nul, W figé ». Le compteur de dose a rendu **283 mises à jour sur 2000 ticks** à `lr=0` (299/300 hors monde) : ce n'est pas le clip, c'est le **compilateur d'auto-câblage NTM** (`NTMProgramCompiler.compile_and_apply`, appelé dans `forward`) qui écrit `W_batch` depuis les slots mémoire, persisté dans `genome.W` par l'appel de gradient — `ABLATE_NTM=True` → 0/300. Sur le chemin de l'arc EVO, **W a deux auteurs**, et seul le second survit à un pas nul. La prémisse « lr=0 ⇒ W figé » est vraie du backend torch (mesurée, P1.6) et FAUSSE du legacy : importée d'un substrat à l'autre sans mesure — la forme exacte d'E19 appliquée à une prémisse. Le plafond de l'incapable se lit sur la DV (0,123 = chance), jamais sur la supposition que W est figé. Garde : `test_legacy_lr_zero_is_the_same_code_at_null_step_and_lr_is_restored` exige W bit-identique hors NTM ; la docstring de `tools/learning_events.py` nomme les deux auteurs ; `legacy_updates` compte « W a bougé », quelle qu'en soit la cause. |
| **E9** | **Généralisation depuis un échantillon saillant** | `agents[0]` (×2), « 3 cas » érigés en propriété d'instrument, une population → toutes. **(2026-07-27/28, EVO-006 — l'occurrence la plus coûteuse à ce jour, et c'est un record ENTIER)** : **1 seed sur 5** élevé en mécanisme (« le crédit partiel est le gradient manquant »), en prescription opposable (« un objectif cognitif doit être DÉCOMPOSABLE ») et en convergence théorique avec [[EDR-S2-010]]. Le contre-test à difficulté APPARIÉE ([[EDR-EVO-007]], n=12) rend **0/12 AVEC crédit partiel, 0/12 SANS, 0/12 au contrôle** ; la réplication directe rend **1/12 vs 0/11 au contrôle, Fisher p = 1.000**. Verdict RETIRÉ, record rétrogradé en observation isolée. ⚠️ Le fait mesuré, lui, TIENT (lecteur authentique, bascule 1.000, reproduit 4×) — ce qui est exactement ce qui rend la classe dangereuse : **un résultat rare et RÉEL + une explication plausible = une histoire indistinguable d'une découverte** **(2026-08-04, EVO-021 — commise DANS UNE RÉFUTATION)** : une revue alléguait que `add_node` décale le bloc d'entrée. J'ai mesuré **0/3000** sur une SOUPE FRAÎCHE et conclu « ne se produit jamais en pratique ». Or sur une soupe fraîche les arêtes vont d'une entrée vers une sortie (`j ≥ 64` toujours) — le régime masquait le défaut. Sur un génome à AUTO-BOUCLES, `j` est uniforme sur tous les nœuds et le bloc d'entrée devient atteignable. La revue avait raison ; mon contre-test portait sur le mauvais régime. | **`non automatisable`** | **Revue adversariale obligatoire** — c'est la classe qui justifie le dispositif, et elle a FONCTIONNÉ : le contre-test a défait le record en quelques heures. Renfort exécutable partiel depuis 2026-07-27 : **pré-inscrire le critère** (`tools/preregister.py`, classe E11) — sans le sceau, ces mêmes chiffres laissaient toute latitude d'expliquer que « le contre-test ne teste pas la même chose » · **Règle ajoutée (2026-08-04)** : une propriété de l'OPÉRATEUR DE MUTATION doit être mesurée sur PLUSIEURS régimes de génome — au minimum soupe fraîche ET génome à auto-boucles. Gelée dans `tests/sandbox/test_mutation_index_invariants.py::test_a_fresh_soup_genome_hides_the_defect`, qui montre le faux négatif et sa cause. |
| **E10** | **Règle documentée sans application exécutable** | contention KuzuDB (moi, ×2) ; `memory_retriever` actif pendant la sim (code d'instrument) ; état global non nettoyé (suite de tests) ; **(4, 2026-07-27, SP-2)** commit `0dba679` (Task 2, dispatch interrompu) — un `git add tests/sandbox/test_instrument_calibration.py` path-NOMMÉ mais pas contenu-SCOPÉ a capturé ~146 lignes du travail EVO-005/003 **non committé** d'une session PARALLÈLE sur ce fichier PARTAGÉ ; rendait aussi `0dba679` isolément non reproductible (import d'un module alors non committé). Réparé de facto au tip de branche par le commit de suivi `c843e37` de la session parallèle ; irréparable maintenant sans réécrire l'historique partagé (proscrit par la règle no-amend) **(5, 2026-07-28, EVO-010)** — 2ᵉ run perdu de la MÊME journée faute d'une garde écrite par moi-même le matin : run lancé **sans pré-vol** (règle scellée, dimensionnement calculé, mais la question « le bras traité est-il seulement VIABLE ? » jamais posée) -> crash au 15ᵉ seed (`surprise` → NaN sous densification). Le pré-vol fait APRÈS montrait la densité passer de 0.3 % à 5.0 %. Le 1ᵉʳ run de la journée était mort faute de plafond de coût, alors que `cost_guard.py` venait d'être livré. **Une garde disponible et NON DÉCLENCHÉE vaut zéro** — le déficit n'est pas l'outillage mais le réflexe de l'appliquer à SA PROPRE expérience au moment de la lancer. **(6, 2026-09-01, arc taxonomie/bilinéaire — le rituel du REGISTRE lui-même non appliqué)** : à l'ouverture d'E19, un `grep -i` de `bilinear`, `mem-perception`, `retain-compose` et `lang-memory` sur ce fichier rend **0 ligne**. Aucune des quatre passes de l'arc (LANG-MEMORY, MEM-PERCEPTION, BILINEAR, RETAIN-COMPOSE) n'avait jamais atterri au registre, alors que deux d'entre elles portaient déjà l'occurrence (1) et (2) d'E19 — et que la règle de promotion (« deux occurrences `documenté` → promotion ») ne peut fonctionner que sur une classe DÉJÀ INSCRITE. Le registre ne protège que des passes qui l'alimentent. **(7, 2026-09-01, commise par moi pendant EVO-026-bis)** — le bail protège les RUNS les uns des autres, mais **pas la SUITE DE TESTS d'un run en vol**. J'ai lancé la suite de calibration complète pendant qu'EVO-026-bis détenait `kuzu` : 4 tests simulant un monde ont échoué (`test_perception_ablation_*`, `test_linear_sanity_*`). La règle existe dans CLAUDE.md « toute simulation de monde doit tenir la ressource `kuzu` » — mais **aucun test ne prend le bail**, donc rien ne m'a arrêté : règle documentée sans application exécutable, la définition de la classe. **Garde à écrire** : une fixture pytest qui SAUTE (ou refuse) les tests simulant un monde quand le bail `kuzu` est détenu par un autre processus vivant — l'information est déjà disponible via `tools.jobs.doctor`. **(8, 2026-09-01, PROMOTION P2.22 — sens A, cas GELÉ de la calibration)** : commit `e21c1f3` — un `git add tests/sandbox/test_instrument_calibration.py` path-scopé mais pas contenu-scopé a happé ~159 lignes de travail NON committé d'une session PARALLÈLE (alors en cours sur P2.19/P2.20) sur ce MÊME fichier à forte contention — 3ᵉ récidive du MÊME mécanisme sur le MÊME fichier (cf. occ. 4, `0dba679`). **(9, 2026-09-01 — sens INVERSE)** : en sens inverse, des sessions parallèles ont committé le contenu d'un implémenteur AVANT lui (`37bb389`, `8207b46`), le laissant sans commit propre à rapporter — sous-forme jamais inscrite jusqu'ici. Les occurrences 8 et 9, toutes deux `documenté` au moment des faits, DÉCLENCHENT la règle de promotion (§Rituel : « deux occurrences documenté → promue ou reclassée ») : E10 passe à `exécutable` pour le sens A (garde ci-contre). **(10, 2026-09-02 — la garde E10 déclarait insoluble une MOITIÉ DE SA PROPRE CLASSE)** : le rapport de livraison écartait le sens B (« quelqu'un committe mon travail avant moi ») comme « sans garde exécutable, **par construction** : rien à comparer dans un stage » — alors que le cas s'était produit **deux fois le jour même**. La prémisse était juste, la conclusion NON : l'empreinte fournit un second point de comparaison **hors du stage**, et la conjonction (arbre ≠ empreinte) ∧ (`git diff HEAD` vide) suffit à conclure que le contenu est déjà committé ; le commit porteur est nommé par recherche de CONTENU. ⚠️ Leçon transverse : **une impossibilité « par construction » est une affirmation MESURABLE** — elle exige une démonstration mécanique, pas une déclaration. C'est le §D du pré-vol (raisonner au lieu de mesurer) appliqué aux GARDES elles-mêmes. **(11, 2026-09-02 — `git add` et `git commit` ne sont PAS atomiques sur un index partagé)** : une session parallèle a committé un chemin **entre** le `git add` et le `git commit` d'une autre — le fichier a disparu du commit résultant **sans erreur ni avertissement**, absent du seul diff-stat. Le rituel « inspecter `git diff --cached` avant de committer » est donc **nécessaire mais PAS SUFFISANT** : la fenêtre de course est **APRÈS** l'inspection. ⚠️ Leçon transverse : **toute vérification pré-action sur une ressource partagée doit être doublée d'une vérification post-action.** **(12, 2026-09-02 — la garde elle-même prise en défaut, EN USAGE, une heure après sa livraison)** : `check_staged_authorship verify` a rendu **OK** sur ce même fichier partagé alors que **2 des 5 hunks stagés** appartenaient à une session parallèle. Cause structurelle, pas un bug : l'outil compare le STAGE à une empreinte prise AU DÉMARRAGE, donc le travail d'autrui écrit **APRÈS** le `snapshot` lui échappe **par construction**. Détecté à la main (`git diff --cached -U0 \| grep '^@@'` : deux hunks à 3300 lignes des zones d'édition), corrigé par découpe du patch et `git apply --cached` des seuls hunks propres — le travail d'autrui est resté intact et non committé. **Correctif à coût NUL, à appliquer** : rafraîchir l'empreinte **juste avant le `git add`** puis comparer les deux — tout hunk apparu entre `snapshot` et `add`, absent de HEAD, est du travail d'autrui par construction. ⚠️ Leçon : **une garde livrée n'est pas une garde éprouvée** — celle-ci a trouvé sa propre limite au premier usage réel, ce qu'aucune relecture n'avait vu. **(13, 2026-09-02 — DEUXIÈME défaut de la même garde, en usage, le même jour)** : sur un fichier déjà `M` à la prise en main, `verify` a de nouveau rendu OK alors que 55 lignes d'une session parallèle étaient stagées. Mécanisme RAPPORTÉ (⚠️ non re-vérifié par le contrôleur) : le bloc étranger était **ADJACENT** au sien, en fin de fichier, et `git diff` les fusionnait en **un seul hunk**. ⚠️ **DIAGNOSTIQUÉ le 2026-09-02 : ni l'un ni l'autre.** Le correctif de fusion n'est PAS incomplet, et l'adjacence n'est PAS la cause — le cas contigu pur est couvert, et le rejeu des artefacts réels de l'occurrence détecte bien ses 55 lignes. La vraie cause est la **COMPÉTITION D'ANCRE** : la seconde passe reste un alignement GLOBAL unique, `SequenceMatcher.get_opcodes()` n'attribue chaque ligne qu'à UN rôle et ancre sur le PLUS LONG appariement ; un run de lignes recopié de HEAD dans le travail PROPRE (motif banal : écrire un test en partant d'un test existant) **VOLE l'ancre** au bloc étranger plus court, qui retombe hors des opcodes `equal` et devient INVISIBLE. Seuil mesuré sur le fichier réel : un bloc étranger de ≤ 41 lignes disparaît face à un run copié de 40 lignes, et réapparaît à 60. Corrigé ; contre-exemple gelé `test_FORME_occ13_a_long_run_copied_from_HEAD_must_not_STEAL_the_anchor` + son positif apparié `test_POSITIVE_occ13_my_copy_of_HEAD_alone_is_NOT_flagged`. **Causalité VÉRIFIÉE** : garde remise à sa version HEAD, le contre-exemple rend `DID NOT RAISE`. Leçon : un soupçon de « correctif incomplet » est une HYPOTHÈSE, pas un diagnostic — ici il désignait la mauvaise cause, et seule la reproduction l'a montré. À ne pas confondre avec la limite « travail écrit après le snapshot » (occ. 12), qui est structurelle. Attrapé à la main par `git diff --cached -U0 \| grep '^@@'` ; réparé en reconstruisant le blob « HEAD + mes seuls hunks » (`git hash-object -w` + `git update-index --cacheinfo`) — le travail d'autrui est resté intact et non stagé. ⚠️ Leçon opératoire, valable dès maintenant : **sur un fichier DÉJÀ modifié à la prise en main, `snapshot`+`verify` ne suffisent pas — identifier les hunks préexistants AVANT d'éditer.** **(14, 2026-09-02 — sens B, et la garde EXISTAIT DÉJÀ)** : mes entrées de backlog (P2.23-P2.25) ont été emportées dans `HEAD` par un commit de l'arc EVO-028 avant que je les commite. `detect_preempted()`, promue le matin même pour EXACTEMENT ce cas, ne s'est pas déclenchée — non qu'elle ait échoué, mais parce qu'elle n'a **jamais été INVOQUÉE** : aucune empreinte n'avait été prise avant édition. Le manque n'est donc PAS la détection, c'est l'**INVOCATION**, et elle reste suspendue à la discipline de l'opérateur. ⚠️ **Et le balayage automatique, qui semblait être la promotion évidente, est RÉFUTÉ PAR LA MESURE** : lancé sur les 8 empreintes RÉELLES présentes dans `runs/staged_authorship/`, `detect_preempted` rend **2 préemptions sur 2 qui sont FAUSSES** — les deux désignent `7de1b54`, le PROPRE commit de l'auteur, faute d'un `confirm_commit(..., owner=)` pour l'inscrire. Câbler ce balayage au hook pre-commit aurait livré une garde qui CRIE À TORT à chaque commit, c'est-à-dire une garde qu'on apprend à ignorer. **(15, 2026-09-02 — sens A, commise PAR MOI dans le commit `297f887` qui consignait l'occurrence 14)** : ce commit a emporté 17 lignes d'un « Brainstorm taxonomies » d'une session parallèle (backlog, l.273-289) et sa ligne **E8** du registre. ⚠️ **Le mécanisme n'est pas l'ignorance de la règle — la règle était l'objet même du commit — c'est que l'INSPECTION ET LE COMMIT étaient dans la MÊME invocation de shell** : `git add … ; git diff --cached -U0 … ; git commit …`. La liste des hunks s'imprime, puis le commit part QUOI QU'IL ARRIVE. Une inspection qui ne peut pas INTERROMPRE n'est pas une inspection : c'est un journal. C'est E10 à l'état pur — une vérification réelle, exécutée, et sans effet parce que rien ne branche son résultat sur une décision. **Règle opératoire qui en découle : sur un fichier partagé, l'étape qui inspecte et l'étape qui commite doivent être SÉPARÉES, ou chaînées sur un code de retour** (`verify` sort non-zéro ; `Select-String` ne sort jamais non-zéro). Non défait : le travail d'autrui est DANS `HEAD`, un revert le supprimerait alors que la session parallèle le croit committé — le dommage est d'ATTRIBUTION, pas de perte. **(16, 2026-09-02 — sens B, et cette fois LA GARDE L'A ATTRAPÉE)** : le commit `481117e` d'une session parallèle a emporté mon travail sur `tests/sandbox/test_experiment_preflight.py` (empreinte `bar-reachable`). `detect_preempted` l'a signalé **automatiquement et PAR CONTENU** (`matched_by_content: true`), sans que rien ne lui soit nommé d'autre que l'empreinte. C'est le contrôle POSITIF apparié de l'occurrence 14 : la garde a bien la sensibilité qu'elle annonce. **(occ. 20, 2026-09-14, subie)** : le commit NU d'une session parallele (97153d7, 51 fichiers) a emporte les hunks NON RELUS de cette session dans QUATRE fichiers partages (backlog, CLAUDE.md, registre, test_instrument_calibration.py) -- dont 13 tests et des declarations CALIBRATED dont l'IMPLEMENTATION n'etait pas committee : HEAD a reference `run_learner_probe` sans le module pendant le temps du commit suivant. La regle « git commit -- <chemins>, JAMAIS nu » est documentee ici depuis l'occ. 19 et n'a AUCUNE application executable ; la porte 7 du hook ne fait qu'AVERTIR celui qui est preempte, pas refuser celui qui preempte. Candidat de garde : refuser tout commit sans pathspec quand l'arbre porte des empreintes d'un AUTRE owner (`check_staged_authorship`). **(occ. 21, 2026-09-15, trouvée EN PASSANT par l'agent P3.6 — un artefact « persisté » que git IGNORE)** : CLAUDE.md § Coût des runs prescrit « persister les génomes entraînés » ; le bassin DAgger de WARM-003 l'EST (`results/warm003_dagger_genome.npz`, 119 114 octets) mais sous le motif `results/*` de `.gitignore` — absent de tout clone, alors que `s2_credit_retention.py`, `s2_reward_ablation.py` et `s2_credit_ablation.py` le chargent (`BASSIN_PATH`) et que trois records (S2-011 bandeau, S2-CREDIT-RETENTION, S2-REWARD-ABLATION) le citent comme « le bassin persisté » : P4.4 / P4.8 reposaient sur une prémisse non reproductible hors de cette machine, même forme que la CI rouge du 2026-09-07 (`5b0025e`, un .pkl ignoré), que `check_backlog_freshness._tracked_by_git` ne couvre que pour les CLAUSES du backlog, jamais pour un chemin chargé par un runner. Correctif immédiat : `!results/warm003_dagger_genome.npz` et fichier suivi. Garde candidate `exécutable`, non écrite dans cette passe (seconde occurrence en `documenté` = promotion obligatoire) : tout chemin `results/*.npz|*.pkl` codé dans `tools/evo_runs/*.py` ou cité par un record doit être suivi par git, contre-exemple gelé (un runner chargeant un chemin ignoré doit ROUGIR). **(17, 2026-09-22, la porte 4 lisait DEUX états du même dépôt)** : la règle « un chemin cité doit être SUIVI par git » était DOCUMENTÉE dans le message même de la porte (sa clause `path_present` cite la CI rouge du 2026-09-07, `5b0025e`) et exécutée pour les seules clauses `closes_when` ; les citations en PROSE testaient `os.path.exists` sur le disque. Mesuré par la revue finale du plan PM (chantier/pm-roles) : le backlog COMMITTÉ à `3e2a9f83` citait `docs/superpowers/plans/2026-09-16-harness-r1-weeks-1-3.md` et `results/s2_credit_ablation.json`, présents seulement sur le disque de la session qui les avait créés → porte 4 VERTE sur l'arbre partagé, ROUGE dans un worktree neuf (témoins rouges, `test_LES_TREIZE_PORTES` rouge) — et dans tout clone frais / CI ; mon `72ce45a` en a ajouté trois autres sans que rien ne crie. Second défaut, trouvé en corrigeant le premier : la porte lisait le TEXTE du backlog et les cibles des clauses `grep_*` SUR DISQUE et les jugeait contre l'index TEMPORAIRE du commit — HEAD + les seuls chemins du commit (mesuré : un fichier stagé par une AUTRE session y rend 0 occurrence à `git ls-files`). Le travail EN VOL de B rougissait donc le commit de A (chemins « non suivis », clause de P2.66 « satisfaite » par `def claude_code_llm_fn` écrit sur disque par une autre session), puis la porte 15 refusait tout commit touchant une porte (« témoins rouges sans mutation ») : TROIS sessions bloquées coup sur coup le même soir. Les deux entrées sont gelées dans la baseline de chantier/pm-roles avec raison et condition de retrait (les deux fichiers sont suivis depuis `8a64b0d7` et `1804c15f`). | `exécutable` (occ. 1-3 : `tools/jobs/` ; occ. 7 : garde de bail `tests/conftest.py` ; occ. 8 : `tools/check_staged_authorship.py`, sens A ; **occ. 9-11, PROMUES le 2026-09-02** : sens B par `detect_preempted()` — avertissement non bloquant, `--strict` pour bloquer — et course add/commit par `confirm_commit(sha, paths)`) · **`non automatisable`** (occ. 4, reclassée le 2026-09-02 pour sa forme RÉTROSPECTIVE — reconstruire l'auteur d'un hunk depuis l'historique SEUL, après coup, exige de comprendre une INTENTION, même limite déclarée que E9/E14 ; **occ. 8 ne contredit pas ce constat** : sa forme est PROSPECTIVE — une empreinte prise AVANT l'édition ne devine rien, elle COMPARE) | `tools/sim_session.py` (occ. 1-3, verrou de processus + isolation garantie). **Occ. 4 — aucune garde exécutable ajoutée cette passe** : détecter mécaniquement « ce hunk appartient à une AUTRE session non committée » exige de comprendre une INTENTION, pas de la matcher — même limite que E9/E14. Gardes PARTIELLES déjà en place : la discipline `git add <path>` explicite + contingence stash-avant-commit (documentées dans `CLAUDE.md` §Environnement et dans le plan SP-2), et le cliquet `check_instrument_calibration.py` qui a **SURFACÉ** la fuite (import d'un module alors non committé) sans la PRÉVENIR. Occurrence unique de cette forme à ce jour — pas de promotion requise (règle : 2 occurrences `documenté` avant reclassement) · ⚠️ `tools/sim_session.py` est DÉPRÉCIÉ au profit de `tools/jobs/` (bail sur ressource NOMMÉE) — la garde vivante est là ; contre-exemple GELÉ : `test_named_resource_is_exclusive` et `test_doctor_never_kills_a_live_holder` dans `tests/sandbox/test_jobs.py`. **Occ. 8 (sens A, PROMOTION P2.22)** : `tools/check_staged_authorship.py` (`snapshot()`/`verify()`) remplace l'inspection visuelle par une comparaison MÉCANIQUE — une empreinte prise au démarrage de la tâche (contenu de l'arbre de travail + blob HEAD, au MÊME instant) permet, au moment du commit, de distinguer un hunk stagé ÉCRIT par la tâche courante (absent du snapshot, par construction) d'un hunk ÉTRANGER (déjà présent dans le snapshot, absent du HEAD capturé au même instant — donc du travail non committé d'autrui). `verify()` lève `ForeignHunkDetected` en NOMMANT les hunks (numéros de ligne + contenu), pas en disant juste non. Calibré sur la FORME gelée d'`e21c1f3`, dans un dépôt git TEMPORAIRE (jamais le dépôt réel) : `test_FORME_e21c1f3_a_foreign_uncommitted_hunk_swept_by_git_add_is_caught` — un hunk étranger CONTIGU à mon propre hunk (aucune ligne de HEAD entre les deux pour les séparer, exactement la forme réelle) ; une première version de l'instrument les fusionnait en un seul bloc et NE DÉTECTAIT RIEN, corrigée par une seconde passe de diff à l'intérieur du bloc contre le snapshot **plus un contrôle POSITIF apparié** (`test_POSITIVE_only_my_own_hunks_staged_PASSES` — aucun travail étranger dans l'arbre au moment de l'empreinte → PASSE), sans lequel une garde qui refuse tout serait aussi inutile qu'une garde qui accepte tout · `tests/sandbox/test_staged_authorship.py` (6 tests, dont la vérification que son propre nom `check_*` reste exclu du cliquet d'instruments). ⚠️ Ne couvre QUE le sens A : le sens B (occ. 9) reste sans garde exécutable — rien à comparer dans un stage, c'est un problème d'ordonnancement entre sessions · occ. 7 : contre-exemple GELÉ `test_the_guard_SKIPS_world_tests_when_a_lease_is_held` (`tests/sandbox/test_lease_guard.py`) — et la garde a mordu en production le jour même (14 tests sautés pendant qu'un run tenait le bail). ⚠️ **occ. 9-11 (sens B) — CALIBRÉE le 2026-09-02, et sa limite est MESURÉE, pas supposée** : `detect_preempted()` est exécutable mais **NON ATTRIBUTIVE**. Confrontée aux 8 empreintes réelles du dépôt, elle rend **2/2 faux positifs**, tous sur un commit de l'auteur lui-même : elle ne sait pas séparer « on m'a préempté » de « j'ai committé sans le déclarer », l'exclusion reposant entièrement sur un `confirm_commit(..., owner=)` que rien n'oblige. Elle est donc SAINE EN FLUX (snapshot -> édite -> commite -> `confirm --owner`) et TROMPEUSE EN BALAYAGE. **Ne pas la câbler au hook** tant que l'attribution n'est pas résolue — un cliquet qui crie à tort est pire qu'absent. Application directe de « ne pas proxifier ce qu'on ne sait pas mesurer » : le sens B n'est PAS décidable sans DÉCLARATION. 🔧 **CORRECTION du même jour, sur mesure — la conclusion ci-dessus était TROP DURE.** Le contrôle apparié a été fait : déclarer les commits miens via `confirm_commit(..., owner=)` fait disparaître les 2 faux positifs **et RIEN d'autre** — un TROISIÈME signalement, lui VRAI, subsiste et sort renforcé (occ. 16, `481117e`, `matched_by_content: true`). Le défaut n'est donc pas un manque de SENSIBILITÉ ni un bruit de fond : c'est une **PRÉCISION CONDITIONNÉE À LA DÉCLARATION**, et les faux positifs ont une source UNIQUE et identifiée — les commits de l'auteur non déclarés. Conséquence révisée : la garde EST câblable, à condition que `confirm --owner` fasse partie du flux de commit et non de la bonne volonté. La dette P2.26 se reformule donc en « rendre la déclaration automatique », pas en « rendre la détection attributive ». **(17, 2026-09-06 — déclaration RENDUE AUTOMATIQUE, scopée par IDENTITÉ DE SESSION)** : hook `tools/hooks/post-commit` → `check_staged_authorship.py declare`, qui inscrit le SHA dans les seules empreintes portant le `session_id` du processus qui commite (`$AGAGI_SESSION_ID` puis `$CLAUDE_CODE_SESSION_ID`, inscrit par `snapshot()`) et dont le chemin est porté par le commit. Le contenu a été REJETÉ comme critère (il ne porte pas d'auteur — un post-commit « par contenu » aurait désamorcé la détection chez la victime, à la ligne près) ; sans identité, rien n'est déclaré. Contre-exemples gelés : `test_P226_SPARES_a_PARALLEL_session_commit_is_NOT_declared_and_detection_SURVIVES` (la question dure) et `test_P226_SPARES_without_session_identity_NOTHING_is_declared` ; positif apparié `test_P226_FIRES_own_commit_is_auto_declared_when_the_session_matches`. **(2026-09-07 — le BALAYAGE est CÂBLÉ, porte 7)** : `scan_own_snapshots` signale les préemptions des empreintes de la session COURANTE, en AVERTISSEMENT SEUL — une préemption n'est pas réparable par celui qui commite, son travail est déjà dans HEAD, et bloquer sur l'irréparable produit un cliquet qu'on désactive. Condition de câblage posée AVANT et mesurée : 9 empreintes, 1 signalement, et c'est le vrai positif — zéro faux positif. Contre-exemples gelés `test_P226_SCAN_FIRES_on_MY_snapshot_preempted_by_another_session` et `test_P226_SCAN_SCOPE_ignores_ANOTHER_session_and_LEGACY_snapshots` (les deux préemptées, les deux ignorées : le scope est mesuré, pas affirmé). **(18, 2026-09-07 — FORME NOUVELLE, et l'erreur est de MOI : `git reset` sur un index PARTAGÉ n'est PAS neutre)** : pour committer 2 fichiers à moi sans emporter les 95 chemins qu'une session parallèle venait de stager, j'ai fait `git reset` puis restagé les miens. Raisonnement : « l'arbre de travail n'est pas touché, rien n'est perdu ». FAUX en pratique — l'autre session a committé dans cette fenêtre et son commit `267edce` est sorti avec 2 fichiers (les miens) au lieu de 96, son message annonçant du travail absent du commit. Rien n'a été perdu (elle a recommitté en `0efbc49`, en documentant la cause), mais j'ai scindé son lot et fabriqué un message qui sur-déclare son contenu — exactement le défaut P2.42 qu'elle avait corrigé la veille. **Ce qui est faux dans mon raisonnement** : l'index partagé n'est pas un brouillon privé, c'est l'ÉTAT D'UN COMMIT EN COURS chez quelqu'un d'autre ; le vider est destructif même sans perte de fichier. **Remède exécutable, et il ne touche RIEN de partagé** : construire un index TEMPORAIRE — `GIT_INDEX_FILE=$tmp git read-tree HEAD` puis `update-index` sur ses seuls chemins, `write-tree`, `commit-tree` — au lieu de toucher `.git/index`. ⚠️ Contrepartie à déclarer : cette voie court-circuite les hooks, donc les portes doivent être lancées à la main avant. Voie plus simple quand elle suffit : `git commit -- <chemins>` (partiel, depuis l'arbre) qui ignore l'index sans le modifier. ⚠️ Leçon de méthode, pour moi-même : j'avais gravé « 2/2 faux positifs » à partir d'UN balayage, sans le contrôle apparié qui l'aurait qualifié — le même défaut que je reproche aux instruments du dépôt. **(19, 2026-09-07 — COMPLÉMENT sens A de l'occurrence 18, commise par l'AUTRE session : `git commit` SANS pathspec)** : le même commit `267edce` a aussi EMPORTÉ une ligne du registre appartenant à la session qui venait d'écrire la porte 7 (occ. 18 ci-dessus décrit la moitié « index vidé » ; voici la moitié « hunk étranger happé »). Cause : `git add -A tools/ tests/ docs/EDR/ …` puis **`git commit` NU**, sans `-- <chemins>` — alors que `docs/REF/REGISTRE_ERREURS.md` n'avait JAMAIS été touché par la session courante et apparaissait `M` dans son propre contrôle pré-commit. ⚠️ **La garde EXISTAIT** : `check_staged_authorship.snapshot()/verify()`, livrée cinq jours plus tôt pour EXACTEMENT ce cas — jamais invoquée. C'est la deuxième fois que la ligne « une garde disponible et NON DÉCLENCHÉE vaut zéro » (occ. 5) décrit un fait du jour. **Rien n'est perdu** (le contenu d'autrui est dans HEAD, intact) : le dommage est d'ATTRIBUTION. **Règle opératoire adoptée** : sur ce dépôt, tout commit passe par `git commit -- <chemins>` (jamais nu), et tout fichier PARTAGÉ édité passe par `snapshot()` AVANT édition — la discipline `git add` explicite ne suffit pas, elle a échoué ici alors qu'elle était appliquée. **Occ. 17 (porte 4, `2ffef2ab`)** : (a) `chemin-non-suivi` en prose — index lu (`git ls-files`), un fichier STAGÉ compte comme suivi, un chemin PROPOSÉ reste épargné ; (b) sémantique unique : EN COMMIT (`GIT_INDEX_FILE`) `_lire_backlog` (`git show :chemin`), `_existe` et `_lire_fichier` lisent l'INDEX — la porte juge exactement ce qui sera committé ; HORS commit, le disque ; git indécidable → le disque, jamais un verdict fabriqué. Contre-exemples : `tests/sandbox/test_backlog_freshness.py`, 12 cas — le cas réel non suivi ; spécificité (suivi → rien) ; stagé → rien ; proposé → épargné ; en commit : fichier sur disque HORS index = MORT pour ce commit (jamais « non suivi ») — c'est le cas « clone frais » du PM ; fichier DANS l'index existe même absent du disque ; clause `path_present` juge l'index et n'est plus REFUSÉE ; clause `grep_*` lit la cible dans l'index ; deux intégrations sur un index temporaire RÉEL construit depuis HEAD (le texte lu est HEAD, jamais le disque quand ils diffèrent). Le helper des cas jouets retire `GIT_INDEX_FILE` (hérité sous la porte 15). Règle humaine gravée dans CLAUDE.md (`05592688`) : une citation et son artefact partent dans le MÊME commit — stager ne suffit pas. ⚠️ La porte 2 avait appris la même leçon le 2026-09-01 (`--only` sur les fichiers stagés, commentaire du hook) : personne ne l'avait relue pour la porte 4. |
| **E11** | **Choix d'analyse post-hoc** (jardin aux sentiers qui bifurquent) | seuil 0.5 sur `gi`, partition FRÉQUENT/RARE, choix du prédicteur — tous arrêtés après avoir vu les données. **(2026-07-27, EVO-006)** : forme NOUVELLE et plus fuyante — la règle de lecture ÉTAIT pré-écrite, mais la sonde mécaniste qui a confirmé le verdict a été choisie APRÈS avoir vu QUELLE sous-tâche avait bougé. **La pré-inscription protège du post-hoc sur le SEUIL, pas sur l'INSTRUMENT** si celui-ci n'est pas nommé dans la règle **(3, 2026-08-04, EVO-019)** — la garde elle-meme EN DEFAUT : la regle scellee prevoyait « >= 3/12 » et « 0/12 » ; le resultat est tombe a **1/12**, dans le TROU entre les deux branches. Le sceau protegeait le SEUIL mais ne verifiait pas que les issues soient **EXHAUSTIVES** — toute la latitude post-hoc qu'il existe pour supprimer etait rouverte au milieu de l'echelle. **(4, 2026-08-04, EVO-019 — trouvée en REVUE ADVERSARIALE, seule des 7 critiques CONFIRMÉE)** : forme que le sceau NE PEUT PAS détecter. La règle scellée exigeait « le plafond doit RÉDUIRE `|logit|` médian » ; le record a substitué une réduction de FAN-IN et écrit « les deux conditions sont satisfaites ». Le sceau prouve la NON-MODIFICATION de la règle, pas la FIDÉLITÉ de son application — et la substitution a été commise dans le paragraphe même où le record se félicitait d'avoir signalé l'autre trou du pré-enregistrement. Aggravant : la clause « si l'un des deux échoue, ne pas lire le taux » a été enfreinte (0/7 lu, puis réinjecté dans un total de 31). | `exécutable` **(2026-07-27)** | `tools/preregister.py` (+ `_assert_exhaustive`, 2026-08-04 : une regle dont les branches ne couvrent pas le CONTINUUM est **REFUSEE** a l'ecriture — il faut une branche attrape-tout ou une cle `regle_de_lecture_continue`. 4 tests, dont un qui rejette exactement la regle d'EVO-019) + `tests/sandbox/test_preregistration_guard.py` (P3.1 CLOSE). Scelle la règle par un hash : ré-enregistrer un contenu DIFFÉRENT sous le même nom **lève** (une règle ne se corrige pas — on en écrit une `-bis`, ce qui rend le changement VISIBLE), et une édition du JSON à la main est DÉTECTÉE. Un test balaie `docs/preregistrations/` et tombe si une règle déjà gravée a été retouchée. ⚠️ **Portée honnête** : ça prouve la NON-MODIFICATION, pas l'antériorité au run (rien ne le peut hors horodatage externe) ; et ça n'empêche pas d'ajouter après coup un instrument non prévu — ça le rend seulement DÉTECTABLE (absent du fichier scellé). D'où la règle d'usage : **nommer les instruments autorisés DANS la pré-inscription** (fait pour EVO-007) · ⚠️ **Ce que la garde ne couvre PAS** : elle scelle la RÈGLE, elle ne vérifie pas que le record MESURE la DV scellée. Seule une revue adversariale l'attrape (occ. 4). **Règle d'usage** : à l'écriture du record, relire la règle scellée CLAUSE PAR CLAUSE et nommer explicitement la DV mesurée pour chacune. **GARDE AJOUTÉE (2026-08-04) pour l'occ. 4** : `tools/check_preregistration_applied.py` + `tests/sandbox/test_preregistration_applied.py` (5 tests). Les grandeurs NOMMÉES dans les clauses de mesure d'une règle scellée doivent apparaître dans le record qui s'en réclame — une DV substituée en silence ne passe plus. Contre-exemple GELÉ : la configuration exacte d'EVO-019 avant correction (règle `|logit|`, record qui ne parle que de fan-in) fait tomber le cliquet. ⚠️ Vérification LEXICALE, pas sémantique : elle attrape l'omission, pas le record qui nomme la grandeur en mesurant autre chose. Nécessaire, pas suffisante — la revue adversariale reste la garde de dernier recours. |
| **E21** | **Le SHELL a mutilé le texte, et l'outil a scellé/committé la version mutilée sans erreur** — toute chaîne passée au shell (`git commit -m`, `python -c`, `echo`) subit la SUBSTITUTION DE COMMANDE sur ses backticks : le fragment part VIDE, silencieusement. | **(1, 2026-09-07)** un message de commit a perdu deux fragments (« mon `git commit` NU » → « mon  NU », commit `8f7abec`, non réparable : pas d'amend). **(2, 2026-09-07, une heure plus tard, et c'est plus grave)** un `python -c` a SCELLÉ une pré-inscription MUTILÉE — quatre champs de `S6-FALLBACK-RATE` vidés de leurs noms de fonctions (« le MESUREUR (, , ) est IMPORTE »). Une règle scellée ne se corrige pas : re-scellée en `-bis` (texte restitué, AUCUNE clause modifiée, et le run n'avait encore rendu aucune lecture). ⚠️ **La règle écrite après l'occurrence 1 ne visait que `git commit -m`** — elle a été prise en défaut par un `python -c` : *une règle apprise sur UN cas doit être énoncée sur le MÉCANISME, pas sur le cas.* | `documenté` | Remède : passer par un FICHIER (`git commit -F`, un script écrit avec l'outil Write) plutôt que par une chaîne shell. Contre-exemple candidat si récidive : un cliquet qui refuse un JSON de pré-inscription contenant `(, ,` ou `«  »` — signature mécanique d'une substitution. |
| **E12** | **Extrapolation depuis un préfixe court** | tendance du grab lue sur 200 epochs — c'était la branche montante d'un transitoire **(2026-09-08, sur une mesure de COUT, et elle a inverse une DECISION)** — la suite `tests/sandbox/` chronometree a **8 h 30**, chiffre publie dans CLAUDE.md et au backlog, et dont j'ai tire « faire tourner la suite en CI est exclu, donc il faut un cliquet de couverture ». La mesure avait ete prise pendant que **SEIZE agents d'un workflow** tournaient sur la meme machine, dont plusieurs simulations de monde. Seconde passe, machine au repos : **30 min 40 s** — un facteur **17**. La recommandation s'inverse : a 30 min la CI PEUT lancer la suite entiere, et un cliquet de couverture ne verifie jamais qu'un fichier PASSE, seulement qu'il est cite. **Ce que ca ajoute a la classe** : E12 ne concerne pas que les mesures scientifiques. Un chiffre de COUT est une mesure comme une autre — il se prend dans un regime declare (ici : la charge de la machine), sinon il extrapole depuis un regime qui n'est pas celui de la decision. Corrige dans les deux documents, avec le facteur et sa cause. **(2026-09-14, P4.4, sur le COÛT — attrapée AVANT engagement)** : unité d'un seed complet extrapolée à ~136 s depuis un smoke à 200-400 ticks ; mesurée à 675 s (le coût par tick CROÎT avec le remplissage du monde). `project_cost` a REFUSÉ le départ ; budget relevé par sceau `-ter` sur la mesure. Et le runner sauvait le JSON APRÈS la garde : une projection refusée perdait ses 11 min de mesure — corrigé (sauver l'unité AVANT de projeter). **(occ. 2026-09-24 — LA CONDITION NON DÉCLARÉE N'EST PAS SEULEMENT LA CHARGE, C'EST L'ÉTAT ET SON INSTANT)** : cette classe a été établie sur le COÛT (8 h 30 mesurées sous seize agents contre 30 min au repos, chiffre qui avait inversé une décision de CI) ; la condition non déclarée y était la charge. Deux faces mesurées le même jour montrent la même forme avec une autre variable — l'état de l'arbre PARTAGÉ et l'instant de la lecture. **(a)** J'ai publié un diagnostic « le dépôt est cassé, `core.bare = true`, la fusion est impossible » dont les mesures dataient de 13:32-13:42 ; l'arbre avait été réparé à **13:42:31**, donc le rapport était PÉRIMÉ À L'ENVOI — juste quand je l'ai pris, faux quand il a été lu. **(b)** Une autre session a lancé une suite complète pendant qu'un implémenteur committait sur sa branche : trois échecs affichés, aucun sens, l'arbre bougeait sous les tests. ⚠️ **Règle opératoire, qui dit quoi ÉCRIRE et pas seulement d'être prudent** : sur arbre partagé, une mesure s'ANCRE SUR UN SHA, ou s'accompagne de l'HEURE et de l'aveu qu'elle peut avoir changé. Ce qui a survécu de ma journée, ce sont les mesures indépendantes de l'état — la reproduction de la fuite `GIT_DIR` sur deux dépôts jouets vaut toujours, le diagnostic d'état non. **(occ. 2026-09-26 — le CONTRÔLE est fait à un instant, l'ACTION prend l'état à un autre ; au débit de la session qui coordonne la flotte, inscrite à sa demande)** : pour committer `.github/workflows/ci.yml`, elle a pris l'empreinte d'autorat (`tools/check_staged_authorship.py`) puis committé par pathspec. Entre les deux, une AUTRE session a écrit dans le MÊME fichier un smoke docker de quelques lignes. Or un commit par pathspec prend le fichier ENTIER du disque : le hunk étranger est parti dans `9bf30520`, sous un message qui parle d'installation de dépendances. Rien n'est perdu — le contenu est poussé tel quel, sur décision de la session coordinatrice — mais un lecteur de l'historique attribuerait ce smoke à une réparation d'installation. ⚠️ **Le mécanisme est celui de cette classe** : l'empreinte est une mesure à l'instant T1, le commit agit à T2 ; la garde d'autorat ne voit que les hunks présents à T1, et la fenêtre T1→T2 est AVEUGLE. Même ISSUE que l'occurrence 19 d'**E10** — le commit nu emporte l'index entier —, au grain du FICHIER : le pathspec protège les autres fichiers, jamais les autres hunks du même fichier ; mais pas le même mécanisme, puisque la règle était ici APPLIQUÉE, seulement au mauvais instant. ⚠️ **Et le signal était lisible** : le compte du commit valait **+20/−14** pour un remplacement voulu de 13 lignes par 13. C'est la leçon d'**E22** occ. 2 — lire le compte de chaque commit — avec un cran de plus : un compte lu mais comparé à RIEN n'est pas un contrôle, il faut le confronter au delta ATTENDU. **Garde** NON écrite, inscrite au backlog en **P2.122** par la session qui a commis l'occurrence : une vérification d'autorat qui reçoit le contenu ATTENDU et refuse, en le NOMMANT, tout bloc absent de HEAD et de ce contenu ; un commit qui ferme la fenêtre ; le compte du diff confronté au delta attendu. Palliatif appliqué par cette session dès `faae4909` : juste avant le commit, le disque est comparé octet pour octet au contenu produit. Documentée tant que la garde n'est pas écrite. | `documenté` | `CLAUDE.md` §Coût des runs |
| **E13** | **Dépassement de coût non borné au design** | 3 runs abandonnés (8 h, 4 h, 89 min) + 1 run de 1,8 h sur une question sans objet. **(2026-07-27, EVO-007 — 2 runs de plus)** : 187 min pour 8 seeds sur 36 (5,6 Go, tué), puis un 2ᵉ lancement bloqué au MÊME seed. **Ces deux-là réfutent l'énoncé de la dette** : le débit AVAIT été mesuré au smoke et il était JUSTE (2,6 s/ère, projection 21 min) — le run a explosé quand même, parce que **le coût dépend du SEED** (il suit le succès évolutif, `CLAUDE.md`). Mesuré : seeds 0-4 = 35 s, seed 8 = jamais fini. Cause localisée : à l'ère 13 du seed 8, la population passe de ~33 à **962 agents** en 84 ticks **(2026-08-04, EVO-019/020)** : la garde a été APPLIQUÉE CONTRE SA PROPRE PRESCRIPTION — `budget_s=150/seed`, alors que la clôture d'E13 impose un plafond en NOMBRE D'AGENTS précisément parce qu'un plafond en SECONDES rend la troncature dépendante de la machine. Conséquence mesurée : 5 abandons sur 12 (EVO-019) et 7 sur 24 (EVO-020), **corrélés au succès évolutif** (le coût suit le succès) — donc une censure INFORMATIVE, biaisée CONTRE la détection d'un lecteur. | `exécutable` **(2026-07-27)** | `tools/cost_guard.py` + `tests/sandbox/test_cost_guard.py` (P3.2 CLOSE). `project_cost` refuse un design intenable AVANT (marge ×3 — **aucune projection linéaire ne borne une queue**, d'où la marge) ; `CostGuard` borne PENDANT. ⚠️ **Deux pièges de conception de la garde, tous deux commis puis corrigés le même jour** : (1) borner le CONTENEUR au lieu de l'**unité de travail ATOMIQUE** — la 1ʳᵉ version testait le budget ENTRE les ères alors que la pathologie est DANS une ère, donc on ne lui rendait jamais la main ; (2) plafonner en **SECONDES** — ça rendrait la troncature dépendante de la machine, donc le banc **NON REPRODUCTIBLE**. Le plafond retenu porte sur le NOMBRE D'AGENTS (`MAX_AGENTS=200`, pics normaux 30-44), déterministe. **Un abandon doit être COMPTÉ et RAPPORTÉ** : un seed silencieusement absent est un biais de sélection · ⚠️ **La garde existe mais n'a pas été SUIVIE** : `budget_s` reste un paramètre libre de `evolve_cognitive`. Dette ouverte : exposer un plafond en nombre d'ères/agents et déprécier `budget_s`. **GARDE RENFORCÉE (2026-08-04)** : `evolve_cognitive(budget_agent_ticks=…)` — plafond DÉTERMINISTE en agent-ticks simulés (même seed → même troncature, partout), et `budget_s` émet désormais un `DeprecationWarning` qui nomme le biais de censure. La prescription de la clôture est enfin APPLICABLE, pas seulement écrite. |
| **E15** | **Statistique de POPULATION comparée entre populations de compositions différentes** — la métrique n'est pas dégénérée, elle est CONFONDUE | EDR-095 : le rêve forcé multiplie `n_lived` par **13-16** ; la survie médiane sur TOUS les agents chute de 55 % (`sign_p` 0.0005, reproduit) alors que sur la cohorte fondatrice appariée l'effet est **ABSENT** (8/12). L'indice — le `n_lived` ×16 — était publié **dans le record**, en « effet secondaire » **(1, 2026-09-22, `tools/func_benefit.py::run_seed` — le mort en attaquant sort du compte ; trouvé par agagi-b0 en re-déclarant l'instrument, P2.56)** : `mammoth = sum(ag.get("mammoth_kills", 0) for ag in env.agents)` (l. 55) somme sur `env.agents`, la liste des VIVANTS — les agents morts sont dans `dead_agents` — puis FIABLE vs SOLO compare les moyennes de ce compte (l. 95). Un bras qui meurt DAVANTAGE en chassant perd ses tués du numérateur : la composition des deux populations comparées diffère par la mort, et le contraste mesure autant « qui survit à la chasse » que « qui tue ». Même forme que le biais de survivants corrélé au bras de CALIB-LEARNER (a), où une recharge d'énergie seule laissait les bras apprenants perdre la moitié de leur cohorte. Verdicts historiques de func_benefit à relire sous cet angle avant toute réutilisation. | `exécutable` *(promue le 2026-09-02 — la règle « pas de troisième fois » l'exigeait : seule classe dont la garde restait une phrase adressée à un humain alors qu'elle est trivialement exécutable)* | `assert_n_per_arm` (`tools/experiment_preflight.py`) — refuse de comparer des médianes quand le ratio des populations dépasse 1.5× ou qu'un bras est vide · contre-exemple GELÉ aux chiffres réels d'EDR-095 (n ×13-16) : `test_n_per_arm_catches_the_EDR095_population_shift` + spécificité (cohortes appariées passent). **Vérifier `n` PAR BRAS avant de comparer des médianes.** ⚠️ Aucune garde de borne ne voit ça : aucun bras n'est au plancher ni au plafond. Une médiane est robuste aux valeurs extrêmes, **pas à un changement de population** **Occ. 1** : documenté dans la déclaration CALIBRATED de `func_benefit.py::run_seed` (b0, P2.56) ; correctif si l'instrument est réutilisé : compter les tués sur `env.agents + env.dead_agents` et publier `survivors` à côté (déjà rendu, l. 61) — le compte des morts par bras devient une DV, pas un silence. |
| **E14** | **Garde exécutable jamais RÉTRO-APPLIQUÉE** — le cliquet protège le travail à venir, pas les conclusions déjà gravées, qui continuent d'être citées | `assert_not_degenerate` existait quand WARM-002 a été relu ×N ; personne ne l'a pointée sur son bras à 5.0-7.2 ticks. Le verdict « paysage PLAT » a alimenté 4 records pendant ce temps **(2026-09-08, HUIT tests rouges d'une SEULE garde, invisibles pendant une semaine)** — le correctif E14 du 2026-09-01 a ajoute `sign_p < 0.05` aux branches de verdict de TROIS instruments (`metabolic_cost_sweep`, `g_fidelity_probe`, et `g_bilinear_probe` qui compose le second). AUCUNE fixture n'a ete retro-appliquee : a n=3 `sign_p` vaut **0,25 au mieux**, a n=5 **0,0625** — les verdicts `EFFICACE`, `G_FIDELE`, `G_INUTILE`, `LATENT_BILINEAR`, `LATENT_LINEAR`, `ENCODING_ARTIFACT` etaient donc devenus **INATTEIGNABLES PAR CONSTRUCTION**, et leurs tests rouges depuis. Le commentaire qui documente le correctif E14 est a trois lignes du `elif` fautif. Corrige : fixtures remontees a n=8, et le PLANCHER DE PUISSANCE grave dans un cas dedie par fichier (« la MEME dose sur 3 seeds rend NEUTRE, par construction ») -- sans quoi la prochaine garde ajoutee re-cassera les fixtures en silence. ⚠️ **Pourquoi une semaine** : ces fichiers ne sont lances par AUCUN job de CI (1937 tests sur 2059, cf. P2.45 du backlog). Un huitieme rouge, de la meme famille, exigeait qu'une cohorte VIDE rende `AUTEL_MORT`. **Regle qui en sort** : ajouter une garde de PUISSANCE a un verdict, c'est changer le contrat de TOUTES ses fixtures — les relancer fait partie du correctif, pas du nettoyage. **(2026-09-14, deux fois en quatre jours, même fichier)** : `_verdict_capacity` a reçu sa garde d'arguments le 2026-09-06 ; ses jumeaux structurels `_verdict_landing` (même forme delta+pente) et `_verdict_evolve_nav` (même forme first/last) ne l'ont jamais reçue. Sur UN seul bras / UNE seule génération, les deux rendaient un négatif de fond (« AFFORDANCE INERTE », « SUBSTRAT BLOQUÉ ») tiré d'un point, et le rapport voisin publiait une `slope` fabriquée à 0.0 dans le JSON. Trouvés par le 2e élargissement de la porte 14, pas par relecture. Même famille, à l'échelle d'une FAMILLE d'outils : `eval_harness.powered_eval` et ses HUIT copies `_stats` fabriquaient `std = 0` pour n = 1, donc Welch t = 0, donc « NON significatif » — corrigées d'un bloc, en cherchant la FORME et non l'instance. **(occ. 2026-09-23, P2.82 — la garde de BAIL apprise sur la COLLECTE, jamais généralisée aux tests qui la contournent ; un skip levé au mauvais niveau devient un verdict)** : suite de nuit du PM sous le bail `kuzu` de P4.16, 1789 verts / 1261 sautés / 5 rouges dont 4 FAUX, reproduits (4 failed en 2,7 s, pid 69232 détenteur) : (a) `tests/sandbox/test_s2_ablation_real_path.py` (l.74, l.92) prend `hold("kuzu")` LUI-MÊME sans porter d'indice de monde — la garde de collecte (`_WORLD_HINTS`) ne le saute pas et `ResourceBusy` = FAIL ; (b) `tests/test_backend.py::test_flatland_runs_crud` et `::test_ws_flatland_run_id_streams_frames` : le `Skipped` du filet runtime (`_arm_runtime_net`, P2.69 ii) est levé DANS l'app ASGI, traversé par Starlette et rendu « RuntimeError: No response returned » / `BaseExceptionGroup` = FAIL. Même mécanisme que l'occ. 1 : une garde posée à UN point (la collecte) et absente des deux autres points où le même état (bail étranger) se manifeste (la prise de bail par le test ; la construction d'un monde côté serveur). Leçon de forme : ces quatre rouges n'existent QUE bail tenu — invisibles à toute suite lancée machine au repos (E12 côté tests), d'où le job « suite sous détenteur factice » proposé dans P2.82. | `exécutable` **(triage seul)** | `tools/retro_audit_records.py` — signale `verdict NUL × conclusion sur le MONDE × plancher avoué`. ⚠️ **La partie jugement N'EST PAS automatisable** : voir ci-dessous · contre-exemple GELÉ : `test_cited_oracle_is_not_a_positive_control` (citer un oracle ne doit PAS faire baisser le score — c'était le faux vert) et `test_positive_verdict_is_not_flagged` (spécificité : un record sain reste à risque 0). **Occ. 2026-09-23 (P2.82)** : `tests/conftest.py::pytest_runtest_call` (wrapper : `ResourceBusy` pendant l'appel du test → `pytest.skip` nommant le détenteur ; toute autre exception passe) et fixture `sans_bail_etranger` (décision `_skip_si_bail_etranger`) sur les tests backend qui servent un monde ; contre-exemples dans `tests/sandbox/test_lease_skip_guard.py` — `test_ResourceBusy_raised_by_a_test_becomes_a_SKIP_naming_the_holder` (rougit sans le hook : AttributeError), `test_any_OTHER_exception_passes_through_the_guard_unchanged` (spécificité), deux bouts-en-bout sous détenteur PRÉSENT ou FACTICE (sous-processus enfant tenant `kuzu`, lancé par le test quand le bail est libre : mesurable machine au repos ; sinon SKIP explicite, jamais un vert fabriqué), `test_sans_bail_etranger_decision_skips_only_when_a_foreign_holder_exists`. |
| **E17** | **AMPLITUDE mesurée là où le mécanisme lit le SIGNE ou le RANG** — l'instrument rapporte ‖Δsortie‖ alors que la décision est prise par `sign()` ou `argmax()`. Sur un substrat **contractif** les amplitudes sont écrasées : une dépendance fonctionnelle TOTALE peut se lire comme une saillance nulle (faux négatif), et du bruit de grande amplitude comme une saillance forte (faux positif) | **(1)** EVO-002 : `sep(D)` (rétention par norme d'état) — ne distinguait PAS un champion à `acc 1.00` d'un génome frais (0.70 vs 0.70/0.47) ; réfuté, rétrogradé en corroborant. **(2)** EVO-004 : `measure_cue_saliency` en amplitude — un champion DEMAND à `acc 1.00` (donc lecteur OBLIGÉ) mesuré à **0.13**, soit SOUS un génome frais (0.10) ; la mesure fonctionnelle `sign_flip` sépare **1.00 vs 0.48** | `exécutable` | Cas de calibration figé : l'instrument doit séparer un LECTEUR connu d'un non-lecteur **sur la grandeur qui AGIT** (`sign_flip`, bascule d'`argmax`), et le test épingle que **la variante en amplitude ÉCHOUE là où la fonctionnelle réussit** (`test_instrument_calibration.py`). Règle de lecture : avant de mesurer une saillance, identifier l'opérateur de DÉCISION en aval (`np.sign(preds)` proxy ; `argmax(logits[:8])` in-world, `world_1_stoneage.py:1291`). **(3) 2026-07-27, EVO-006 — même défaut, déplacé du choix de la GRANDEUR à celui de la SORTIE** : la sonde d'`argmax(logits[:8])` rend **0.000** sur un génome qui lit PARFAITEMENT le signal `throw` (bascule de `sign(logits[8])` = 1.000, vérifiée), parce que sa lecture ne passe pas par les logits de déplacement. Un lecteur avéré mesuré comme non-lecteur : c'est un faux négatif produit par le choix de la sortie sondée, pas de la grandeur. Contre-exemple gelé (`test_argmax_saliency_is_blind_to_a_perfect_throw_reader`), instrument `measure_decision_saliency` né et calibré dans la même passe. **Règle élargie** : avant de mesurer une saillance, identifier l'opérateur de décision en aval ET **la sortie par laquelle il passe** — un monde qui lit plusieurs décisions par tick a plusieurs opérateurs, et une sonde par opérateur |
| **E18** | **Estimateur qui récompense l'ABSENCE DE PREUVE** — un score par agent lissé (ou seuillé) vers une valeur SUPÉRIEURE au régime réel de la population crédite le manque de données. Mis dans une fitness, il sélectionne pour **produire moins de preuve** — typiquement mourir tôt, ou n'agir qu'une fois — et le banc rend alors un faux négatif qui ne mesure QUE l'estimateur | **(1)** [[EDR-056]] : distinction référentielle = 1.0 dès `m.sum() ≥ 1 et l.sum() ≥ 1`, donc un agent ayant parlé UNE fois près de chaque apex décrochait le score maximal ; ×400 dans la fitness -> propagation d'agents à distinction FORTUITE, émergence 38 %→12 % (le record impute le backfire au « bruit », sans nommer le mécanisme : c'est l'absence de preuve qui était payée). **(2)** EVO-005, attrapé AU PRÉ-VOL avant tout run : lissage vers la CHANCE (0.5) alors que les agents réels plafonnent à 0.10 -> un mort à 3 ticks vaut 0.435 contre 0.157 pour un vivant de 120 ticks ; à poids fort la sélection aurait optimisé la mort précoce | `exécutable` | Lisser vers **0** (pessimiste), jamais vers la chance : `succès/(ticks + PSEUDO)`. Propriété à asserter — un tick RÉUSSI de plus doit TOUJOURS améliorer le score (`t + PSEUDO > h`, vrai par construction). Contre-exemple GELÉ : `test_cognitive_rate_has_no_incentive_to_die_early` épingle que la variante « lissage vers la chance » préfère le mort précoce. ⚠️ Ni E3 (l'estimateur n'est pas dégénéré, il varie normalement) ni E17 (la grandeur est la bonne, c'est son PRIOR qui est faux) |
| **E16** | **Métrique nommée pour un mécanisme MORT** — une compétence agrégée dont un terme DOMINANT est alimenté par une stat que rien n'écrit ; la métrique varie normalement (≠ E3), mais toute sa variation vient de ses termes accessoires et se lit sous le nom du terme mort | `altars_solved` jamais incrémenté dans les 5 mondes actifs (bloc de résolution resté dans du legacy déprécié) → `gym_competence` ≡ 0, `industrial_competence` plafonnée à 0.4, et le barreau 2 du design dreaming (« la compétence-autels quitte le plancher ») ne pouvait bouger que par la SURVIE — que [[EDR-DREAM-001]] augmente de 77 % : faux positif armé ([[EDR-AUDIT-002]]). **(occ. 2, 2026-09-14, P4.8 pré-vol — un TERME DE RÉCOMPENSE mort)** : [[EDR-S2-CREDIT-RETENTION]] écrivait la récompense du crédit in-world comme « Δénergie + curiosité + nouveauté » et en faisait son hypothèse mécaniste ; or sous le backend torch (`use_torch_inworld`, le chemin PUBLIÉ) `backend_torch.py` n'écrit JAMAIS `model.surprise` — seul le forward legacy numpy le pose (`mamba_agent.py:824`) — donc le terme `curiosity_scale·surprise` de `world_1_stoneage.py:1713` vaut 0 à chaque tick. Mesuré au smoke (`results/s2_reward_ablation_smoke.json`) : bras « +curiosité » BIT-IDENTIQUES à leurs jumeaux (dW 1442,82 = 1442,82 sur 200 ticks). Deux bras sur cinq du design scellé ne pouvaient rien changer (E2), attrapé AVANT ~10 h de calcul ; design re-scellé en `S2-REWARD-ABLATION-bis` à trois bras. Garde : `preflight_curiosity_dead` (`tools/evo_runs/s2_reward_ablation.py`, contre-exemple `test_preflight_curiosity_dead_refuses_a_live_surprise`) ; le cliquet statique de l'occ. 1 ne voit pas ce site (la stat est lue par le MONDE, pas par une fonction de compétence) — la « curiosité » d'EDR 014 est legacy-only, P2.65 | `exécutable` | `tests/sandbox/test_competence_stats_are_live.py` — cliquet statique : toute stat lue par une fonction de compétence doit être écrite par `src/worlds/` hors init-à-0 et hors classes `Legacy`. Dette gelée `{altars_solved}`. ⚠️ **Ni E3 (pas dégénérée) ni E15 (pas de comparaison de populations)** : défaut COMPOSITIONNEL, détectable statiquement pas dynamiquement |
| **E19** | **NUL DE CAPACITÉ produit par un RÉGLAGE D'OPTIMISATION validé sur les conditions FACILES, puis appliqué à la condition TESTÉE** — un verdict comparatif oppose des conditions de **difficulté d'optimisation inégale** (1 pas vs 2 pas ; opérandes co-présents vs portés à travers un tick) à réglage FIXE. Le réglage n'a jamais été validé QUE dans les bras faciles ; dans le régime qui PORTE le verdict il diverge, et le nul mesure alors le RÉGLAGE, pas le substrat. **Aggravant structurel** : tous les contrôles de calibration de la sonde vivent eux aussi dans le régime FACILE, donc aucun ne PEUT voir la pathologie — l'instrument est déclaré *calibré* et son « OK » ne dit rien du régime qui porte la conclusion | **(1) 2026-08-03, BILINEAR Tâche 2** : le nul REINFORCE/2-pas lu comme « le bilinéaire ne débloque pas la composition » ; la revue adversariale y trouve **2 confonds non contrôlés** (crédit, rétention) et juge la conclusion PRÉMATURÉE (`EDR-BILINEAR…:18-22`). Attrapée en revue, **jamais rattachée au registre**. **(2) BILINEAR Tâche 3 — l'occurrence GELÉE** : le nul 2-pas SUBSISTE crédit réparé (bilinéaire **0.178** SOUS plain **0.218**, tous deux sous `bar = 1/6+0.15 ≈ 0.317`, à `lr=0.02`) et devient la conclusion « le confond dominant était la RÉTENTION ». Elle était FIGÉE par `test_bilinear_composition_null_under_retention_supervised` (`assert r["bilinear_median"] <= bar` et `assert not r["unlocked"]`, au SEUL `lr=0.02`) : **le cliquet de calibration VERROUILLAIT l'artefact au lieu de l'attraper** — toute correction du réglage aurait fait ÉCHOUER ce test. Doublon d'**E14**. **RÉSOLU dans la passe du 2026-09-01** : la clause a été RE-MESURÉE sur SA PROPRE sonde (n=12, pas inférée par analogie) — `lr=0.02` → bilinéaire **0.1789** sous plain 0.2180, `unlocked=False` (reproduit le nul de 2026-08-03 au chiffre près) ; `lr=0.002` → bilinéaire **0.3797** > bar, `unlocked=True` ; séparation par-seed TOTALE (max 0.2016 < min 0.3500, **0/144**, 12/12). La clause « le confond dominant était la RÉTENTION » **n'est donc PAS établie** : elle mesurait le réglage. Le test a été renommé `..._is_lr_dependent` et **gèle désormais la BASCULE** au lieu du nul — le verrou d'erreur est devenu la garde. ⚠️ Ne PAS sur-lire : 0.3797 reste loin du 0.932 du régime 1-pas et à peine au-dessus d'une barre elle-même douteuse — le 2-pas n'est pas « résolu », c'est le NUL qui n'est pas robuste. **(3) 2026-09-01, RETAIN-COMPOSE — le verdict d'un record ENTIER** : `run_retain_compose_diagnostic_probe` (fonction CALIBRÉE), `episodes=600`, `n_agents=16`, `K=6`, n=12, `bar=0.3167`, **seule variable changée = `lr`** — 0.02 → `same_tick` 0.969 / `oracle` 0.971 / `learned` **0.173** (verdict `RETENTION`, reproduit le record au chiffre près) ; 0.002 → 0.937 / 0.945 / `learned` **0.923** (`INCONCLUSIVE`). **Séparation TOTALE** : min(lr=0.002) = 0.897 > max(lr=0.02) = 0.192, **0/144** chevauchement, 12/12 seeds au-dessus de la barre à lr=0.002 (test de signe p = 2⁻¹²) ; l'écart `learned`↔`oracle` passe de **0.798 à 0.022**. **Cause racine** : `n_agents=16` n'est PAS un minibatch — chaque agent porte ses PROPRES `W/U/V/W_bl` (`src/agents/backend_torch.py:85-86`, `:113-115`), donc `F.cross_entropy` sur 16 lignes donne **exactement 1 exemple par jeu de paramètres** (batch effectif = 1) sous Adam `lr=0.02` (`tools/retain_compose_diagnostic_probe.py:80`, signature `:101`). `same_tick` et `oracle` sont des problèmes à UN SEUL `_step` (bien conditionnés) et tolèrent ce pas ; `learned` enchaîne DEUX `_step` avec BPTT et diverge. Les deux contrôles calibrés de la sonde (`test_retain_compose_same_tick_composes`, `:1658` ; `test_retain_compose_decorrelated_oracle_is_floor`, `:1668`) sont TOUS DEUX des conditions à un seul `_step` : aveugles PAR CONSTRUCTION. Corroborants (agent, **NON RÉPLIQUÉS**) : le key est décodable linéairement à **1.000** depuis l'état porté `H1[:, 59:]` **dès l'init** (il était TOUJOURS retenu) ; une chirurgie constructive résout le 2-pas à **0.969** ≈ oracle (capacité prouvée PAR CONSTRUCTION), et la ré-entraîner à `lr=0.02` la **DÉTRUIT** (0.977 → 0.272) là où `lr=0.002` la tient (0.981) **(4) 2026-09-07, P2.15 — LE MÊME DÉFAUT SUR L'OPTIMISEUR DE *RECHERCHE*, pas d'apprentissage.** Un « plafond structurel en forme close » de 0.3889 a été publié le 2026-09-02 et cité dans SIX documents comme preuve que le substrat plain est PROUVABLEMENT INCAPABLE de composer. C'était le plancher d'une descente de gradient trop courte : 14/36 est un optimum local BANAL de cette surface (distribution mesurée des optima locaux : {14:1, 15:1, 16:1, 17:3, 18:2, 19:3, 20:2, 21:4, 24:1, 25:4, 26:2, 27:6}), et avec 8 restarts on y tombe typiquement. **L'aggravant structurel d'E19 est présent à l'identique** : les deux contrôles appariés de la mesure — table LIBRE → 1.000, forme close sur cible SÉPARABLE → 1.000 — vivent tous deux dans le régime FACILE et ne PEUVENT pas voir un sous-apprentissage sur le problème DUR. Reproduit à dessein : à 2 restarts × 60 pas, les deux valent 1.000 pendant que le plafond lit 0.278. **Mesure de l'écart, sur une réponse EXACTE connue** : sur la sous-forme additive, dont le MILP prouve 27/36 = 0.75 (gap 0), la descente de gradient ne trouve que 0.3611 — et un chercheur indépendant y sous-estimait de 13 cellules. **Conséquence publiée puis rétractée LE JOUR MÊME** : j'ai d'abord écrit « la conclusion d'EDR-BILINEAR tient, marge 0.084 » en adossant une affirmation d'impossibilité à un MINORANT. Trois recherches indépendantes ont ensuite rendu 29/36, 34/36 et 36/36 par ordre d'effort — un plafond qui monte avec l'effort est le PLANCHER DE L'OPTIMISEUR. **Règle** : un plafond obtenu par RECHERCHE ne peut jamais fonder un « X ne peut pas » ; seule une borne SUPÉRIEURE PROUVÉE le peut. **Garde armée** : `run_bilinear_composition_probe` rend `unlocked=None` + `bar_status="CEILING_IS_MINORANT"` tant qu'aucune borne prouvée n'est déclarée (`ceiling_is_proven=True`). **(5) 2026-09-08, P3.3 — LA MEME RACINE, AU NIVEAU DE L'AGREGATION.** Sept derivations INDEPENDANTES du meme plafond (3 agents de derivation, 3 refutateurs, moi), chacune par sa propre methode : **0.8056 · 0.8333 · 0.8611 · 0.9444 · 0.9444 · 1.000 · 1.000** — soit 29/36 a 36/36, SEPT cellules d'etendue sur une question a reponse unique. La grandeur est un EXTREMUM (un maximum sur un espace de recherche), donc CHAQUE estimation est un MINORANT et chaque recherche sous-dimensionnee tire le consensus VERS LE BAS. La mediane rend 0.9444 — au moins deux cellules sous le meilleur resultat obtenu — et le presente avec l'assurance d'un accord entre sept sources. **Regle : un extremum s'agrege par EXTREMUM, jamais par consensus.** ⚠️ Regle SPECIFIQUE, pas generale : pour une moyenne de population bruitee, la mediane reste le bon agregateur (positif apparie dans le test). ⚠️ Corollaire mesure sur le JUGEMENT, de signe OPPOSE : les 3 refutateurs ont tous conclu `refute=False` avec `confiance=haute` — unanimite parfaite. La variance INTRA-architecture disperse une MESURE, pas un JUGEMENT ; et la complementarite des 4 defauts trouves etait CONÇUE (angles assignes dans le prompt), pas emergente. **(6) 2026-09-08, P3.3 — L'ANCRAGE A EMPECHE UNE CORROBORATION FABRIQUEE.** Deux chemins de REPLICATION independants ont ete tentes sur le plafond du substrat plain, chacun avec un ancrage OBLIGATOIRE sur une reponse EXACTE connue (27/36, MILP gap 0) evalue AVANT tout rapport. Les deux ont ete RECALES : v1 (espace circulant) plafonne a 6/36 = le HASARD ; v2 (`differential_evolution` + `SLSQP`, 78 dims, 2 h 27) rend **11/36**. **Sans l'ancrage, v2 aurait rapporte « un chemin independant trouve 11/36 »** -- lu comme une corroboration que le plafond est BAS, c.-a-d. l'exact contraire de la verite, et une resurrection du `0.3889` rétracté la veille. **Corollaire qui touche TOUTES les estimations, la mienne comprise** : sur la sous-forme dont l'optimum EXACT est connu, la descente de gradient trouve 14/36 et DE+SLSQP 11/36, contre 27/36 prouve -- AUCUNE methode numerique testee n'atteint l'optimum du cas le PLUS FACILE. Les sept estimations du plafond de la forme complete etant toutes numeriques, le minorant gele (34/36) est probablement tres sous-estime. **Regle : un instrument de replication se calibre sur une reponse connue AVANT de rapporter, et un refus de rapporter vaut mieux qu'un chiffre.** Contre-exemple gele : `test_the_CIRCULANT_construction_is_WORTHLESS_and_that_is_why_ANCHORS_matter`. **(2026-09-14, [[EDR-CALIB-LEARNER]] — LA DOSE EST UN REGLAGE)** : nul de crédit publié à ~48 mises à jour par agent, apprentissage établi à 2250 (12/12 seeds) ; l'écart résiduel à l'oracle est INVARIANT au pas (`assert_verdict_invariant_to_optimizer` appliqué à l'in-world pour la première fois). Et sur le même run, un biais de SURVIVANTS corrélé au bras (v1 à recharge d'énergie seule) fabriquait un avantage des variantes (×0,05 : 0,390 → 0,264 à cohorte complète) — un réglage d'IMMORTALITÉ validé sur les bras qui ne meurent pas. **(2026-09-14, un « mur » du dépôt — [[EDR-LOCK-002]])** : la quatrième manifestation de [[EDR-LOCK-001]] (« à D = 2 la rétention ne s'apprend pas là où D = 0 s'apprend, lr = 0,002 ») était un nul à PAS FIXE. Barreau 1 de P4.5, règle scellée avec une clause E19 (second lr sur le barreau du haut) : à 0,002, 4× plus d'épisodes ne changent rien et le CONTRÔLE est dégradé ; à **0,0005**, 3/3 puis **12/12 seeds** apprennent (0,814 / 0,168, ratio 4,85, PRESENT X_DECOY, alias SURGICAL). Le record disait « ni la capacité, ni le pas ne suffisent » : le pas n'avait pas été balayé. Sans la clause scellée, la branche eût été NE_PERCE_PAS et le levier « épisodes » déclaré épuisé — le nul se serait gravé une seconde fois. **(7, 2026-09-16, lr/B — le pas NOMINAL n'est pas le pas APPLIQUÉ)** : sous torch in-world, `TorchPopulationModel` porte un W (B, N, N) DISJOINT par agent, mais `_td_update` MOYENNE actor_loss et critic_loss sur B (`backend_torch.py:219-221`) et optimise par SGD (`:117`), où le 1/B ne s'annule pas (il s'annulerait sous Adam). Chaque agent reçoit donc **lr/B** : « 0,04 » à B = 12 vaut 0,0033 par agent, « 0,004 » vaut 0,00033. Aucun record ne le disait, et `count_learning_events` posait `LR_ACTOR = lr` (legacy, PAR agent) ↔ SGD `lr` (torch, /B) comme ÉQUIVALENTS (`learning_events.py:189-192`) : le couple E19 {0,04 ; 0,004} de P1.6 et la phrase « le legacy à 0,004 bat torch à 0,04 » de CALIB-LEGACY comparent 0,004 à 0,0033 — un facteur 12 entre deux backends « à lr égal ». Trouvé par la critique de complétude du 2026-09-16 (aucun des 7 lecteurs ni des 10 sceptiques ne l'avait vu) ; **vérifié par PRÉDICTION**, pas par lecture : le même agent, en population de 1 et de 12 copies, sous la même transition, bouge 12,000× moins à B = 12. | `exécutable` | **Formulation de la garde — elle porte sur l'ÉCART, pas sur un SEUIL** : un nul de CAPACITÉ doit conserver son **écart au bras de RÉFÉRENCE** sur au moins une décade de `lr` ; **flaguer si le balayage referme l'écart de plus de 2/3 — JAMAIS « si un bras franchit une barre »**. La spécificité est MESURÉE, pas supposée : le nul STRUCTUREL de BILINEAR (plain prouvablement incapable de composer — plafond en forme close **0.3889** — ⚠️ **SUPERSÉDÉ le 2026-09-07 : 30/36 ≈ 0.833 mesuré, 27/36 = 0.75 PROUVÉ pour la sous-forme séparable** ; le « contrôle positif sur table libre = 1.000 » cité ici est justement celui qui NE POUVAIT PAS attraper le défaut, puisqu'il innocente l'optimiseur sur un paysage trivial sans rien dire de la forme contrainte — cf. occ. 3 d'E1) **franchit la barre** `1/K+0.15 = 0.3167` à **3 pas balayés sur 7** (`lr ∈ {0.05, 0.1, 0.2}` → 0.3625 / 0.3719 / 0.3391), et même au pas d'origine avec 8× de budget (`lr=0.02, episodes=2400` → 0.3703, 3/3 au-dessus) — il reste pourtant un VRAI nul : son écart au bilinéaire (0.966) n'est jamais refermé (**0.577** au meilleur pas). Un critère de seuil absolu flaguerait donc ce nul réel ; le critère d'écart discrimine parfaitement — l'artefact RETAIN-COMPOSE referme le sien à **97 %** (0.798 → 0.022), le nul BILINEAR en garde 0.577. Garde écrite dans la MÊME passe : assertion de pré-vol (`tools/experiment_preflight.py`, convention `assert_<propriété>`) + **contre-exemple GELÉ** = la configuration exacte de l'occ. (3) (`conditions=("learned",)`, `lr` 0.02 vs 0.002, bascule de verdict + séparation par-seed), et la déclaration `"run_retain_compose_diagnostic_probe": ["*"]` (`test_instrument_calibration.py:163`) remplacée par **une branche déclarée PAR RÉGIME**. **Règle d'usage** : quand deux bras d'un verdict n'ont pas la même difficulté d'OPTIMISATION, le réglage n'est pas une nuisance mais un **FACTEUR** — le balayer fait partie du contrôle positif. ⚠️ **Ni E2** (le bras POUVAIT réussir : la chirurgie constructive le résout à 0.969), **ni E4** (la calibration n'est pas VIDE — elle mesure réellement quelque chose — elle est HORS RÉGIME), **ni E6** (ce n'est pas la GRANDEUR mesurée qui change de régime, c'est le RÉGLAGE, qui n'a jamais été validé dans le sien) · P3.3 : contre-exemple GELE `test_aggregating_estimates_of_an_EXTREMUM_by_CONSENSUS_is_WRONG` **(2026-09-09, la MÊME forme appliquée non plus au RÉGIME mais à la RÉGION DE CODE — et elle vise le cliquet de calibration lui-même)** : `tools/hcm_analyzer.py::run_hcm_analysis` était **DÉCLARÉ CALIBRÉ** et **ne pouvait pas s'exécuter**. `load_hall_of_fame()` rend `(version, entries)` ; la ligne 29 faisait `[g for g in hof if g[1]...]`, d'où `TypeError: 'int' object is not subscriptable` à **chaque** appel. Ses deux cas déclarés — `empty-cohort:raises` et `guard-before-world` — n'exercent que la **garde d'arguments posée en tête**, qui lève AVANT la ligne 29 : la calibration passait sans jamais entrer dans le corps. C'est l'aggravant structurel d'E19 mot pour mot, transposé : les contrôles vivent tous dans la région d'entrée, donc **aucun ne PEUT voir** la pathologie du corps. **Exposition MESURÉE, et c'est le chiffre qui compte : 92 des 248 déclarations (37 %) n'ont AUCUN cas atteignant le corps** — elles sont calibrées uniquement par la famille garde (`*:raises`, `guard-before-world`). Ce n'est pas dire qu'elles sont cassées ; c'est dire que leur certification ne porte que sur leur porte d'entrée. Origine : la **7ᵉ passe d'élargissement** (famille `run_*`, 72 fonctions) a été fermée « par garde d'en-tête » — technique légitime pour le coût, mais qui ne certifie rien au-delà de la garde, et CLAUDE.md le disait déjà pour les orchestrateurs sans en tirer la conséquence générale. ⚠️ **Le défaut n'était visible qu'après le 9ᵉ élargissement de PÉRIMÈTRE** (`src` entier + les `.py` de la RACINE) : c'est en cherchant la dette d'un axe (OÙ le cliquet cherche) qu'on a trouvé celle d'un autre (ce que ses cas ATTEIGNENT). Dette réelle révélée par ce 9ᵉ élargissement, comme aux huit précédents : **5 sites cassés par le même contrat** (`multiverse_runner.py`, `src/environments/biosphere.py`, `src/worlds/world_0_soup.py` — ces deux derniers sur un chemin **ATTEIGNABLE** du moteur, agent sur le trésor avec `out_accept > 0` —, plus les deux `hcm_analyzer`), dont un où l'`except Exception` **avalait la TypeError en imprimant « Erreur lors du chargement de KuzuDB »** : un diagnostic FAUX qui envoie chercher la cause à l'opposé, et la soupe primordiale partait sans ancêtre en accusant la base. Plus une **garde INERTE** (classe E1) : `if not hof` sur un 2-uplet est toujours vrai, donc la garde de cohorte vide ne pouvait pas lever. Tout corrigé dans la même passe, cliquet à **214 détectés / 209 calibrés / 0 dette**. Contre-exemple GELÉ : `tests/sandbox/test_perimeter_widening.py::test_the_HEAD_guard_alone_CANNOT_certify_the_body` (l'ancienne forme rejouée sur un retour CONFORME au contrat, appariée à la démonstration que la garde d'en-tête lève plus tôt et la masque), plus `test_the_measured_EXPOSURE_of_head_guard_only_calibration_is_PUBLISHED` qui **recompute** le compte pour que personne ne puisse croire la dette résorbée sans l'avoir résorbée. ⚠️ **ET LA DETTE S'EST AGGRAVÉE LE JOUR MÊME, ce qui est le fait le plus instructif de la passe** : le **10ᵉ élargissement** (le VERBE NU — tous les motifs exigeaient un suffixe, donc `compare` seul, qui rend `compute_ab_verdict` dans 10 fichiers avec les docstrings « verdict de learnabilité » et « verdict de survie », était invisible) a fait entrer **+7 noms / +22 définitions**. Les 20 vrais instruments ont été fermés par **garde d'en-tête**, faute d'un cas CORPS-ATTEINT abordable — il y coûte un monde. Compte recomputé le soir même : **114/272 (42 %)**, contre 92/248 le matin. On ne peut donc pas fermer cette classe en élargissant : chaque élargissement honnête l'aggrave tant que la technique de fermeture reste la garde d'entrée. C'est écrit dans `CALIBRATED` à côté des 20 déclarations concernées, et non masqué. **(2026-09-15, l'apprenant de TOUT l'arc EVO)** — P3.4/P2.72 : `MambaBatchModel.compute_policy_gradient` tourne à `lr_actor = 0,04` depuis l'origine, un pas jamais balayé. Balayé à n = 12 sur la tâche linéaire de S2-011 (cohorte immortelle) : 0,04 → 0,249 avec **4 seeds effondrés à 0,000 et 1 NaN** ; 0,01 → 0,303 ; 0,004 → 0,446 ; **0,001 → 0,518, 12/12, 0 effondré**. Monotone dans le sens du pas, plafond non atteint. Le pas publié est le plus mauvais des quatre, et il est instable ; tout nul de l'arc EVO lu sur cet apprenant à n = 1 est un tirage dans une distribution bimodale. Garde : `learner_verdict` + `assert_verdict_invariant_to_optimizer` (P1.6) s'appliquent désormais au legacy (`policy="legacy"`, compteur de dose) ; records [[EDR-CALIB-LEGACY-LEARNER]] (+ addendum courbe), règles `LEGACY-LR-CURVE-R1`. **Occ. 7 (lr/B)** : `TorchPopulationModel.effective_lr_per_agent` (= lr/B, publié) et `count_learning_events().summary()["lr_effective_per_agent"]` + `lr_effective_unit` à côté du `lr` nominal, dans tout bloc `learning` ; contre-exemple `tests/sandbox/test_torch_effective_step.py` (ΔW(B=1) = 12·ΔW(B=12) composante par composante, ratio des sommes 12,000 ; un `.sum()` ou un passage à Adam le rougit). |
| **E20** | **Crossover à ordre fixe : l'estimand est le PRODUIT facteur × carry-over** — quand le facteur manipulé (ici la POSITION d'une fenêtre) est confondu avec le TEMPS, aucun contrebalancement n'est possible (l'ordre EST le facteur) : tout test within-sujet mesure r_facteur × r_carry-over, jamais r_facteur. L'équivalence est OBSERVATIONNELLE (un carry-over pur k produit exactement les mêmes marges et la même distribution des discordants qu'une dépendance r=k — aucune statistique sur les sujets ne les sépare), et un « contrôle carry-over » à bande tolérante ne peut pas échouer à la taille d'effet testée (calculé : P(faux verdict)=0,858-0,946 selon design, contrôle muet). | 1 (2026-09-02, panel EVO-028 : les 3 juges ont convergé INDÉPENDAMMENT vers un crossover within-seed — le réfutateur les a tués tous les trois par la même identité, AVANT tout run ; spec `docs/superpowers/specs/2026-09-02-evo028-weak-position-design.md` §1.1). La règle scellée doit nommer son CONTRASTE IDENTIFIANT, et pour un facteur confondu avec le temps ce contraste est BETWEEN. | `documenté` | contre-exemple gelé = les 3 designs du panel (spec §1.1, chiffres exacts reproduits 2×). Candidat de promotion si récidive : `tools/preregister.py` exige un champ `contraste_identifiant` pour tout design within à fenêtres ordonnées. |
| **E22** | **Une suppression de test rend la suite PLUS VERTE** — une réécriture programmatique non ancrée (regex, sed, script de refactor) efface des tests SANS lever, et le signal habituel s'améliore : rien ne peut alerter. Un test disparu est une garde **désarmée en silence**. | **(1, 2026-09-07)** retirer un décorateur `@pytest.mark.xfail` devant quatre fonctions nommées : le motif était ancré sur `@pytest.mark.xfail` **sans ancrage de début**, donc `re.search` a démarré au PREMIER décorateur du fichier et effacé tout ce qui l'en séparait. **Quatre tests supprimés, aucune exception.** La suite est passée de « 28 passés, 15 xfail » à « 32 passés, 7 xfail » — plus verte qu'avant. Seul un RECOMPTE par fichier l'a révélé ; les quatre tests ont été récupérés depuis les transcripts d'agents, puis re-vérifiés. ⚠️ **Ce qui distingue cette classe d'E21** : là, le texte mutilé se voyait à la lecture (fragments vides) ; ici, l'artefact reste parfaitement valide, s'exécute, et rend un MEILLEUR résultat. La seule mesure qui discrimine est un **compte tenu hors de l'artefact modifié**. **(2, 2026-09-09, MÉCANISME NEUF — l'ORDRE D'ÉVALUATION DES ARGUMENTS, et la garde censée l'attraper a rendu OK)** : `docs/roadmap/PRIORITES_ET_DETTES.md` est passé de **2352 lignes à 0** sans qu'aucune exception ne soit levée, et le fichier vide a été **committé** (`3e96c7d`, 2352 suppressions). La forme fautive tient en une ligne : `io.open(p, "w").write(io.open(p).read().replace(old, new))` — Python évalue les arguments de **gauche à droite**, donc `open(p, "w")` **TRONQUE** le fichier *avant* que le `read()` interne ne le lise ; le read rend `""`, le replace rend `""`, et le fichier est écrasé par du vide. Aucune erreur, aucun avertissement. ⚠️ **Et le pire est en aval** : `tools/check_backlog_freshness.py` a rendu **`exit 0` et « OK »** sur ce backlog VIDE, puis a **invité à resserrer sa baseline** (« 2 résorbée(s) → `--update-baseline` ») — ce que j'ai fait, ce qui aurait figé l'amputation et effacé pour de bon la trace des péremptions légataires. **Entrée vide → succès, commis par une GARDE** : le biais systématique que ce dépôt traque chez ses sondes, retourné contre l'un de ses cliquets. Les **12 portes** du hook sont toutes passées sur ce fichier vide. Détecté par un seul chiffre — `2372 deletions` dans la sortie de `git commit`, lue par réflexe. **Garde armée** : `compter_entrees()` + un `plancher_entrees` gelé dans la baseline, posé **AVANT** `--update-baseline` (geler contre un fichier amputé figerait l'amputation) et qui ne peut que **MONTER** (`max` avec l'ancien). ⚠️ **La PREMIÈRE version de cette garde était elle-même INERTE** : elle lisait `_load_baseline().get("plancher_entrees")`, or `_load_baseline()` rend le sous-dictionnaire `legataires` — le plancher valait donc **toujours 0** et la garde a passé son propre contre-exemple (backlog vide, exit 0). Classe **E1**, commise en armant une garde contre exactement ça ; seul le contre-exemple GELÉ l'a dit, la relecture ne l'avait pas vu. Contre-exemples gelés : `tests/sandbox/test_backlog_freshness.py::test_the_ratchet_REFUSES_an_amputated_backlog` (l'incident rejoué), apparié à `test_the_ratchet_SPARES_an_intact_backlog` (sans lui, une garde qui refuse TOUT passerait le premier), plus `test_the_FLOOR_can_only_go_UP` qui vérifie sur le code que la garde précède `--update-baseline`. **Règle qui en sort, et elle est mécanique** : ne jamais imbriquer une lecture dans l'appel qui ouvre le même fichier en écriture — LIRE dans une variable, transformer, puis ÉCRIRE ; et faire porter à toute réécriture de fichier une assertion de TAILLE avant d'écrire. **(3, 2026-09-09, TROIS HEURES PLUS TARD, et c'est la règle elle-même qui est prise en défaut)** : `s = s[:i] + NOUVEAU` — un découpage par INDEX. Il LIT bien dans une variable (la règle est respectée) et jette pourtant **toute la queue du fichier** : **4 tests supprimés** de `test_orchestrator_injection.py`, et la suite est restée **VERTE** (77 passés) parce qu'une suite ne compte pas ce qui manque — le cœur d'E22. Détecté par un `grep -c '^def test'` comparé à `git show HEAD:fichier`, c'est-à-dire un compte pris **HORS de l'artefact modifié** ; `check_test_census` l'aurait bloqué au commit, mais trois heures plus tard. ⚠️ **Ce que la récidive corrige dans la règle** : une assertion de TAILLE ne suffit pas — un découpage par index peut retirer 4 tests sans changer notablement la taille. Ce qu'il faut asserter est un **COMPTE DE L'ENTITÉ** (nombre de `def test`, d'entrées, d'arêtes), **>= au compte d'avant**, pris hors de l'artefact. La règle du matin était énoncée sur le CAS (`open(p,"w")` imbriqué) et non sur le MÉCANISME (toute réécriture programmatique peut amputer sans lever) — la faute même que ce registre reproche à E21 occ. 2. **(occ. 2026-09-24 — LA VARIANTE QUI NE LAISSE AUCUNE TRACE : détruire du travail NON COMMITTÉ d'une AUTRE session, sans même laisser un compte de suppressions à lire)** : mon script de lot alignait le disque APRÈS le succès du commit en y **écrivant son BLOB**. Or le blob vaut `HEAD + mes transformations`, tandis que le disque vaut `HEAD + les miennes + CELLES DES AUTRES` : la troisième part est perdue. Mesuré par la session victime : l'entrée `P2.110` qu'elle avait écrite à 13:00 est absente du disque, de l'index **et de tout commit** (`git log --all -S` : zéro) ; le backlog sur disque était redevenu IDENTIQUE à HEAD, de mtime **15:49:50.684** = date d'auteur de mon commit `3ee39e33`. ⚠️ **Ce qui fait la variante, et pourquoi elle est pire** : E22 décrivait jusqu'ici une amputation DANS un commit, où il reste au moins un compte de suppressions à lire — c'est exactement ainsi que l'anéantissement du backlog avait été découvert. Ici le contenu détruit n'a **jamais** fait partie d'un commit : pas de diff, pas de compte, pas de test manquant, **rien à lire**. La porte 16 juge un COMMIT ; elle est structurellement aveugle à ce cas, et aucune autre porte ne le voit. Seule la session victime pouvait s'en apercevoir, en cherchant son propre travail. ⚠️ **Et c'était une CORRECTION.** Les scripts précédents faisaient `écrire(transformer(disque))` : on part du disque, donc les hunks d'autrui survivent. J'ai différé l'écriture APRÈS le commit et je l'ai faite depuis le blob, précisément pour ne pas bloquer la flotte via la porte 8 qui juge le DISQUE (**P2.85**, subie deux fois le même jour). J'ai corrigé une moitié du problème et cassé l'autre : différer évite de BLOQUER, écraser par un blob DÉTRUIT. **Forme correcte** : différer l'écriture ET re-appliquer ses transformations au disque COURANT relu juste avant — jamais reconstruire le fichier depuis son blob. ⚠️ **La garde qui manquait était l'assertion SYMÉTRIQUE de celle que ces scripts portent déjà** : ils assertent qu'aucune ligne de HEAD ne disparaît, il fallait asserter qu'aucune ligne du DISQUE COURANT ne disparaît. C'est la même leçon que l'occurrence précédente — une règle énoncée sur le CAS (`git show HEAD:fichier` comme référence) et non sur le MÉCANISME (toute référence d'assertion doit être l'état qu'on va ÉCRASER, pas celui dont on est parti). Dette **P2.112**. ⚠️ **PORTÉE ÉTENDUE le même jour : P2.110 n'était pas la seule perte, et la cause n'est PAS identifiée.** La session autrice d'E31/E32 a mesuré la disparition de DEUX blocs de plus — un bandeau à l'intérieur de P2.108, un autre à l'intérieur de P2.109 — jamais committés, donc **invisibles à tout `git log -S` par construction** : chercher leur texte dans l'historique rend zéro avant même de chercher. Elle les a réappliqués depuis les scripts qui les avaient produits. ⚠️ **L'écriture destructrice n'est pas identifiée** : la mienne de 15:49:50 depuis un blob est CANDIDATE et non établie — elle ne peut pas dater ses ajouts, je ne peux pas dater leur disparition, et un mtime nomme le DERNIER écrivain, jamais le destructeur. Inscrit comme candidat à sa demande explicite. ⚠️ **Et j'avais répondu « rien d'autre n'est perdu » sur une mesure qui visait le mauvais OBJET** : elle parlait d'ajouts À L'INTÉRIEUR des entrées, j'ai mesuré l'existence DES ENTRÉES. L'instrument était correct, sa cible non — troisième variante d'**E33** chez moi le même jour, et la plus bête, puisque le zéro était garanti d'avance. ⚠️ **L'AUTRE FACE, et elle est exécutable : la garde d'empreinte est ASYMÉTRIQUE.** Mesuré par elle pendant l'incident : `tools/check_staged_authorship.py` rend par `verify()` **8 hunks étrangers** sur le backlog et **2** sur ce registre, *pendant que ses propres hunks avaient disparu de ces deux fichiers* — aucune des deux sorties ne distingue « on a ajouté chez moi » de « on m'a RETIRÉ ». Elle détecte l'intrus, pas la perte : même cécité que la porte 16, qui juge un COMMIT et ne voit rien de ce qui n'y est jamais entré. La dette **P2.112** doit donc couvrir les DEUX sens, pas seulement l'écrasement. ⚠️ **TROISIÈME VECTEUR, même outil, même jour** : le commit par index temporaire n'avance pas l'index PARTAGÉ, qui garde l'ancienne version de chaque chemin committé — mesuré, l'index portait ADR-005 sans l'amendement du critère de révision (131 lignes contre 152), et un commit nu l'aurait installé, donc aurait SUPPRIMÉ du travail publié. Remède d'une ligne, inscrit en **P2.109** : un reset de l'index sur les chemins committés, à la fin de la procédure. ⚠️ **La racine des trois est une** : cette technique est ISOLÉE par construction, ce qui est sa vertu, mais isolée veut dire qu'elle ne propage rien vers les deux autres états qui comptent — le DISQUE et l'INDEX PARTAGÉ. Lire HEAD plusieurs fois annule les commits d'autrui ; écrire le disque depuis son blob détruit leurs hunks ; ne pas rafraîchir l'index arme un piège contre HEAD. Trois vecteurs, un seul oubli : faire avancer avec HEAD tous les états qu'on partage. **(occ. 2026-09-25 — une suppression casse DEUX citations du même témoin, et UNE SEULE est vue ; mesurée par la session PM, vérifiée avant inscription)** : le commit `a49f839c` (2026-09-24 17:24) a SUPPRIMÉ le témoin « test_un_temoin_dont_SEUL_l_arbre_differe_ne_desarme_PAS » de `tests/sandbox/test_hook_on_merge.py` — « le témoin est SUPPRIMÉ plutôt que raffiné une quatrième fois ». Ce témoin était cité à DEUX endroits, tous deux écrits par `3e26caf2` (14:54) — `git log -S` rend exactement ces deux commits : (1) dans la colonne Garde d'**E31** de ce registre ; (2) à la ligne 129 de `tests/sandbox/test_guard_negative_cases.py`, dans le test qui vérifie qu'une citation qualifiée est reconnue. La première a été vue en QUELQUES HEURES : une porte contrôle les citations du registre à CHAQUE commit, et elle a bloqué toute la flotte jusqu'à réparation. La seconde est restée rouge **25 heures** sans qu'aucune porte ne la voie : un test ne tourne sous la porte 15 que si les fichiers de SA porte sont stagés, et aucun commit ordinaire ne stage le harnais — c'est une fusion, en stageant le lanceur de mutations, qui a fait muter TOUTES les portes et l'a rencontré. Correctif : `8988ac8b`, citation repointée vers « test_un_temoin_ETRANGER_ne_desarme_PAS_une_fusion_fautive » du même fichier, 12 témoins de la porte 3 verts ensuite. ⚠️ **Et la CI ne pouvait pas le voir non plus** : le job qui lance la suite complète s'interrompt à la collecte — vérifié sur deux runs, dont celui de `5e4b3e32` : « Interrupted: 8 errors during collection », **zéro test exécuté sur ~2 600** — parce que pytest arrête TOUTE la session à la première erreur de collection et que le workflow ne passe pas l'option qui l'en empêcherait. Les huit erreurs avaient UNE SEULE cause, torch absent de l'environnement CI : six modules importent `FlatModel`, défini dans `tools/disjoint_heads_ab.py` sous une garde « if torch is not None », donc inexistant sans torch ; les deux autres importent des modules de `tools/` qui importent torch — la même cause, un niveau plus bas. Réparé par `b895a0a3` ; preuve locale mesurée par la session qui l'a livré : torch rendu inimportable, les huit modules rendent « 8 skipped, 0 erreur », et avec torch 58 passent. ⚠️ Cette phrase disait d'abord « une classe retirée par une fusion de juillet » : c'était FAUX, corrigé le 2026-09-26 — cf. **E33** occ. 7. Dernier succès de la CI sur cette branche : le 2026-09-09 ; 21 runs rouges depuis le 14. Un job déjà rouge pour une raison connue MASQUE toutes les suivantes. ⚠️ **Ce que l'occurrence ajoute à la classe** : E22 décrivait une suppression qui rend la suite plus VERTE ; ici elle rend un test ROUGE, et le rouge est invisible parce que le seul lanceur qui l'exécuterait ne tourne plus. Une citation qui vit dans un TEST n'est protégée que si ce test tourne. | `exécutable` | `tools/check_test_census.py` (porte 10 du hook) — baseline gelée `tools/test_census_baseline.json` (221 fichiers, 1785 tests), la baseline déclenche la garde elle-même (E4 occ.5), `--only` pour l'arbre partagé, et le filtre inclut `D` sinon la SUPPRESSION d'un fichier — le cas le plus grave — serait le seul non vu. Contre-exemple gelé : `tests/sandbox/test_test_census.py::test_CONTRE_EXEMPLE_la_reecriture_regex_du_2026_09_07_est_ATTRAPEE` (la réécriture fautive rejouée telle quelle), apparié à son no-op EXACT `test_CONTRE_EXEMPLE_la_reecriture_ANCREE_ne_declenche_RIEN` (même intention, correctement ancrée → le cliquet se tait). Limite ASSUMÉE et écrite : il compte des `def test_`, pas des assertions — un test vidé mais conservé passerait. ⚠️ **Cette limite est LEVÉE depuis le 2026-09-09** : « le mesurer demanderait du test de mutation, qu'on ne proxifie pas » était juste comme DÉCISION et faux comme IMPOSSIBILITÉ — `tools/check_gate_mutation.py` (porte 15) le mesure, et le fait pour les quatorze portes. **Garde GÉNÉRALISÉE AU MÉCANISME le 2026-09-10** : `tools/check_amputation.py` (porte 16). Les portes 10 et 4 répondaient chacune à UNE occurrence par un cliquet lié à UN artefact (les tests ; le backlog) ; le mécanisme, lui, est indifférent au fichier qu'il détruit — le même accident sur `tools/ablation.py`, sur un record ou sur une baseline JSON n'était couvert par RIEN. La porte 16 BLOQUE l'**anéantissement** (compte d'ENTITÉ tombé à zéro sur un fichier CONSERVÉ, statut `M` : aucun commit légitime ne fait ça, et `git rm` reste hors périmètre) et SIGNALE, sans bloquer, toute baisse partielle — retirer du code mort est légitime et fréquent, et ce qui manquait n'était pas un refus mais **le chiffre sous les yeux**, celui-là même qui a révélé l'occurrence (2). Contre-exemples gelés, un par occurrence : `tests/sandbox/test_amputation.py::test_CONTRE_EXEMPLE_le_backlog_vide_du_2026_09_09_est_ATTRAPE` et `test_CONTRE_EXEMPLE_la_perte_de_QUATRE_tests_est_SIGNALEE_sans_bloquer`, appariés à `test_le_backlog_INTACT_ne_declenche_RIEN` et `test_une_AUGMENTATION_ne_declenche_RIEN`. |
| **E23** | **Une FAMILLE de tests n'est pas traitée comme une famille** — et le défaut est symétrique : la correction de multiplicité peut DISPARAÎTRE (p-hacking silencieux) ou MANQUER là où un contrôle est appliqué à N cellules (fausse alarme). Dans les deux cas rien ne le signale, et dans les deux cas le nombre de tests est connu D'AVANCE. | **(1, 2026-09-07, sens « la correction disparaît »)** `tools/s2_demand.py::run_s2` ITÉRAIT `worlds` DEUX FOIS — la boucle de mesure, puis la famille Holm. Passé en ITÉRATEUR (forme naturelle : `map`, un générateur), la seconde passe était VIDE : aucun `p_monde_holm` écrit, et `_print_table` retombait **en silence** sur le `p_monde` NON corrigé. La correction FWER de la famille des mondes disparaissait sans un mot — exactement le p-hacking que le commentaire du code interdit. Trouvé par injection à dose connue, corrigé (`worlds = list(worlds)`), non-régression `test_run_s2_KEEPS_the_Holm_correction_when_worlds_is_an_ITERATOR`. **(2, 2026-09-07, sens INVERSE — la fausse alarme)** le contrôle (i) du pré-vol EVO-011 appliquait une bande FIXE (0,25 ; 0,75) à une proportion par seed, sur une famille de **24 cellules** (2 types × 12 seeds). Taux de fausse alarme jamais contrôlé ; calcul exact aux n observés, sous un harnais PARFAIT : **0,216** — un dispositif correct échouait une fois sur cinq, et c'est ce qui est arrivé au premier run (`INDÉTERMINÉ-HARNAIS`, gravé). Re-scellé en `-bis` : test binomial exact au seuil de Bonferroni `0,05/24`, FWER ramené à ≤ 0,05 **et** contrôle qui sait encore refuser un brouilleur cassé. ⚠️ **Ce que la paire enseigne** : chercher la multiplicité dans les CONTRÔLES autant que dans les DV. Le dépôt corrigeait déjà ses p-values ; personne n'avait regardé combien de fois ses GARDES pouvaient crier à tort. **(3, 2026-09-07, la MÊME faute, dans l'instrument FONDATEUR)** — trouvée en cherchant systématiquement l'idiome par AST : `tools/s2_cognition_body.py::cognition_body_study` itérait `worlds` deux fois exactement comme `run_s2` (boucle de mesure ligne 45, famille Holm ligne 62), et c'est l'instrument qui porte **S2-012** (`champion_body`, verdict COGNITION/BODY). **Mesuré sur le code de HEAD, pas supposé** : chargé tel quel avec `worlds` en itérateur, il rend les deux mondes SANS `policy_p_holm` et imprime `p=0.0025` — la p-value NON corrigée — sans un mot. Corrigé, plus un test de non-régression qui exige `p_holm >= p`. Le même balayage AST a trouvé 3 sites de la forme « sélection VIDE = TOUT » (`run_ablation_map`, `cognition_body_study`, `s2_fallback_rate_probe.plan` — ce dernier écrit le jour même où le défaut était corrigé ailleurs) : tous corrigés avec leur cas négatif apparié. ⚠️ **La leçon opératoire** : la première occurrence a été trouvée par injection, la deuxième par un run qui a échoué, la troisième en cherchant **l'idiome** sur tout le dépôt. Seule la troisième méthode est exhaustive — après avoir corrigé un défaut, chercher sa FORME partout, jamais seulement son instance. | `exécutable` | Côté DV : la correction de famille est tenue par l'injection à dose connue des orchestrateurs — `tests/sandbox/test_orchestrator_injection.py`, cas `test_run_s2_CORRECTS_the_whole_family_of_worlds_including_the_VOID_ones` et `test_run_s2_KEEPS_the_Holm_correction_when_worlds_is_an_ITERATOR`. Côté CONTRÔLE : `tests/sandbox/test_evo011_preflight.py`, cas `test_e11bis_l_ANCIEN_controle_echouait_une_fois_sur_cinq_sur_un_harnais_PARFAIT` (le taux de fausse alarme se CALCULE en forme close, sans run), apparié à `test_e11bis_le_controle_SAIT_ENCORE_ECHOUER_sur_un_brouilleur_CASSE` — sans ce second, corriger une fausse alarme produirait un contrôle increvable, soit E1. **AUTOMATISÉ le 2026-09-07** — et par DÉCLARATION, pas par proxy : mesuré, un détecteur syntaxique « seuil littéral dans une boucle » trouve 2 sites dans tout le dépôt et **aucune des deux occurrences réelles**, car la famille se forme ENTRE les appels. La garde est donc `assert_control_family` dans `tools/experiment_preflight.py` (rend le seuil par cellule, refuse un seuil non corrigé en NOMMANT la borne de fausse alarme, et exige une raison écrite pour `method='none'`), branchée dans `declare_design` qui REFUSE tout run à n > 1 sans famille déclarée — même forme que la garde E8 juste au-dessus. Une garde d'appel ne protégeant que ses appelants (9 runners scellés sur 12 ne déclaraient AUCUN design, dont celui de la 3ᵉ arête établie), le cliquet `tools/check_control_family.py` (porte 11, détection AST) ferme le trou, baseline `tools/control_family_baseline.json` gelée à 9 légataires. Contre-exemple gelé : `tests/sandbox/test_control_family.py`, cas `test_CONTRE_EXEMPLE_la_garde_aurait_refuse_le_controle_qui_a_echoue` (la bande d'EVO-011 rejouée en forme close), apparié à son no-op exact `test_CONTRE_EXEMPLE_la_garde_ACCEPTE_le_reglage_qui_a_repare_le_run`. |
| **E24** | **Une partie des SORTIES du réseau EST l'entrée** — les blocs d'entrée et de sortie du génome se CHEVAUCHENT, et rien ne le signale : `max_H = max_N − max_I − max_O` devient simplement NÉGATIF. Toute mesure de « la politique lit-elle l'observation ? » est alors confondue sur les slots partagés, et le sujet ne peut pas être aveuglé en annulant des poids. | **(1, 2026-09-08)** le **champion HoF** — sujet de S2-002/003, d'EDR-124 et de tout l'arc « le monde exige-t-il la perception ? » — déclare **64 entrées et 126 sorties dans 172 nœuds**. 64 + 126 = 190 > 172 : **18 nœuds partagés**. `forward` écrit l'observation dans `H[:, :max_I]` et lit les logits d'action à partir de `max_I + max_H` = 46 : **les 18 premiers logits d'action SONT les composantes 46 à 63 de l'observation**, à 0,0025 près (l'écart est la mise à jour `(1−δ)·H + δ·tanh`), vérifié composante par composante. Un agent FRAIS n'a pas le défaut (59 + 108 = 167 ≤ 172) : c'est propre à la lignée du champion — la divergence `num_inputs` 64 ↔ 59 déjà connue, dont personne n'avait mesuré CETTE conséquence. **Trouvé par accident** : la sonde S2-BLIND-CHAMPION croyait aveugler le champion en annulant `W[:num_inputs, :]`, son contrôle a échoué, et la chasse à la cause a éliminé successivement le monde-modèle (inactif : `world_model` vaut `None`), le routeur neuromodulateur et le compilateur NTM — tous mesurés, tous innocentés — avant d'arriver à l'arithmétique des blocs. ⚠️ **Ce que ça n'invalide pas** : `PerceptionAblatedMamba` permute `batch_obs` À L'ENTRÉE, donc il ablate bien les deux chemins ; les verdicts `PERCEPTION_DECOY` publiés tiennent. Ce que ça change est la LECTURE : une saillance nulle mesurée sur une politique dont un dixième des sorties EST l'entrée ne dit pas la même chose. | `exécutable` | `tools/experiment_preflight.py`, fonction `assert_no_io_overlap` — à appeler par toute sonde qui prétend mesurer si une politique LIT l'observation (saillance, ablation de perception, aveuglement) ; elle rend le nombre de slots partagés et LÈVE si > 0, en le CHIFFRANT. Posée dans le pré-vol et non dans `MambaBatchModel.__init__` : faire lever le substrat casserait tout run utilisant le champion, et on ne répare pas un génome déjà publié — on rend son défaut VISIBLE. Contre-exemple gelé : `tests/sandbox/test_control_family.py`, cas `test_le_chevauchement_du_CHAMPION_est_refuse_et_CHIFFRE`, apparié à `test_un_genome_FRAIS_passe_sans_bruit` et au cas LIMITE `test_le_cas_LIMITE_exact_passe` (zéro nœud caché mais aucun slot partagé : la garde porte sur le chevauchement, pas sur la platitude du connectome, qui est légitime). |

| **E25** | **Le test gèle une VALEUR historique au lieu d'un INVARIANT** — l'assertion recopie le chiffre du jour (`>= 100`, `== 0.0`, `== "N_EMERGE_PAS"`) plutôt que la propriété qui doit tenir. Deux conséquences opposées et également graves : soit le test **PUNIT le correctif** (résorber la dette le fait rougir), soit il **GÈLE le défaut** (corriger l'instrument le fait rougir). Dans les deux cas le rouge est ADRESSÉ AU MAUVAIS ENDROIT — et si le fichier n'est lancé par aucun témoin, il dort. | **(1, 2026-09-09, PUNIT LE CORRECTIF)** `test_the_REAL_tree_has_NO_new_site_and_a_NON_EMPTY_baseline` exigeait `len(base) >= 100` alors que sa PROPRE docstring, quatre lignes plus haut, disait qu'un seuil figé « punirait le progrès » ; la dette est tombée de 121 à 85 dans la journée et le test est devenu rouge **parce qu'on l'avait résorbée**. Invariant qui, lui, ne se retourne pas : `1 <= len(base) <= 121` — non vide (sinon garde désarmée) et jamais REMONTÉ au-dessus de la mesure fondatrice (sinon re-gel abusif). **(2, 2026-09-09, GÈLE LE DÉFAUT)** `test_verdict_empty` exigeait `N_EMERGE_PAS` sur **ZÉRO seed** — une affirmation de fond, négative, tirée d'aucune donnée : la forme (a) du biais du dépôt, gelée par le test censé l'attraper. La garde `INDETERMINE_AUCUN_SEED` était posée dans l'instrument depuis le 2026-09-01 (E18/E4) ; le test est resté rouge **huit jours**. **(3, 2026-09-09)** `test_run_curriculum_warmup0_is_compositional` exigeait `warmup_didx_start == 0.0` pour une phase de warmup ABSENTE — le correctif P2.52 rendait `None` (« je ne sais pas ») le jour même. **(4, même famille)** trois fixtures de `tests/test_agi_taxonomy.py` étaient devenues invalides-de-forme après deux durcissements de la porte (M4 puis P2.15) : corrigées **par la FIXTURE, pas par l'attente**, parce que leur intention — « une arête bien formée est ACCEPTÉE » — en faisait les seuls tests vérifiant que la porte sait dire OUI. ⚠️ **Ce que les quatre partagent** : aucun n'était lancé par un job de CI, donc aucun n'a coûté une seconde à qui l'a cassé. | `exécutable` | ⚠️ **La garde ne PRÉVIENT pas la faute — elle BORNE sa durée de vie**, et c'est ce qui manquait : de « indéfinie » à « un push ». (a) le job `suite-complete` de `.github/workflows/ci.yml` lance `tests/` PAR RÉPERTOIRE et non par liste nommée — 1937 tests sur 2059 (94 %) échappaient à toute CI, et c'est en le branchant que les quatre occurrences ci-dessus sont sorties, d'un coup ; coût MESURÉ sur machine à charge connue : 2597 passés, 25 min 44 s. (b) le **contrôle INTACT** de `tools/check_gate_mutation.py` refuse de rendre un verdict de mutation sur une porte dont les témoins ne passent pas — c'est lui qui a fait remonter l'occurrence (1) le jour même. Contre-exemple gelé : `tests/sandbox/test_gate_mutation.py::test_des_temoins_ROUGES_rendent_la_mesure_NULLE_et_le_disent`, apparié à `test_le_harnais_dit_TUEE_sur_une_mutation_qui_CASSE_la_porte` (sans lui, un harnais qui déclarerait tout « témoins rouges » passerait le premier). |
| **E26** | **Le monde dérive le CORPS des mêmes paramètres que la POLITIQUE** — `hp_bonus = 10·Σ|W[0:5]|`, `inv_capacity = max(3, Σ|W[5:10]|)`, `drain = 1 + hp/100 + 0,1·inv` (`src/agents/mamba_agent.py:47-50`) : les lignes d'entrée de `W` portent l'observation ET le métabolisme. Toute édition des lignes d'entrée (annuler, déranger, amplifier) est donc AUSSI une intervention métabolique, et un contraste ENTRE sujets à `W` édité compare des corps avant de comparer des politiques. | **(2026-09-14, P1.7, [[EDR-S2-BLIND-CHAMPION]] et [[EDR-S2-SUBJECT-VARIANCE]])** : `make_blind` annonçait « corps, biais et récurrence restent identiques » — FAUX par construction, le drain du champion tombe de 2,40 à 1,30 (−46 %) ; les contrôles câblés de SUBJECT-VARIANCE (couplage ×3 / ×8) sont morts de leur corps (drain 11,0 / 15,0), pas de leur couplage. Les DEUX records arrêtés du 2026-09-08 sont des artefacts candidats ; leurs `-bis` passent par un sujet LESTÉ. | `exécutable` | `assert_phenotype_matched(subject, reference, tol=0)` + `ballast_phenotype` (`tools/experiment_preflight.py` ; lest EXACT par la diagonale des nœuds d'entrée, canal inerte sur la politique — prouvé bit-identique ; REFUSE le chevauchement E24 et tout corps à réduire). **Contre-exemple GELÉ** : `test_assert_phenotype_matched_REFUSES_make_blind` (`tests/sandbox/test_phenotype_guard.py`), apparié à `test_ballast_is_bit_identical_on_the_policy_it_does_not_touch`. |
| **E27** | **La VÉRIFICATION réécrit l'ÉVIDENCE** — un test de fumée rejoue le runner RÉEL au seed de la mesure PUBLIÉE, le runner écrit au chemin même de l'évidence (`results/<name>_<seed>.json`), et le fichier que le record cite n'est plus la mesure qu'il décrit : une dose jouet (R=1, eras=2, max_ticks=80) sous le nom d'une dose publiée. C'est E5 (l'acte de mesurer mute le mesuré) porté sur l'ARTEFACT plutôt que sur le système, et il est SILENCIEUX : un `M` dans `git status` que personne ne lit, aucun test rouge, aucune porte qui parle — les 12 portes du hook passaient dessus. | **(1, 2026-09-14, mesuré ; 2026-09-15, gardé)** 11 fichiers suivis par git réécrits à chaque passe de la suite : `competence_profile_99240`, `lewis_capacity_nav_12345`, `lewis_evolve_nav_107`, `lewis_forage_deconfound_99140`, `lewis_landing_nav_88113`, `lewis_reach_oracle_88114`, `memory_credit_horizon_99167`, `qd_tier_rescue_99260`, `s2_demand_2026`, `tom_coordination_99300`, `tom_probe_99280` — tous via `Harness.save`, tous appelés par un `test_main_*_smoke` au seed publié. Le cas RED de la garde l'a rejoué en direct : sans elle, `Harness(seed=99240, name="competence_profile").save(...)` a réécrit l'évidence dans le worktree en une ligne. ⚠️ Le proxy `grep -rln 'results/' tests` proposé pour trouver les écrivains rendait 13 fichiers dont **zéro** n'écrivait (commentaires, docstrings) : les écrivains se trouvent par ce qu'ils CONSTRUISENT (`Harness(`), pas par ce qu'ils MENTIONNENT. | `exécutable` | Deux couches. (a) L'ARTEFACT se protège lui-même : `src/seed_ai/harness.py::_tracked_by_git` + `Harness._chemin_de_sortie`, décidé à l'`__init__` (jamais dans `save`, appelé en fin de run — E13) — un nominal qui EXISTE et est SUIVI par git est détourné vers `_rerun` (puis `_rerun2`… si le rerun est committé), le JSON porte `rerun_of` ; hors dépôt le suivi est indécidable et rien ne change. (b) Les tests sortent de l'arbre : `AGAGI_RESULTS_ROOT=tmp_path` (11 fichiers). Contre-exemple gelé : `tests/sandbox/test_harness_provenance.py::test_un_fichier_SUIVI_par_git_est_DETOURNE_vers_rerun_et_l_evidence_reste_bit_identique` (évidence comparée OCTET par octet), apparié à `test_un_fichier_NON_SUIVI_prend_le_chemin_NOMINAL_et_est_ecrase` (sans lui, une garde qui détournerait TOUT passerait le premier) et à `test_la_decision_est_prise_a_l_INIT_et_jamais_au_save` (OÙ la garde est posée, pas seulement QU'elle agit). |
| **E28** | **Un NON-FINI transformé en ISSUE DE FOND par une opération qui ne le propage pas** — `max(0.0, nan)` vaut `0.0` en Python : un état INCONNU devient une MORT, sans exception, sans avertissement. La forme (b) de CLAUDE.md (« le nan détecté puis avalé ») appliquée non à un instrument mais à la SIMULATION elle-même | **(1, 2026-09-15, P2.72 c — le tueur des apprenants legacy)** : le World Model PAR AGENT (`world_model.py::observe_batch`, SGD brut sur l'observation, lr 0,01, énergie jusqu'à 100) DIVERGE dès le tick ~51 ; son `err` devient NaN, donc `surprise` NaN, donc `brain_cost` NaN (`world_1_stoneage.py`), donc `energy = max(0.0, energy - nan)` = 0 : l'agent meurt À CHAQUE TICK. Mesuré : 669/669 morts d'une cohorte immortelle avaient un Wp non fini ; la garde sur dW seule (`LEGACY-NAN-GUARD-R1`) ne changeait RIEN (ratio 1,00) ; la garde à la SOURCE (`LEGACY-WM-GUARD-R1`) fait tomber les résurrections de **18 265 à 139** (ratio 0,008), 0 effondrement, 0 NaN — branche `ARTEFACT_WM`. Conséquence : « les apprenants legacy meurent 100× plus », « instable à lr 0,04 », et TOUTE mesure legacy du jour (courbe de pas, cause de mort) sont à RELIRE sous garde ; les runs legacy de l'arc EVO tournaient avec ce World Model. | exécutable | Gardes exécutables : `observe_batch` (Wp/err non fini → reset compté `WorldModel.nonfinite_resets`, err = 1,0) et `compute_policy_gradient` (dW non fini → saut compté) ; contre-exemples : `tests/sandbox/test_world_model_nonfinite_guard.py`, `test_compute_policy_gradient_SKIPS_and_COUNTS_a_non_finite_update_instead_of_poisoning_W`. Ce qui reste : le monde lui-même fait encore `max(0.0, energy - brain_cost)` sans vérifier `brain_cost` — un NaN venu d'ailleurs tuerait encore. |
| **E29** | **Le SUBSTRAT dépend d'un fichier NON VERSIONNÉ chargé À CHAUD** — une composante du calcul (ici l'activation du connectome legacy) est lue sur le disque à l'exécution, hors de HEAD, ignorée par git, et RECHARGÉE dès que son mtime change : le même commit calcule différemment selon la machine ET selon l'instant. Cousine d'E5 (état global) et d'E8 (prémisse non mesurée) : la prémisse « activation = tanh » n'est écrite nulle part, et elle est FAUSSE sur la machine des records | **(1, 2026-09-16, `generated_ops.py` — Swish sur cette machine, tanh sur un clone)** : `src/metaprog/sandbox/generated_ops.py` (Swish x·σ(x)) est ÉCRIT par la boucle métaprog (`compiler.py:35`), supprimé de l'index à d4982b0 (EDR 035), ignoré (`.gitignore:72`), absent de HEAD — et `MambaBatchModel.forward` appelle `_get_activation_function()` à CHAQUE pas (`mamba_agent.py:575, 667`), qui recharge le fichier dès que son mtime a bougé, dans un `try/except Exception: pass`. Conséquences mesurées : (a) tout record legacy (EDR-139 « champion évolué sous Swish », arc EVO, EDR-135, CALIB-LEGACY R2) a tourné sous une activation qu'un clone N'A PAS ; (b) un run legacy peut CHANGER d'activation en cours de route si la boucle métaprog réécrit le fichier (mtime 01:07 le 2026-09-16 : un run l'a bien réécrit cette nuit) ; (c) `check_substrate_pinning` ne connaît que `BILINEAR` : la classe [A] « substrat hérité de l'ambiant » existait pour le PROCESSUS, pas pour le SYSTÈME DE FICHIERS. Trouvé par la critique de complétude du 2026-09-16 ; un lecteur affirmait le fichier « SUIVI » — c'était l'inverse (règle du grep : valider sur un cas positif). **(2, 2026-09-16, le même soir — la classe en ACTE)** : `tests/sandbox/test_instrument_calibration.py::test_synthetic_reader_needs_the_reflex_diagonal_state_drift_counterexample` est ROUGE de façon déterministe (0,749 contre < 0,6 attendu) sur un WORKTREE basé sur 29b5bf3, et VERT sur l'arbre principal au même commit — vu indépendamment par deux implémenteurs de la session harnais. Rejoué sur l'arbre principal avec `ACTIVATION_PIN="builtin"` : 1 failed à **0,7487179487179487**, le chiffre exact du worktree ; sous Swish : 1 passed. La « réponse connue » de ce test de calibration n'était connue que sous une activation qu'un clone n'avait pas. Correctif = 3e2a9f8 (fichier versionné) : rebaser suffit. | exécutable | `mamba_agent.ACTIVATION_PIN` (None = historique bit-identique ; "builtin" = jamais de chargement ; sha256 = charge SSI le fichier présent a CE hash, sinon `ActivationPinMismatch`, posé AVANT le `try/except` qui avale tout) ; `activation_provenance()` publie source / nom / sha256 / `versioned: False` — dans le bloc `regime` de `run_learner_probe` (`legacy_activation`) ; `pinned_activation()` gèle le hash PRÉSENT pour la durée d'un run (câblé dans `_pinned_substrate`) et restaure l'ambiant. Contre-exemples : `tests/sandbox/test_activation_pin.py` (13 cas : le pin AGIT sur une valeur — swish(1) = 0,7311 ≠ tanh(1) = 0,7616 ; l'échange en cours de run est ATTRAPÉ sous pin et gelé comme PASSANT EN SILENCE sans pin ; le clone sans fichier refuse au lieu de rendre tanh). Décision P2.75 (même jour) : le fichier est VERSIONNÉ (sorti de `.gitignore`, contenu identique à celui supprimé en juillet — la boucle métaprog réécrit le même Swish), `versioned` est MESURÉ par `git ls-files` (None si git absent), le sha normalise CRLF→LF, et `test_le_fichier_d_activation_du_depot_est_VERSIONNE` rougit si on le re-ignore. Les records legacy du 2026-09-15 restent VRAIS sous leur activation, désormais reproductible sur un clone. |
| **E30** | **Seuil comparé en FLOTTANTS sur une grandeur de GRILLE** — la DV est un compte sur une grille discrète (k succès sur N évaluations, donc un multiple de 1/N) et la MARGE du critère est elle-même un multiple exact du pas ; comparée en flottants, une ÉGALITÉ exacte devient un ÉCHEC, et le compte par seed publié est faux d'une unité. Le verdict peut ne pas bouger — le CHIFFRE publié, lui, est faux, et il voyage. | **(1, 2026-09-24, P4.12 / `BILINEAR-SHAM-R1`)** : critère `sham <= plain + 0,05` sur 40 lots × 16 = 640 évaluations (pas 1/640), marge 0,05 = **32/640 exactement**. Le seed 3 donne plain 178/640 et sham 210/640, soit 210 <= 178+32 : une ÉGALITÉ. En flottants, 0,328125 > 0,27812498807907104 + 0,05 de **1,19e-08** → compté échec. Publié `8/12` alors que l'arithmétique exacte donne **9/12** ; les 11 autres seeds coïncident dans les deux arithmétiques. La branche `SHAM_PARTIEL` ne bouge pas (9 < 11 exigés) : c'est le chiffre qui est faux, pas le verdict — mais il est publié à TROIS endroits (le bloc `_lecture` du JSON, le bandeau d'EDR-BILINEAR, l'entrée P4.12). Trouvé en auditant la clôture d'ADR-005, recompté seed par seed. | `documenté` | Règle : une comparaison de seuil sur une grandeur de grille se fait **en unités de grille** (`round(x*N) <= round(y*N) + round(marge*N)`), jamais en flottants — et le pas de grille se PUBLIE à côté du critère. **Correction ATTERRIE le 2026-09-24 (`ba56ede3`, session propriétaire), vérifiée et non prise au mot** : les trois publications portent 9/12 (bandeau d'EDR-BILINEAR, bloc P4.12 du backlog, clé `sham_sous_plain_plus_marge` du JSON), le correctif est à la SOURCE (`tools/bilinear_sham_run.py::_sous_barre`, comparaison sur la grille, repli DÉCLARÉ si la marge n'est pas commensurable), `n_grille` = 640 est publié dans la lecture, deux cas gèlent la sémantique, et le verdict `SHAM_PARTIEL` est inchangé (9 < 11). ⚠️ **LE SECOND CAS EST PARU LE JOUR MÊME — la promotion est DUE.** Balayage de la FORME, motif validé sur le cas positif connu avant usage : **12 sites** portent une comparaison `seuil + marge` en flottants, dont **3 dans `tools/td_step_pilot.py`** (`n_sup`/`n_inf`, lignes 136, 240, 350) — le runner de P4.11/P4.17. La condition y est **structurellement satisfaite et mesurée** : l'accuracy est un compte sur `eval_batches × n_agents` = 40 × 16 = **640**, la marge 0,05 vaut **exactement 32 pas**, et les valeurs sont stockées en **float32** (`0,184375` revient comme `0.18437500298023224`, soit 118/640 à 1,9e-6 près). ET POURTANT, recompté : **0 comparaison ne change**. ⚠️ **Ce recompte a d'abord été fait FAUX par moi et corrigé le jour même** : ma première passe énumérait **15 paires de mémoire** et publiait « les 17 comptes » — elle attribuait à R1 une comparaison qui est celle de R0 et MANQUAIT quatre comparaisons réelles (`tdlam09@4 > tdlam05@4` à **marge 0,0**, `aide099`, `lam0_sup_ref`, et le contrôle positif `bptt`). Recompte refait en ANCRANT chaque cas sur sa ligne du code : **25 comparaisons** que `_lecture`/`_lecture_r1`/`_lecture_r2` effectuent vraiment, dont **5 déclarées NON LISIBLES** (bras de lr 1,0 absents — jamais comptées 0) et **20 recomptées : 0 changement, 0 égalité exacte**. Pire écart à un compte entier k/640 sur **504 valeurs lues : 1,526e-05 pas** — ce sont bien des COMPTES. Le billet de P4.11 **TIENT** et le défaut y est **LATENT**. ⚠️ **Mais le recompte publie ce que le compte seul cachait : la MARGE MINIMALE EN PAS.** Le `12/12` de `tdlam > td0` à lr 4,0 — la mesure qui établit l'aide — est à **+1 pas de grille**, soit UNE évaluation sur 640 chez un seul seed ; `td0_d0 > ref_d0 @lr1` aussi (+1), `aide099 @4,0` et `tdlam05@4` à −2. Aucun ne bascule, donc rien n'est à rétracter, et le VERDICT est robuste (le seuil scellé `seeds_aide` vaut 11 : même un 12 → 11 laisse la branche `AIDE_A_UN_POINT`). La leçon opératoire est la distinction : **le COMPTE est au plancher de résolution, la CONCLUSION ne l'est pas** — et un compte au plancher se publie AVEC sa marge en pas, exactement comme ce dépôt exige un plancher de bruit à côté de tout ratio, ici pour le bruit de **REPRÉSENTATION**. ⚠️ **La forme n'est pas confinée à ce runner : second module mesuré le même jour par la session du harnais** — son module de verdict a le MÊME N = 40 × 16 = 640 (coïncidence de design), valeurs vérifiées k/640 en float32 (pire écart 1,526e-05 pas, même chiffre que moi, atteint indépendamment), et un ÉTAGE QUE MON CAS N'AVAIT PAS : la nécessité s'y décide sur des **MÉDIANES**, or une médiane de 12 valeurs est un demi-entier sur 640 donc vit sur la grille **1/1280**, où 0,05 vaut **64 pas** — encore entier. L'étage existe aussi chez moi et je l'avais manqué : le contrôle positif de R0 compare `max(médianes de bptt)` à la constante **0,5**, soit **640/1280**, commensurable ; recompté, il ne change pas (+393 et −407 demi-pas). État chez elle : **LATENTE** aussi (4 comparaisons × 3 cellules, exact et flottant concordent, marges les plus serrées +66 et −59 pas). ⚠️ **CONTRE-EXEMPLE GELÉ, NATUREL, offert par cette session** : une égalité EXACTE sortie d'un run publié — cellule B, D2, seed 5, `126/640` contre une barre `94/640 + 32 = 126/640`, où le flottant répond AU-DESSUS et la grille répond SUR la barre. Avantage sur un cas fabriqué : il est naturel. **Condition d'usage, qu'elle signale elle-même** : ce contraste-là n'est JAMAIS calculé par son module (les seuls comptes par seed y sont A contre A0 et A2 contre A0), donc le contre-exemple doit être PORTÉ sur une comparaison réelle avant de servir. C'est la distinction à garder : la forme est présente, sa conséquence ne l'est pas, et un balayage qui confondrait les deux fabriquerait une rétractation. ✅ **E14 ÉVITÉE, close le jour même.** Signalée à la session propriétaire AVANT sa lecture, elle a rétro-appliqué le correctif aux **trois** lectures du pilote (`_cmp_grille`, repli DÉCLARÉ si la marge n'est pas commensurable au pas), publié `n_grille` = 640, puis recompté AVANT/APRÈS sur les mêmes données : **aucun compte ne bouge**, ni sur les cellules historiques ni sur les **48 cellules NEUVES**. Le défaut était donc latent des deux côtés et ne s'est réalisé nulle part. ⚠️ Ce qui ne retire rien à l'urgence, et c'est la leçon à garder : la fenêtre entre « la garde existe pour UN runner » et « elle est rétro-appliquée aux autres » a duré environ une heure, et E14 se loge exactement dans cette fenêtre — une garde livrée est une garde à rétro-appliquer DANS la même passe, pas à la prochaine revue. Deux autres des 12 sites produisent un VERDICT publié avec marge et leur commensurabilité n'est PAS mesurée : `tools/adaptive_planning_probe.py:154` (`ADAPTIVE_NEUTRAL`) et `tools/hunif_retention_probe.py:148` (`NO_RETENTION_ADVANTAGE`) — deux NÉGATIFS DE FOND, exactement la direction que ce dépôt surveille ; les autres ne gouvernent qu'un affichage. Porte à écrire : **P2.105**, avec le motif validé et l'inventaire des 12 sites prêts à geler. ⚠️ **Méthode, récidive consignée contre moi** : mon PREMIER balayage a rendu **0 site** — le motif ne trouvait pas le cas POSITIF connu, sa classe de caractères excluant les accolades d'une f-string (`db[f"sham|lr={lr}|seed={sd}"] <= …`). Un balayage dont le motif n'a pas été validé sur un positif produit une ABSENCE, pas une mesure : c'est la règle du grep de CLAUDE.md, écrite pour de la prose, appliquée ici à une regex sur du CODE. Sans cette validation je publiais « la forme n'existe nulle part ailleurs ». ⚠️ **Et la règle a une SECONDE face, payée deux fois le même jour, par la session du harnais puis par moi : un RECOMPTE se valide sur ce que le CODE compare, pas sur ce qu'on croit qu'il compare.** Elle a trouvé une égalité exacte, écrit « la forme est vive, elle a changé un nombre », puis mesuré que le contraste recompté n'est jamais calculé par son module — faux publié pendant vingt minutes, rétracté par elle. Moi, symétriquement, j'avais énuméré mes paires de mémoire : une mal attribuée, quatre manquantes. Les deux fois l'instrument de recompte était CORRECT ; c'est sa CIBLE qui n'existait pas, ou qui était incomplète. Forme opératoire : énumérer les comparaisons depuis les LIGNES du code (les `out[...] = ` et les branches), une par une, et déclarer NON LISIBLE toute comparaison dont une clé manque — jamais la compter 0. Troisième et quatrième morsures de la même règle dans ce dépôt le même jour. Garde exécutable, dont l'heuristique reste : l'heuristique « la marge est-elle un multiple du pas ? » est décidable à coût nul dans les instruments de verdict qui connaissent leur N (`n_eval`, `eval_batches × n_agents`). |
| **E31** | **La GARDE porte une IDENTITE qui omet CE QU'ELLE A VERIFIE** — un jeton « c'est deja fait » certifie le CONTEXTE de la verification (qui, quand, dans quelle operation) et non son OBJET (l'etat exact qui a ete juge). Le contexte se REATTEINT ; l'objet, non. Tant que l'identite decrit le contexte, un residu parfaitement VALIDE desarme une verification qui n'a jamais eu lieu, **sans malveillance et sans trace**. | **(1, 2026-09-24, P2.108, temoin de fusion)** : le temoin pose par `pre-commit` et lu par `commit-msg` portait (horodatage, HEAD, MERGE_HEAD) -- ce triplet certifie « une fusion de B dans A », pas « CET index-ci ». Sequence REPRODUITE 4 fois, sans rien faire d'anormal : une fusion est refusee par une porte (MERGE_HEAD reste) ; l'auteur corrige, `git add`, `git commit` ; `pre-commit` PASSE et pose le temoin ; le commit AVORTE a l'editeur (`:cq`, editeur introuvable, Ctrl-C) -- `commit-msg` n'a jamais tourne, donc n'a rien CONSOMME, et un temoin VALIDE survit dans un git-dir PARTAGE par une dizaine de sessions. La meme paire (HEAD, MERGE_HEAD) est ensuite reatteinte avec un index DIFFERENT et **toutes les portes sautent en silence**. C'est le seul des cinq scenarios testes qui laisse un residu. ⚠️ Deux identites avaient deja ete essayees et rejetees : la PRESENCE seule (v1, desarmee par n'importe quel commit avorte) et le PID (mesure : `$PPID` vaut 1 dans les deux crochets sous Git Bash/MSYS -- il n'identifie aucun `git commit`). **(2, 2026-09-24, MEME JOUR, la correction de l'occurrence 1 REFUTEE a son tour)** : la 4e ligne `git write-tree` disait bien « CET index-ci » -- et ne suffisait pas. Les portes de ce depot jugent le DISQUE (`check_synthesis_counts` compte des FICHIERS, `check_amputation` et `check_instrument_calibration` balayent l'ARBRE), pas l'index ; l'index peut donc etre identique pendant que CE QUI EST JUGE a change, et l'arbre etant PARTAGE entre une dizaine de sessions, il change tout seul pendant qu'un editeur est ouvert. Mesure APPARIEE du refutateur : le meme etat du monde passe rc=0 avec ZERO porte quand le residu est la, et est REFUSE rc=1 sans lui. Troisieme identite essayee, troisieme refutee. | `exécutable` | ⚠️ LE REMEDE N'EST PAS UNE MEILLEURE IDENTITE, C'EST LA SUPPRESSION DU RACCOURCI. Trois identites ont ete essayees (presence seule ; horodatage+HEAD+MERGE_HEAD ; +`git write-tree`) et les trois sont tombees. La regle qu'on en tire : **quand l'identite d'un jeton ne peut pas couvrir ce que la verification JUGE, on retire le jeton**. Retirer le seul mecanisme capable de SAUTER une verification ne peut pas ouvrir de trou -- remede MONOTONE, ce qu'un raffinement n'est jamais ; et le cout se chiffre, il ne se suppose pas (ici : les portes tournent DEUX fois sur une fusion conflictuelle, cout deja paye des qu'un editeur depassait 120 s). Contre-exemples geles, tous dans `tests/sandbox/test_hook_on_merge.py` livre DANS CE MEME COMMIT : `test_une_FUSION_AVORTEE_A_L_EDITEUR_ne_laisse_AUCUN_residu_et_ne_desarme_rien` (rejoue la sequence entiere) et `test_un_jeton_FABRIQUE_quelle_que_soit_son_identite_ne_desarme_rien` (le jeton le plus credible possible -- horodatage frais, HEAD juste, MERGE_HEAD juste, arbre REEL -- ne fait plus rien) ; deux gardes TEXTUELLES, qu'un harnais qui mute en MEMOIRE ne peut pas voir : `test_le_crochet_ne_LIT_jamais_un_jeton_et_ne_laisse_aucun_residu` et `test_le_pre_commit_ne_POSE_plus_aucun_jeton`. ⚠️ CONTROLE POSITIF, sans lequel « refuser toujours » passerait tout : `test_une_fusion_CONFLICTUELLE_resolue_tire_les_portes_DEUX_fois_et_c_est_le_prix_assume` chiffre le cout du retrait, et `test_controle_positif_la_meme_fusion_SANS_divergence_PASSE` verifie qu'une fusion saine passe encore. ⚠️ HISTOIRE DE CETTE LIGNE, a garder : elle a ete RETROGRADEE en `documente` le 2026-09-24 par la session qui tient ce registre, pour reparer un BLOCAGE de la flotte -- les contre-exemples qu'elle citait avaient ete RENOMMES sur le disque par son auteur, sans commit, pendant la correction du crochet ; et c'est le durcissement de la forme « fichier.py::fonction » pose le matin meme par ce meme auteur qui a transforme le decalage en refus. Reparation juste, et le choix de ne PAS substituer un nom voisin (E33) aussi. Re-promue par son auteur dans le commit qui livre les cas qu'elle cite. La lecon vaut hors de cette classe : la regle « citation et artefact dans le MEME commit » ne protege que HEAD ; sur un arbre partage, une porte qui juge une citation sur le DISQUE voit les renommages NON COMMITTES des autres, et les casse pour tout le monde. |
| **E32** | **Une garde DEGRADE son PERIMETRE en silence** — faute de pouvoir calculer ce qu'elle devait examiner, elle se rabat sur un perimetre plus etroit et rend VERT. Le vert est alors indiscernable d'un vrai controle : ce n'est pas « rien trouve », c'est « rien cherche ». Forme (a) du biais dominant du depot (une absence devenue affirmation de fond), appliquee non au resultat mais a l'ETENDUE de la mesure. | **(1, 2026-09-24, P2.108, portee de fusion)** : le bloc `AGAGI:FUSION-SCOPE` elargit la portee des 17 appels a `git diff --cached --name-only` de HEAD (le PREMIER PARENT, donc le seul cote entrant) vers la BASE DE FUSION -- sans quoi un document porteur d'un compte, ecrit a la MEME valeur des deux cotes, est identique a HEAD, absent du diff, et la porte ne s'arme pas. Or `git merge-base` rend du VIDE **sans echouer bruyamment** sur des histoires sans ancetre commun (`--allow-unrelated-histories`) comme sur un MERGE_HEAD illisible : `AGAGI_DIFF_BASE` restait vide, la portee retombait au premier parent -- exactement le trou que le bloc existe pour boucher -- et **rien ne le disait**, alors que la branche SQUASH juste dessous criait deja pour le meme symptome. Asymetrie trouvee en re-verification, pas en relecture. **(occ. 2026-09-24 — une erreur d'INVOCATION avalée devient un verdict de PROPRETÉ ; mesurée par la session autrice de la classe, sur sa propre sonde)** : pour décider si deux worktrees de juillet pouvaient être retirés, la sonde lançait un `git status --porcelain` suivi de l'option « --no-optional-locks » et d'une redirection de stderr vers le néant. Or cette option est GLOBALE : placée après la sous-commande, git la refuse (« unknown option »), n'écrit rien sur stdout, et l'erreur partait dans le néant. La sonde a rendu « 0 modifié, 0 non suivi » pour les deux, et « propres, retirables » a été écrit dans un message de commit. Re-mesuré avec stderr visible : l'un porte **170 fichiers STAGÉS jamais committés**, l'autre un fichier non suivi. Rien n'a été retiré — `git worktree remove` a refusé, et c'est lui qui a tenu. ⚠️ La sonde n'a pas cessé de rendre un verdict quand elle a cessé de mesurer : une commande refusée a produit le MÊME « propre » qu'un worktree vraiment propre, la seule différence étant sur le canal qu'on avait coupé. **Règle** : couper stderr sur une sonde de diagnostic est interdit tant que la sonde n'a pas été validée sur un cas POSITIF connu — un worktree qu'on sait sale. La règle du grep, appliquée au code de retour. | `exécutable` | La branche FUSION annonce desormais la degradation et NOMME la portee reellement utilisee. ⚠️ On ne peut pas « elargir a l'arbre vide » pour compenser : sur des histoires non apparentees la portee deviendrait le depot ENTIER et toutes les portes rougiraient sur du legataire -- le remede serait pire que le mal, donc la portee reste inchangee et DITE. Contre-exemple gele : `tests/sandbox/test_hook_on_merge.py::test_une_fusion_SANS_ANCETRE_COMMUN_DIT_que_la_portee_est_degradee`, qui MESURE d'abord que `merge-base` echoue vraiment (sinon le cas ne juge rien) -- sa premiere redaction SAUTAIT pour cette raison meme, la base etant calculee APRES la fusion, quand HEAD a deja l'orpheline pour parent. |
| **E33** | **La PREUVE est fausse, la CONCLUSION est juste — et la vérification porte sur la CONCLUSION, donc elle ne peut pas voir le défaut** (une conclusion vraie appuyée sur une preuve qui ne l'établit pas SURVIT au contrôle habituel, et plus longtemps qu'une conclusion fausse) | **Quatre occurrences mesurées le 2026-09-24, dans l'ordre de gravité croissante de la preuve.** **(1) L'indice RÉEL, MAL LU** — conclusion retenue : « le commit `56bc8781` a amputé l'historique, deux entrées de backlog et un amendement de spec annulés ». VRAIE, mesurée, et confirmée par son auteur (moi) dans le message de `5548a5a6` : le script de commit par index temporaire lisait HEAD TROIS fois. Preuve avancée : « le commit est signé `Test <test@example.com>`, donc un processus de test a committé ». RÉFUTÉE : `Test <test@example.com>` est l'identité git LOCALE du dépôt, lue par tout `commit-tree` qui ne passe pas d'identité explicite — **33 commits sur 1725** la portent, dont du travail de session légitime et des commits faits à la main. La signature dit « ce commit est passé par un script d'index temporaire qui n'a pas transmis l'identité », pas « un processus de test a committé ». L'indice existait et désignait un mécanisme hors de cause — il continue d'apparaître et d'inviter à la même erreur. **(2) La preuve FAUSSE** — conclusion : « le déploiement manuel du hook est un risque » (VRAIE). Preuve : « le hook installé est périmé, les portes 19/20/22 sont absentes de la copie active ». RÉFUTÉE : `.git/hooks/pre-commit` est IDENTIQUE à `HEAD:tools/hooks/pre-commit` (blob `cd391fb4`, commit `55194b52`) ; les portes manquent parce que la branche qui les porte n'est pas fusionnée — état NORMAL. **(3) La preuve VRAIE dont la PRÉMISSE est réfutée** — conclusion : « la réparation de `.git/config` à 13:42:31 n'était pas un contournement de la permission refusée » (peut-être vraie). Preuve : « une session y a écrit sur décision explicite du propriétaire, pour remplacer l'identité `Test` par la bonne ». RÉFUTÉE : trois des cinq commits de la fenêtre sont des `commit-tree` sans aucun `-c`, dans un environnement mesuré sans `GIT_AUTHOR_*`, `GIT_COMMITTER_*` ni `GIT_CONFIG` — leur identité a donc été LUE dans la config, auteur ET committer, et elle était déjà bonne à 13:32:22, dix minutes avant l'écriture. L'identité ne pouvait pas être le motif. La conclusion reste plausible ; sa prémisse ne tient plus. ⚠️ **Deux compléments du soir.** (a) **Ratifiée a posteriori par le propriétaire** (« je donne mon autorisation pour tout ») : l'action est désormais autorisée ; la question de l'instruction préalable reste sans réponse et devient sans objet. (b) **Mais ma réfutation portait sur un objet plus étroit que ce qu'elle laissait entendre** : l'identité du dépôt valait `Test <test@example.com>` à 12:35 — mon propre commit `7fa6b2d8` la porte, comme `2d7e5619` dix minutes plus tôt — et `Robin Denis` dès 12:52. Une écriture d'identité a donc BIEN eu lieu, une heure avant 13:42:31, exactement comme le récit l'affirmait ; le récit relayé a vraisemblablement FUSIONNÉ deux écritures, la correction d'identité et la réparation de `core.bare`. Ma mesure réfutait le motif de la SECONDE, et je l'ai laissée valoir contre le récit entier : E33 appliquée à sa propre occurrence. **(4) La preuve VRAIE AILLEURS, dont le RÉFÉRENTIEL n'est pas énoncé — la plus instructive, et produite EN DIRECT par l'auteur de la classe au moment où il tendait sa garde.** Conclusion : « la garde existe et est calibrée » (VRAIE sur sa branche). Preuve : trois `fichier:ligne` plus une déclaration `CALIBRATED` — de forme impeccable, exactement les formes que cette garde-là accepte. RÉFUTÉE ICI : les deux fichiers sont ABSENTS et non suivis sur `feat/d1-prod-pairing` ; ils vivent sur `chantier/pm-portes` et dans son worktree, et la ligne citée de `test_instrument_calibration.py` y porte un commentaire sans rapport. ⚠️ **Un `fichier:ligne` est SOUS-DÉTERMINÉ dès qu'il existe plus d'une branche — et ce dépôt a six worktrees.** Vérifié au sha plutôt que cru : `775865c6` porte bien la garde, avec deux des trois contre-exemples aux lignes EXACTES annoncées et le troisième à 219/231 au lieu de 225. **(4-bis, même réponse)** : ce 225 venait d'un `grep` montrant une assertion `preuve_de_forme` à l'INTÉRIEUR d'un test, prise pour sa ligne de définition. ⚠️ **Ce que la gradation enseigne** : la dangerosité ne vient pas de la fausseté de la preuve mais de sa FORME — (2) est la plus facile à attraper, (4) la plus difficile, et c'est celle dont la preuve est la mieux formée. **(occ. 5, 2026-09-24 — LE RÉFÉRENTIEL BOUGE AUSSI DANS LE TEMPS, pas seulement entre BRANCHES ; trouvée et formulée par la session autrice d'E31/E32, inscrite ici sous son nom de mécanisme)** : la règle « citation et artefact dans le MÊME commit » ne protège que **HEAD**. Sur un arbre PARTAGÉ, un renommage NON COMMITTÉ casse les citations de HEAD **pour tout le monde**. Cas mesuré : E31 déclarait sa garde exécutable en nommant trois cas gelés ; son autrice a supprimé le témoin que sa revue adversariale avait réfuté et renommé les cas SUR LE DISQUE sans committer ; les trois citations de HEAD sont devenues fausses, et son propre durcissement — la notation « fichier.py::fonction » exige que ses deux moitiés existent — a transformé ce décalage en REFUS pour toute la flotte, elle-même incluse. Deux mécanismes sains SÉPARÉMENT : une porte qui lit le DISQUE pour juger une citation, et un renommage avant son commit ; leur rencontre bloque l'écriture de tous. ⚠️ **Symétrie exacte avec l'occ. 4** : là le référentiel variait entre BRANCHES, ici il varie dans le TEMPS sur le même arbre — une citation est sous-déterminée sur les DEUX axes, et le remède est le même, citer « sha:chemin ». ⚠️ **Faux positif de la stricture, à connaître AVANT d'écrire dans ce registre** : sa note d'historique mentionnait la NOTATION entre backticks, et sa propre porte l'a lue comme une vraie citation dont aucune moitié n'existe. La stricture n'a pas été relâchée, et c'est le bon choix : les backticks signifient « ceci est un artefact », donc une MENTION s'écrit entre guillemets. ⚠️ Écart connu et NON clos : la porte 3 compte 26 exécutables par sous-chaîne quand la porte 8 en compte 24 par colonne — E28 et E29 portent leur statut sans backticks. Correctif structurel (lire la COLONNE) chez la session qui tient les rôles. **(occ. 7, 2026-09-26 — à mon débit, rectifiée par la session qui a réparé la CI)** : j'ai affirmé, à quatre sessions puis dans l'occurrence d'E22 ci-dessus, que la classe `FlatModel` avait été « retirée par une fusion de juillet » et que l'import était cassé en local comme en CI. Deux preuves bien formées, braquées sur le mauvais objet : (1) mon grep « ^class FlatModel » exigeait la COLONNE 0 et ne voyait pas la définition INDENTÉE sous une garde — motif non validé sur un cas positif ; (2) j'ai lu un `git log -S` qui désignait une fusion comme un RETRAIT, sans ouvrir le fichier — alors que j'avais déjà compté QUATRE occurrences du nom dedans. **La mesure qui me contredisait était sous mes yeux**, et ouvrir le fichier aurait pris une seconde. D'où « ce n'est pas un écart local/CI », quand c'était exactement un écart local/CI — torch présent ici, absent là. La session PM avait relayé ce diagnostic tel quel au propriétaire, et l'a rectifié elle-même. ⚠️ **Ce que l'occurrence ajoute** : quand une mesure déjà faite CONTREDIT l'interprétation d'une autre, c'est l'interprétation qu'on vérifie — en lisant l'objet lui-même, pas en cherchant un indice de plus sur lui. | `documenté` | Garde EXÉCUTABLE existante, **sur une autre branche** : `775865c6:tools/refutateur_temoins.py::preuve_de_forme` — trois formes acceptées et elles seules (un fichier:ligne, une commande avec sa sortie, une valeur numérique opposée à une autre). Contre-exemples gelés dans `775865c6:tests/sandbox/test_refutateur_temoins.py` : l. **205** l'attaque universelle tuée par la SEULE absence de preuve de forme ; l. **258** les TROIS formes acceptées et le reste refusé (les deux issues) ; l. **219/231** le contrôle symétrique qui compte le plus — la recopie porte une preuve de BONNE forme et doit être rejetée par une AUTRE garde. Statut `documenté` et non `exécutable` parce que **dans la branche où vit ce registre la garde est ABSENTE : elle ne peut rien y refuser** ; promotion attachée à la fusion, et c'est la seule formulation qui dit vrai aujourd'hui et demain. ⚠️ **RÉSERVE ESSENTIELLE, sans quoi la garde paraîtra plus large qu'elle n'est** : elle vérifie la FORME d'une preuve, JAMAIS qu'elle ÉTABLIT ce qu'on lui fait dire — **les quatre occurrences ci-contre portaient toutes une preuve de bonne forme**. C'est une condition NÉCESSAIRE, pas suffisante : elle tue l'attaque vague, elle ne peut pas tuer une preuve fausse bien formée. Dette **P2.111** : étendre le périmètre hors du Réfutateur, et traiter la PORTABILITÉ — une critique est ancrée tant qu'elle vit dans sa revue, et redevient sous-déterminée dès qu'on la cite ailleurs ; le remède est la règle de citation `sha:chemin`, pas un champ de plus dans la garde. |
---

## Lecture du registre

- **Toutes les classes ont une garde exécutable ou documentée** :
  **24 exécutables** <!-- count:classes_executables=24 --> (E15 et E8 promues le 2026-09-02 ; E27 ajoutée le 2026-09-15 ; E32 le 2026-09-24 — **E31 RAMENÉE à `documenté` le jour même**, ses trois contre-exemples ayant disparu avec le témoin que la revue adversariale a réfuté ; elle attend une re-promotion par son auteur —, chacune livrée avec sa garde ET son contre-exemple gelé dans la même passe),
  **6 documentées** <!-- count:classes_documentees=6 --> (E21 ajoutée le 2026-09-07 ; E30 le 2026-09-24, sa garde exécutable attend un SECOND cas) ; **AUCUNE n'est plus sans garde** ; 1 est
  explicitement non automatisable (E9), et c'est un choix, pas un manque.
  *(Ces deux comptes sont RECOMPUTÉS par `tools/check_synthesis_counts.py` : la phrase « 19 classes
  sur 19 » qui vivait ici était fausse le jour même de l'ajout d'E20 — c'est ce défaut qui a motivé
  le cliquet des synthèses.)*
- **E13 est CLOSE le 2026-07-27** (`tools/cost_guard.py`, P3.2), au prix de deux runs abandonnés de plus
  (5 au total). Comme E11, elle a **corrigé l'énoncé de sa propre dette en se fermant** : le backlog
  réclamait « débit mesuré au smoke + coût projeté » — or le débit mesuré était JUSTE et le run a explosé
  quand même, parce que le coût dépend du SEED. Aucune projection linéaire ne borne une queue ; il faut un
  plafond AU RUNTIME sur l'unité ATOMIQUE, et il doit être DÉTERMINISTE (nombre d'agents, pas secondes).
- **E11 est CLOSE le 2026-07-27** (`tools/preregister.py`, P3.1), et c'est sa 2ᵉ occurrence qui l'a
  débloquée : sur EVO-006, la règle de lecture *était* pré-écrite, mais la sonde qui a confirmé le verdict
  a été choisie après avoir vu quelle sous-tâche bougeait. La forme fuyante de la classe n'est donc pas le
  SEUIL — que la discipline manuelle protégeait déjà — mais l'**INSTRUMENT**. La garde scelle la règle ; la
  règle d'usage qui en découle est de **nommer les instruments autorisés dedans**. Appliqué dès EVO-007.
- **E18 est la première classe attrapée AVANT d'avoir produit un résultat faux.** E17 avait été promue
  après coup, sur un record déjà gravé ; E18 a été trouvée pendant le **pré-vol** d'EVO-005, en relisant
  l'estimateur avant de lancer le run. Sa 1ʳᵉ occurrence est antérieure de neuf mois ([[EDR-056]], dont le
  backfire est imputé au « bruit » sans que le mécanisme — payer l'absence de preuve — soit nommé) : la
  classe existait donc déjà dans le dépôt, non identifiée. C'est le cas d'usage exact du registre —
  reconnaître qu'une erreur nouvelle est une ANCIENNE erreur, et hériter de sa garde plutôt que de la
  redécouvrir par un run gâché.
- **La passe EVO-005 a aussi rattaché deux occurrences à des classes EXISTANTES plutôt que d'en créer**
  (E4 : le cliquet de calibration est aveugle aux noms hors de sa grammaire — 3ᵉ angle mort du même outil ;
  E6, portée élargie : une POLITIQUE validée à `H=0` puis présumée valide dans un monde où `H` accumule).
  Le registre gagne à avoir peu de classes FORTES : une classe par symptôme le rendrait illisible et
  casserait la règle « pas de troisième occurrence », qui n'a de sens que si les occurrences se regroupent.
- **E17 est née directement `exécutable`, et c'est le rituel qui a fonctionné** : sa 1ʳᵉ occurrence
  (`sep(D)`, EVO-002) avait été traitée comme un fait local (« cet instrument-là est trompeur ») au lieu
  d'être élevée en classe. La 2ᵉ (EVO-004) est arrivée quelques heures plus tard, dans la même session,
  sur un instrument DIFFÉRENT — preuve que le défaut est structural (substrat contractif + décision par
  `sign`/`argmax`), pas une maladresse ponctuelle. La règle « pas de troisième occurrence » l'a promue
  d'emblée.
- **E17 est aussi le premier contre-exemple à E14** (« une garde n'est jamais rétro-appliquée ») : la
  classe a été **immédiatement pointée sur le record déjà gravé** ([[EDR-EVO-004]], committé la veille) au
  lieu d'attendre un audit. Le verdict a été re-mesuré sur la grandeur fonctionnelle (bascule d'`argmax`)
  et **tient** — champions ≤ 0.06 vs 1.00 pour un lecteur avéré. Un verdict qui survit à un changement de
  la grandeur mesurée est bien plus solide qu'un verdict jamais re-interrogé.
- **E9 est la plus coûteuse et la seule irréductible** : trois occurrences, aucune détectable par du code.
  C'est elle, et elle seule, qui justifie que la revue adversariale soit une **obligation** et non un confort.
- **E10 est la plus instructive** : la règle existait, écrite depuis longtemps, et a été violée par trois
  acteurs différents en une journée — moi, le code d'instrument, la suite de tests. **Toute règle
  documentée sans application exécutable finit violée.** **Promue `exécutable` le 2026-09-01 (P2.22)**
  après une 8ᵉ ET une 9ᵉ récidive le MÊME jour, sur le MÊME fichier à forte contention, dans les DEUX sens
  — `tools/check_staged_authorship.py` remplace l'inspection visuelle par une empreinte comparée
  mécaniquement pour le sens où un commit happe du travail étranger (occ. 8) ; le sens inverse,
  ordonnancement de commits entre sessions (occ. 9), reste `documenté` — rien à comparer dans un stage.
- **E14 est la plus inquiétante, et c'est la nouvelle du 2026-07-21.** E3 avait déjà une garde
  `exécutable` quand WARM-002 a été gravé, cité et propagé. La garde n'a rien attrapé parce qu'**un
  cliquet ne regarde jamais en arrière** : il bloque le prochain instrument non calibré, il ne relit pas
  les verdicts publiés. Corollaire : le nombre de classes « couvertes » **surestime** la protection réelle
  tant que le stock de conclusions actives n'a pas été repassé au crible.
- **La garde d'E14 est délibérément PARTIELLE, et c'est un résultat, pas un renoncement.** Le signal qu'on
  voulait automatiser — « verdict nul publié SANS contrôle positif » — a **échoué deux fois** sa
  calibration sur l'archétype : le mot `oracle` apparaît dans WARM-002 en cadrage (`## Question`) puis en
  valeur citée (`oracle intact ≈ 200 (S2-009)`), sans qu'aucun contrôle ait été lancé. **Distinguer « a
  lancé un contrôle positif » de « cite un contrôle fait ailleurs » exige de comprendre la phrase, pas de
  la matcher.** Le code énumère et priorise (`verdict NUL × portée MONDE × plancher avoué`, calibré sur
  l'archétype) ; le jugement tranche — même partage que E9. Continuer à raffiner les motifs sur l'unique
  exemple disponible aurait été de la classe **E11**. Les deux échecs sont figés en régression dans
  `tests/sandbox/test_retro_audit.py`.
- **E19 est le corollaire d'E14 au niveau du RÉGIME, pas du temps — et le premier cas où la batterie de
  calibration était COMPLÈTE au sens du cliquet tout en étant STRUCTURELLEMENT incapable de voir le
  défaut.** L'instrument était déclaré `["*"]` (`test_instrument_calibration.py:163`), le cliquet ne
  signalait rien, et ses deux cas gelés étaient VALIDES — mais tous deux dans le régime à UN `_step`,
  alors que le verdict était lu dans le régime à DEUX. « Calibré » ne veut dire calibré QUE dans les
  régimes des cas gelés ; la couverture doit donc être déclarée **par RÉGIME**, pas par fonction.
  **Corollaire à graver** : un contrôle positif du régime FACILE ne calibre PAS le régime DUR, et **un
  contrôle positif se juge à SA PROPRE réponse connue, jamais à la barre de la condition testée**. Le
  dépôt en donne la preuve : le contrôle de rappel 2-tick EXISTAIT déjà (`test_bilinear_noop_on_recall`,
  `:1641`, `same_tick=False` par défaut) et il est resté VERT tout l'arc **parce qu'il était jugé contre
  `bar = 1/K+0.15`** alors que sa réponse connue est **1.000** — il dégrade pourtant à **0.70-0.85** à
  `lr=0.02` (mesuré, 4 seeds, séparation totale avec les 1.000 de `lr=0.002`). Le seuil d'une tâche à
  chance 1/6 avait été importé sur une identité : c'est la même faute-mère que l'artefact lui-même —
  un réglage valide sur la condition facile, réutilisé sur la condition dure.
- **Note d'auto-critique de la passe E19** : les occurrences (1) et (2) datent du 2026-08-03 et n'ont
  JAMAIS été inscrites — aucune des quatre passes de l'arc (LANG-MEMORY, MEM-PERCEPTION, BILINEAR,
  RETAIN-COMPOSE) n'apparaissait au registre (`grep -i` = 0 ligne) avant aujourd'hui. C'est une
  occurrence de plus d'**E10** (règle documentée non appliquée), inscrite dans sa cellule Occurrences :
  le rituel du registre s'est appliqué à tout sauf à un arc entier, et c'est précisément l'arc où la même
  erreur a pu se répéter trois fois.

Cf. [`REF-EXPERIMENT-PREFLIGHT.md`](REF-EXPERIMENT-PREFLIGHT.md) (les 4 générateurs) ·
[`../roadmap/PRIORITES_ET_DETTES.md`](../roadmap/PRIORITES_ET_DETTES.md) (P3 : E11 et E13) ·
`tools/experiment_preflight.py` · `tools/sim_session.py` · `tools/check_instrument_calibration.py`
