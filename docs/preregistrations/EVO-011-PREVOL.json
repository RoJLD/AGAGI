{
  "name": "EVO-011-PREVOL",
  "rule": {
    "confond_declare_AVANT": "Le lecteur cable est mesure DANS le regime du monde (E6) : `H` accumule et la derive d'etat peut noyer un signal qui serait parfait a H=0 (EVO-005 : un lecteur parfait a H=0 tombe a la chance in-world). C'est pourquoi les TROIS bras portent la diagonale reflexe +10 -- sans elle, le contraste mesurerait la derive, pas la lecture. Un `r` proche de 1 avec le controle (i) in situ PASSE signifie donc « lire ne paie pas », pas « le lecteur ne lit pas ».",
    "controle_de_manipulation_OBLIGATOIRE": "(i) MANIPULATION, deux fois : `measure_decision_saliency` (instrument calibre) > 0.5 pour LECTEUR et < 0.05 pour TEMOIN, ET **in situ** dans le regime reel (E6, le forward batch, pas `recurrent_forward`) : P(throw | obs4=+1) > 0.9, P(throw | -1) < 0.05 pour LECTEUR ; ~0.5/0.5 pour BROUILLE ; `throws == 0` pour TEMOIN. (ii) ARETE PRESENTE en fin de run : `W[4, o+8] == 8.0` EXACT dans LECTEUR/BROUILLE, `== 0.0` dans TEMOIN. (iii) BUDGET : 24 agents par bras (`assert_n_per_arm`), <= 4800 agent-ticks par bras-seed, `CostGuard` 150 s/seed, `project_cost` APRES le smoke. (iv) E1/E2 : `throw_prey_hits >= 1` au moins une fois en LECTEUR (la chaine se ferme reellement), et profils P(throw | type) differents entre LECTEUR et BROUILLE (`assert_ablation_changes_something`). (v) E14 : `assert_not_degenerate` sur les ages de CHAQUE bras ; censure > 50 % dans un bras -> INDETERMINE-PLAFOND (baisser ticks/energie, JAMAIS le seuil). (vi) IDENTITE life_score : pour chaque agent, `calculate_life_score(a)` egale exactement le score recompose depuis ses composantes -- le defaut (3) du harnais etait une INCOHERENCE de runner, pas de formule.",
    "cout": "3 bras x 12 seeds x 24 agents x <= 200 ticks = <= 172 800 agent-ticks, plus 3 mesures de saillance. Smoke 1 seed chronometre AVANT engagement ; plafond scelle 150 s/seed ; sous bail `kuzu`.",
    "design": "3 bras x 12 seeds, monde `Biosphere3D` + `_setup_lewis`, cohorte FIXE de 24 clones (`benchmark_mode=True` : aucune naissance, n exact), 200 ticks, energie 80, `night_enabled=False`, `current_era` eleve (scaffolds annules), sous bail `kuzu`. Genomes `Genome(W, 59, 108)` a diagonale reflexe +10 (LES TROIS bras la portent : le confond reflexe est neutralise par construction) et cablage d'approche/visee IDENTIQUE partout. TEMOIN : rien de plus (`logits[8]` = 0 exactement -> ne lance jamais). LECTEUR : + `W[4, o+8] = +8` -- l'arete EXACTE de la regle d'origine. BROUILLE : meme cablage, mais obs[4] remplace par +/-1 tire sur un RNG DEDIE (le RNG global reste identique entre bras, classe E5) -- il separe « LIRE LE TYPE paie » de « LANCER paie ».",
    "discrimination": {
      "r <= 1.10 (le lecteur ne paie pas)": "NE PAIE PAS -- c'est la branche « reponse sans evoluer » de la regle d'origine : la survie seule ne garde pas une cognition atteignable sur ce canal, et aucun run evolutif n'est engage.",
      "r >= 1.25 ET sign_p < 0.05 (le lecteur paie)": "LE LECTEUR PAIE -> lire ALORS LECTEUR vs BROUILLE aux memes seuils : s'il paie aussi, c'est la DISCRIMINATION DE TYPE qui paie (le run evolutif d'EVO-011 devient legitime, via une regle -bis qui remplace la chaine kill par stun->melee) ; s'il ne paie pas, seul le LANCER paie et le TYPE est indifferent (S2-012 tient pour obs[4], la question tombe) ; entre les deux, INDETERMINE-DISCRIMINATION.",
      "toute autre issue (1.10 < r < 1.25, ou r >= 1.25 avec sign_p >= 0.05)": "INDETERMINE -> extension UNIQUE a 24 seeds, relecture aux MEMES seuils ; si la valeur reste dans la bande, INDETERMINE FINAL, grave tel quel.",
      "un controle (i)-(vi) echoue sur au moins un seed": "INDETERMINE-HARNAIS : verdict sur l'INSTRUMENT, pas sur le monde. Ne pas lire la suite."
    },
    "dv_primaire": "`r` = mediane sur seeds du ratio (age median LECTEUR / age median TEMOIN). Statistique : `sign_p` (test des signes sur les 12 seeds appariés). DV de survie secondaires, rapportees sans poids : `throws`, `throw_prey_hits`, `throw_decided` (les compteurs TOUT REGIME poses le 2026-09-07), `env.big_kills` ET `sum(mammoth_kills)` cote a cote (crédit de meute vs compteur monde), `env.leurre_hits`.",
    "pourquoi_une_regle_A_PART": "La regle d'origine `EVO-011.json` reste scellee et NON appliquee : son dispositif fait peser `mammoth_kills` (poids 400) sur la chaine obs[4]->throw->KILL, qui est COUPEE PAR CONSTRUCTION (registre E2 occ. 3, 2026-09-07) -- le bloc balistique fait `hit_entity['energy'] -= damage` SI la cle existe et pose `stunned` SINON, et une proie n'a que {x, y, type, stunned, hp}. Une pre-inscription ne se corrige pas : celle-ci scelle la chaine REELLE, mesurable, qui passe par le DETOUR stun -> l'apex ne se deplace ni ne riposte -> melee sans riposte.",
    "question": "PRE-VOL DECISIF d'EVO-011 : un LECTEUR CABLE A LA MAIN du canal de type obs[4] survit-il MIEUX in-world qu'un TEMOIN identique sans l'arete ? Si le lecteur ne paie pas, la question « la survie seule garde-t-elle une cognition ATTEIGNABLE sur ce canal ? » tombe SANS run evolutif.",
    "regle_de_lecture_continue": "`r` se lit sur le continuum entier via les trois seuils ci-dessus (partition exhaustive de R+), et `sign_p` n'intervient QUE dans la branche haute -- un `r` sous 1.10 est un non-paiement quel que soit `sign_p`. Les DV secondaires se rapportent dans TOUS les cas, y compris quand le verdict est INDETERMINE."
  },
  "seal": "ea378165054ab1436c0db4d648b3809decb1b0d53f50d8542da6a1e4cbd3ec6b"
}