# Rapport cartographe — 2026-07-02 (2ᵉ passe)

> Source : `signals-2026-07-02.json` (déterministe) + interprétation lead. 2ᵉ passe depuis
> `rapport-2026-07-01.md`. Advisory : ne décide pas, propose.

## Delta depuis la 1ʳᵉ passe (07-01)

Les **trois gaps ownables** du backlog sont **fermés** (3 EDR préfixés, 3 PRs mergées) :

| Gap | EDR | Résultat | PR |
|---|---|---|---|
| P1 CRAFT | EDR-CRAFT-001 | rétention POLICY_LOCKED (aucun levier environnemental) | #130 |
| P2 WLD | (câblage) | gate de cohérence survie câblé dans le runner S2 (faux VOID fermé) | #132 |
| P3 NAV | EDR-NAV-001 | mur de navigation = **READOUT_GAP** (H encode, tête d'action ignore) | #133 |

Nouvel artefact de coordination : **`../HANDOFF_TORCH_READOUT_CREDIT.md`** — NAV-001 + C1 (disjoint-heads)
+ C2 (BIND) convergent : le verrou est le **READOUT/CRÉDIT**, pas l'encodeur (disculpé 3× indépendamment).

## Signaux 07-02 (après corrections)

- **Orphelins : 2 → 0.** EDR-190/191 (extension disjoint-heads) n'étaient pas mappés dans le
  `legacy_edr` de COG → **corrigé** (COG.legacy_edr += 190,191). Registre propre.
- **Densité-verrou (lock terms) : NAV 65 (#1), BIND 63 (#2)**, COG 39, MEM 30. → **valide la
  priorisation du handoff** : T1 (NAV readout) + T3 (BIND) ciblent exactement le top-2. (Indicateur
  *retardé* : NAV-001 localise le mur mais le corpus mémoire reste dense en marqueurs — normal.)
- **Verdicts ouverts : 2.** EDR-151 (ToM comportemental INDÉTERMINÉ — vraiment ouvert, volet causal
  différé migration torch, cf. backlog V1). EDR-121 (storage INCONCLUSIF — **résolu-par-aval** 155/157,
  annoté PR #128 ; le détecteur ne lit que le frontmatter, faux positif connu, cf. README §bornage).
- **Dormance : les 10 territoires** flaggés — bruit du **proxy numérique** (écart au max EDR 191).
  Le champ `statut` désambiguïse : CRAFT/WLD/NAV sont passés `actif` cette session malgré des numéros
  legacy bas. Signal non actionnable tel quel.

## Nouveaux gaps ownables ?

**Aucun.** Les territoires dormants *ownables* (CRAFT/WLD/NAV) sont désormais travaillés ; le reste de
la frontière à haute valeur (NAV/BIND/COG — top densité-verrou) est le **substrat torch/prod**, territoire
des sessions parallèles → **à coordonner, pas à posséder**. La backlog « à faire ici » est **drainée**.

## Recommandation lead

1. **Prochain mouvement = le handoff torch** (`HANDOFF_TORCH_READOUT_CREDIT.md`), priorité **T1 (NAV
   readout)** : cible la plus nette, banc prêt (`nav_localization_probe`), critère binaire. À remettre à
   la session torch (coordination), pas à exécuter en solo (collision `src/backend_torch`).
2. **Volet causal ToM (EDR-151, V1)** reste le seul gap COG ownable en tooling, mais il est *bloqué* sur
   `life_score`↔ToM (incitation) — faisable seulement une fois la boucle torch dispo. Différé.
3. Ré-évaluer à la prochaine passe : si la densité-verrou NAV/BIND **baisse** après T1/T3, c'est le signal
   que le handoff a mordu.
