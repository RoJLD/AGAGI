# EDR 201 — Décomposition 2×2 + robustesse du verdict COS — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fermer les 2 caveats de la revue finale COS — isoler le levier décisif du binding (décomposition 2×2 crédit×curriculum) et confirmer que le verdict [2] est robuste sur E0.

**Architecture:** Additif à `tools/craft_or_starve_edr.py` (mergé sur main, collision-safe, pur numpy). Réutilise TOUS les primitifs Phase B ; aucune logique de monde/apprenant nouvelle — juste un dispatcher de cellule (`_train_cell`), une grille factorielle (`decompose_2x2`), un sweep (`robustness_sweep`), leurs verdicts gelés et rapports.

**Tech Stack:** Python, numpy (déterministe `np.random.default_rng`), pytest.

## Global Constraints

- **Additif strict** : APPEND uniquement à `tools/craft_or_starve_edr.py` (+ une branche `--edr201` dans le bloc `__main__` existant) et `tests/sandbox/test_craft_or_starve_edr.py`. Ne PAS éditer le code existant (Phase A, L0/L1/L2, ladder). AUCUN import de `src/`/`world_1_stoneage.py`/`backend_torch.py`. PUR NUMPY.
- **Réutilise** (déjà dans le fichier) : `NpReinforceLearner(seed, arm)`, `NpTickLearner(seed, arm)`, `rollout_learn(learner, arm, params, seed, M, n_episodes)`, `rollout_learn_tick(learner, arm, params, seed, M, n_episodes, entropy_beta=0.0)`, `rollout_learn_curriculum(learner, arm, params, seed, M, n_warm, n_cold, c_warm=0.5, entropy_beta=0.01)`, `evaluate_learner(learner, arm, params, seed, M) -> dict{"binding_gap","survival",...}`, `ladder_verdict(seeds, E0, M, n_episodes, n_warm, n_cold) -> {"verdict","rungs","E0"}`, `_rung_composes(binding, survival) -> bool`, `PILOT_SEEDS=(1000,1001,1002,1003)`, `Params`, `replace`, `np` (module top).
- **Seuils gelés** hérités : `LADDER_BIND_PASS=0.5`, `LADDER_SURV_PASS=0.5` (via `_rung_composes`). Curriculum : warm `c_consume_empty=0.5` + `seed`, cold params pleins + `seed+100` (schedule IDENTIQUE à `rollout_learn_curriculum`).
- **Verdicts gelés pré-enregistrés** : décomposition = arbre gaté sur (tick,True)=L2 ; robustesse = [2]-ROBUSTE ssi verdict par-E0 == `"[2] CREDIT-ATTRIBUE"` sur TOUS les E0.
- **Ne PAS préjuger le verdict** : les tests vérifient le CONTRAT (structure) + des sanités CONNUES (L2 compose, L0 non), jamais la cellule ouverte (substep, on).
- **Déterminisme** : `np.random.default_rng(seed)` ; deux runs au même seed byte-identiques.
- **Tree partagé** : le CWD des sous-agents = repo principal, PAS le worktree → tous les handoffs (brief, report, fichiers édités) en **chemins ABSOLUS worktree** (`c:/Users/robla/VScode_Project/AGAGI/.claude/worktrees/cos-edr201/...`), et pytest/git préfixés par `cd` dans le worktree.

## File Structure

- `tools/craft_or_starve_edr.py` — APPEND : `_train_cell`, `_DECOMP_CELLS`, `decompose_2x2`, `_decomp_verdict`, `_report_decompose` (T1) ; `robustness_sweep`, `_report_robustness` + branche `--edr201` (T2).
- `tests/sandbox/test_craft_or_starve_edr.py` — APPEND : tests contrat/déterminisme/sanité.

Deux tâches : **T1** = décomposition 2×2 (dispatcher de cellule + grille + verdict + rapport) ; **T2** = sweep de robustesse + rapport + CLI.

---

### Task 1: Décomposition 2×2 crédit×curriculum (`_train_cell` + `decompose_2x2` + verdict + rapport)

**Files:**
- Modify: `tools/craft_or_starve_edr.py` (APPEND)
- Test: `tests/sandbox/test_craft_or_starve_edr.py` (APPEND)

**Interfaces:**
- Consumes : `NpReinforceLearner, NpTickLearner, rollout_learn, rollout_learn_tick, rollout_learn_curriculum, evaluate_learner, _rung_composes, Params, replace, PILOT_SEEDS, np` (Phase B / module).
- Produces :
  - `_train_cell(credit, curriculum, arm, params, seed, M, n_episodes, n_warm, n_cold) -> learner` : entraîne l'apprenant d'une cellule. `credit in {"substep","tick"}`, `curriculum` bool.
  - `_DECOMP_CELLS = (("substep", False), ("substep", True), ("tick", False), ("tick", True))`.
  - `decompose_2x2(seeds=PILOT_SEEDS[:3], E0=16.0, M=32, n_episodes=120, n_warm=80, n_cold=80) -> {"cells": {(credit,curr): {"binding","survival","composes"}}, "verdict": str, "E0": float}`.
  - `_decomp_verdict(cells) -> str` ∈ {"CURRICULUM-SUFFISANT","CREDIT-SUFFISANT","BOTH-NECESSARY","INCOHERENT"}.
  - `_report_decompose(res)`.

- [ ] **Step 1: Écrire les tests qui échouent**

Ajouter à `tests/sandbox/test_craft_or_starve_edr.py` :

```python
# === EDR 201 Task 1 : decomposition 2x2 credit x curriculum ===

from tools.craft_or_starve_edr import _train_cell, decompose_2x2


def test_train_cell_determinism():
    a = _train_cell("substep", True, "inesc", Params(E0=16.0, T=40), seed=1000, M=8, n_episodes=6, n_warm=6, n_cold=6)
    b = _train_cell("substep", True, "inesc", Params(E0=16.0, T=40), seed=1000, M=8, n_episodes=6, n_warm=6, n_cold=6)
    assert np.array_equal(a.W_out, b.W_out)
    assert np.array_equal(a.W_hh, b.W_hh)
    assert np.array_equal(a.W_ih, b.W_ih)


def test_decompose_2x2_contract():
    # CONTRAT uniquement — on ne prejuge PAS quelle cellule compose (c'est le resultat du run).
    res = decompose_2x2(seeds=(1000,), E0=16.0, M=8, n_episodes=10, n_warm=10, n_cold=10)
    assert set(res["cells"]) == {("substep", False), ("substep", True), ("tick", False), ("tick", True)}
    for c in res["cells"].values():
        assert set(c) >= {"binding", "survival", "composes"}
        assert isinstance(c["composes"], bool)
    assert res["verdict"] in ("CURRICULUM-SUFFISANT", "CREDIT-SUFFISANT", "BOTH-NECESSARY", "INCOHERENT")
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `cd "c:/Users/robla/VScode_Project/AGAGI/.claude/worktrees/cos-edr201" && python -m pytest tests/sandbox/test_craft_or_starve_edr.py -k "train_cell or decompose_2x2" -q`
Expected: FAIL — `ImportError: cannot import name '_train_cell'`.

- [ ] **Step 3: Écrire le dispatcher + la grille + le verdict + le rapport**

Ajouter à `tools/craft_or_starve_edr.py` (après le bloc ladder, avant `if __name__`) :

```python
# ============================ EDR 201 : decomposition 2x2 (credit x curriculum) + robustesse ============================
# Ferme les 2 caveats de la revue finale COS. AUCUNE dynamique nouvelle : reutilise les entraineurs Phase B.


def _train_cell(credit, curriculum, arm, params, seed, M, n_episodes, n_warm, n_cold):
    """Entraine l'apprenant d'une cellule du 2x2 (credit x curriculum) et le retourne.
    credit in {'substep','tick'} ; curriculum bool. Curriculum = warm (c_consume_empty=0.5, seed) puis cold
    (params pleins, seed+100) — MEME schedule que rollout_learn_curriculum, avec l'entraineur du credit choisi."""
    if credit == "substep":
        learner = NpReinforceLearner(seed=int(seed), arm=arm)
        if curriculum:
            rollout_learn(learner, arm, replace(params, c_consume_empty=0.5), seed=int(seed), M=M, n_episodes=n_warm)
            rollout_learn(learner, arm, params, seed=int(seed) + 100, M=M, n_episodes=n_cold)
        else:
            rollout_learn(learner, arm, params, seed=int(seed), M=M, n_episodes=n_episodes)
    else:  # 'tick'
        learner = NpTickLearner(seed=int(seed), arm=arm)
        if curriculum:
            rollout_learn_curriculum(learner, arm, params, seed=int(seed), M=M, n_warm=n_warm, n_cold=n_cold)
        else:
            rollout_learn_tick(learner, arm, params, seed=int(seed), M=M, n_episodes=n_episodes)
    return learner


_DECOMP_CELLS = (("substep", False), ("substep", True), ("tick", False), ("tick", True))


def _decomp_verdict(cells):
    """Arbre de decision gate sur (tick,True)=L2 (cellule CONNUE-composante). Isole le levier decisif du binding."""
    if not cells[("tick", True)]["composes"]:
        return "INCOHERENT"                     # (tick,on) ne compose pas -> contredit le verdict merge = artefact
    if cells[("substep", True)]["composes"]:
        return "CURRICULUM-SUFFISANT"           # curriculum seul rachete le credit substep faible -> bootstrap = levier
    if cells[("tick", False)]["composes"]:
        return "CREDIT-SUFFISANT"               # tick-return seul binde sans curriculum
    return "BOTH-NECESSARY"                      # ni curriculum seul ni tick seul ; les deux ensemble oui


def decompose_2x2(seeds=PILOT_SEEDS[:3], E0=16.0, M=32, n_episodes=120, n_warm=80, n_cold=80):
    """Decomposition factorielle 2x2 credit x curriculum sur le bras inesc a E0 fixe. Isole le levier decisif.
    Cellules connues : (substep,off)=L0, (tick,off)=L1, (tick,on)=L2 ; NOUVELLE = (substep,on) (tranche)."""
    P = replace(Params(), E0=E0)
    cells = {}
    for credit, curr in _DECOMP_CELLS:
        binds, survs = [], []
        for s in seeds:
            lr = _train_cell(credit, curr, "inesc", P, seed=int(s), M=M,
                             n_episodes=n_episodes, n_warm=n_warm, n_cold=n_cold)
            ev = evaluate_learner(lr, "inesc", P, seed=int(s) + 5000, M=M)
            binds.append(ev["binding_gap"])
            survs.append(ev["survival"])
        b, sv = float(np.median(binds)), float(np.median(survs))
        cells[(credit, curr)] = {"binding": b, "survival": sv, "composes": _rung_composes(b, sv)}
    return {"cells": cells, "verdict": _decomp_verdict(cells), "E0": E0}


def _report_decompose(res):
    lab = {("substep", False): "(substep, off ) L0", ("substep", True): "(substep, CURR)   ",
           ("tick", False): "(tick,    off ) L1", ("tick", True): "(tick,    CURR) L2"}
    print("\n=== EDR 201 — decomposition 2x2 credit x curriculum (bras inesc, E0=%.1f) ===" % res.get("E0", float("nan")))
    for key in _DECOMP_CELLS:
        c = res["cells"][key]
        print("    %s  binding=%+.3f  survie=%.3f  compose=%s" % (lab[key], c["binding"], c["survival"], c["composes"]))
    print("=== LEVIER DECISIF : %s ===" % res.get("verdict"))
    print("    CURRICULUM-SUFFISANT = bootstrap seul suffit | BOTH-NECESSARY = credit-horizon + bootstrap requis")
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `cd "c:/Users/robla/VScode_Project/AGAGI/.claude/worktrees/cos-edr201" && python -m pytest tests/sandbox/test_craft_or_starve_edr.py -k "train_cell or decompose_2x2" -q`
Expected: PASS — 2 tests verts.

- [ ] **Step 5: Commit**

```bash
cd "c:/Users/robla/VScode_Project/AGAGI/.claude/worktrees/cos-edr201"
git add -- tools/craft_or_starve_edr.py tests/sandbox/test_craft_or_starve_edr.py
git commit -m "feat(EDR-201): decomposition 2x2 credit x curriculum (_train_cell + decompose_2x2 + verdict gele) (T1)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Sweep de robustesse E0 + rapport + CLI `--edr201`

**Files:**
- Modify: `tools/craft_or_starve_edr.py` (APPEND + branche `__main__`)
- Test: `tests/sandbox/test_craft_or_starve_edr.py` (APPEND)

**Interfaces:**
- Consumes : `ladder_verdict, decompose_2x2, _report_decompose, _train_cell, evaluate_learner, Params, PILOT_SEEDS, np` (T1 / Phase B).
- Produces :
  - `robustness_sweep(seeds=PILOT_SEEDS[:3], e0_grid=(12.0,16.0,24.0,32.0), M=32, n_episodes=120, n_warm=80, n_cold=80) -> {"grid": [ {"E0","verdict","L0_composes","L1_composes","L2_composes"} ], "robust": bool, "verdict": str}`.
  - `_report_robustness(res)`.
  - Branche `--edr201` dans `__main__` (lance décomposition PUIS robustesse).

- [ ] **Step 1: Écrire les tests qui échouent**

Ajouter à `tests/sandbox/test_craft_or_starve_edr.py` :

```python
# === EDR 201 Task 2 : sweep de robustesse E0 ===

from tools.craft_or_starve_edr import robustness_sweep


def test_robustness_sweep_contract():
    res = robustness_sweep(seeds=(1000,), e0_grid=(16.0, 24.0), M=8, n_episodes=10, n_warm=10, n_cold=10)
    assert len(res["grid"]) == 2
    for row in res["grid"]:
        assert set(row) >= {"E0", "verdict", "L0_composes", "L1_composes", "L2_composes"}
    assert isinstance(res["robust"], bool)
    assert res["verdict"] in ("[2]-ROBUSTE", "[2]-FRAGILE")


def test_decomp_sanity_known_cells():
    # cellules CONNUES (jamais la cellule ouverte) : (tick,on)=L2 compose ; (substep,off)=L0 ne binde pas.
    # config validee (M=32, 80+80) pour que L2 binde de facon fiable.
    P = Params(E0=16.0)
    l2 = _train_cell("tick", True, "inesc", P, seed=1000, M=32, n_episodes=120, n_warm=80, n_cold=80)
    l0 = _train_cell("substep", False, "inesc", P, seed=1000, M=32, n_episodes=160, n_warm=80, n_cold=80)
    e2 = evaluate_learner(l2, "inesc", P, seed=6000, M=32)
    e0 = evaluate_learner(l0, "inesc", P, seed=6000, M=32)
    assert e2["binding_gap"] >= 0.5 and e2["survival"] >= 0.5   # L2 compose
    assert e0["binding_gap"] < 0.5                               # L0 ne binde pas
```

(`_train_cell` est déjà importé par le bloc de test T1 ; `evaluate_learner` est déjà importé par la section Phase B T2 du fichier de test. Le SEUL nouvel import à ajouter ici est `robustness_sweep`, montré ci-dessus.)

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `cd "c:/Users/robla/VScode_Project/AGAGI/.claude/worktrees/cos-edr201" && python -m pytest tests/sandbox/test_craft_or_starve_edr.py -k "robustness_sweep or decomp_sanity" -q`
Expected: FAIL — `ImportError: cannot import name 'robustness_sweep'`.

- [ ] **Step 3: Écrire le sweep + le rapport + la branche CLI**

Ajouter à `tools/craft_or_starve_edr.py` (après `_report_decompose`, avant `if __name__`) :

```python
def robustness_sweep(seeds=PILOT_SEEDS[:3], e0_grid=(12.0, 16.0, 24.0, 32.0), M=32, n_episodes=120, n_warm=80, n_cold=80):
    """Rejoue ladder_verdict sur le grid E0. [2]-ROBUSTE ssi le verdict par-E0 == '[2] CREDIT-ATTRIBUE' partout."""
    grid = []
    for e0 in e0_grid:
        res = ladder_verdict(seeds=seeds, E0=e0, M=M, n_episodes=n_episodes, n_warm=n_warm, n_cold=n_cold)
        r = res["rungs"]
        grid.append({"E0": e0, "verdict": res["verdict"],
                     "L0_composes": r["L0"]["composes"], "L1_composes": r["L1"]["composes"],
                     "L2_composes": r["L2"]["composes"]})
    robust = all(row["verdict"] == "[2] CREDIT-ATTRIBUE" for row in grid)
    return {"grid": grid, "robust": robust, "verdict": "[2]-ROBUSTE" if robust else "[2]-FRAGILE"}


def _report_robustness(res):
    print("\n=== EDR 201 — robustesse du verdict [2] sur le grid E0 (bras inesc) ===")
    print("    E0    verdict               L0/L1/L2 compose")
    for row in res["grid"]:
        print("    %5.1f  %-21s %s/%s/%s" % (row["E0"], row["verdict"],
              row["L0_composes"], row["L1_composes"], row["L2_composes"]))
    print("=== %s ===" % res.get("verdict"))
```

Et remplacer le bloc `__main__` existant :

```python
if __name__ == "__main__":
    import sys as _s
    if "--ladder" in _s.argv:
        _report_ladder(ladder_verdict())
    elif "--learner" in _s.argv:
        _report_learner(recalibrate_learner())
    else:
        _report(calibrate())
```

par :

```python
if __name__ == "__main__":
    import sys as _s
    if "--edr201" in _s.argv:
        _report_decompose(decompose_2x2())
        _report_robustness(robustness_sweep())
    elif "--ladder" in _s.argv:
        _report_ladder(ladder_verdict())
    elif "--learner" in _s.argv:
        _report_learner(recalibrate_learner())
    else:
        _report(calibrate())
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `cd "c:/Users/robla/VScode_Project/AGAGI/.claude/worktrees/cos-edr201" && python -m pytest tests/sandbox/test_craft_or_starve_edr.py -k "robustness_sweep or decomp_sanity" -q`
Expected: PASS — 2 tests verts. (Puis la suite complète doit rester verte : `python -m pytest tests/sandbox/test_craft_or_starve_edr.py -q`.)

- [ ] **Step 5: Commit**

```bash
cd "c:/Users/robla/VScode_Project/AGAGI/.claude/worktrees/cos-edr201"
git add -- tools/craft_or_starve_edr.py tests/sandbox/test_craft_or_starve_edr.py
git commit -m "feat(EDR-201): sweep robustesse E0 + rapport + CLI --edr201 (T2)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Run décisif (hors tâches TDD — action du contrôleur après revue finale)

Run: `cd "c:/Users/robla/VScode_Project/AGAGI/.claude/worktrees/cos-edr201" && python -m tools.craft_or_starve_edr --edr201` (2 passes byte-identiques).
- **Décomposition** : la cellule (substep, on) tranche → `CURRICULUM-SUFFISANT` (bootstrap = levier décisif) ou `BOTH-NECESSARY` (crédit-horizon + bootstrap requis). Les deux RAFFINENT [2] sans le contredire.
- **Robustesse** : `[2]-ROBUSTE` sur E0 {12,16,24,32} → verdict non config-fragile ; sinon fenêtre auditée.
- Un `INCOHERENT` ou un `[2]-FRAGILE` large = signal d'artefact à investiguer AVANT consolidation (a priori improbable vu la séparation stark 1.0-vs-0.0).
