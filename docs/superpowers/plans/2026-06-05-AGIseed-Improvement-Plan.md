# AGIseed Improvement Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement prioritized improvements identified in the audit: persistence abstraction layer, attention operator, interactive D3 dashboard, property-based testing & pre‑commit hooks, and up‑to‑date documentation.

**Architecture:** We will introduce a Repository interface for KuzuDB, add a simple attention mechanism to genome mutation, extend visualization with a D3 dashboard, add testing and linting hooks, and produce ARCHITECTURE.md plus tutorials.

**Tech Stack:** Python, KuzuDB, NumPy, pytest, hypothesis, ruff, black, mypy, D3.js (via HTML/JS), Git.

---